{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Datapass Workbench",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "Datapass: compile"
    }
  ]
}
