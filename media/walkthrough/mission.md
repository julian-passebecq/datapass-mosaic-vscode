# Missions

A mission is a ticket, like one from a colleague. Start one in the **Terminal Lab**: Datapass creates its folder
under `missions/` and opens a terminal there. Do the work with your own commands, then use **Check mission**
(above any file of the mission folder, or in the lab). A hidden checker reads the result; it never runs your
commands.
