[
 {
  "semantic": {
   "id": "zilla-001-popular-videos",
   "version": "1",
   "industry": "Video streaming",
   "learning_objectives": [
    "Filter rows; there is nothing to aggregate.",
    "Write the window as release_year >= 2024 - 5."
   ],
   "optimization": "A row filter with two conditions. The boundaries are the lesson: `view_count > 1000000` is strict and",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Popular recent videos",
   "difficulty": "easy",
   "topics": [
    "filtering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A video streaming platform keeps one row per video in `input_df`. Return the videos that have more than 1,000,000 views and were released in the last five years, taking 2024 as the current year: release years 2019 to 2024. Keep the input's columns.",
   "sections": [
    {
     "title": "Tables",
     "body": "input_df(video_id INTEGER, title VARCHAR, genre VARCHAR, release_year INTEGER, duration INTEGER, view_count BIGINT)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: video_id, title, genre, release_year, duration, view_count. One row per qualifying video; duplicate input rows stay duplicated. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "The view threshold is strict (exactly 1,000,000 views does not qualify) while 2019 is inside the window. A NULL view count is never above the threshold."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 1, \"Streaming Platform\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Output columns keep the input order (ZillaCode's printed sample sorted them alphabetically)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-001-popular-videos-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "boundaries"
   ],
   "hints": [
    "Filter rows; there is nothing to aggregate.",
    "Write the window as release_year >= 2024 - 5."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A row filter with two conditions. The boundaries are the lesson: `view_count > 1000000` is strict and `release_year >= 2019` is inclusive. Comparisons with NULL are unknown, so a video without a view count is filtered out in every engine.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "filtering"
   },
   "related_associations": [
    "zillacode/1"
   ],
   "recommendation": {
    "rank": 1,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "video_id",
     "title",
     "genre",
     "release_year",
     "duration",
     "view_count"
    ],
    "exact_schema": [
     "video_id",
     "title",
     "genre",
     "release_year",
     "duration",
     "view_count"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "input_df",
     "columns": {
      "video_id": "INTEGER",
      "title": "VARCHAR",
      "genre": "VARCHAR",
      "release_year": "INTEGER",
      "duration": "INTEGER",
      "view_count": "BIGINT"
     },
     "sample_rows": [
      {
       "video_id": 1,
       "title": "Amazing Adventure",
       "genre": "Action",
       "release_year": 2020,
       "duration": 120,
       "view_count": 2500000
      },
      {
       "video_id": 2,
       "title": "Sci-fi World",
       "genre": "Sci-fi",
       "release_year": 2018,
       "duration": 140,
       "view_count": 800000
      },
      {
       "video_id": 3,
       "title": "Mysterious Island",
       "genre": "Drama",
       "release_year": 2022,
       "duration": 115,
       "view_count": 1500000
      },
      {
       "video_id": 4,
       "title": "Uncharted Realms",
       "genre": "Action",
       "release_year": 2019,
       "duration": 134,
       "view_count": 3200000
      },
      {
       "video_id": 5,
       "title": "Journey to the Stars",
       "genre": "Sci-fi",
       "release_year": 2021,
       "duration": 128,
       "view_count": 1100000
      }
     ]
    }
   ],
   "output_schema": {
    "video_id": "value",
    "title": "value",
    "genre": "value",
    "release_year": "value",
    "duration": "value",
    "view_count": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 1 \"Streaming Platform\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-001",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Output columns keep the input order (ZillaCode's printed sample sorted them alphabetically).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: video_id, title, genre, release_year, duration, view_count\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "filtering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: video_id, title, genre, release_year, duration, view_count\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "filtering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pd.DataFrame(input_df)))\n",
    "supported_operations": [
     "filtering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pl.DataFrame(input_df)))\n",
    "supported_operations": [
     "filtering"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ninput_df = spark.table(\"input_df\")\ninput_df\n",
    "supported_operations": [
     "filtering"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'input_df') }}\n",
    "supported_operations": [
     "filtering"
    ],
    "model": "popular_videos"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-002-crm-orders",
   "version": "1",
   "industry": "CRM software",
   "learning_objectives": [
    "Join orders to customers and to products with inner joins.",
    "Rename email and category to the requested output names."
   ],
   "optimization": "Two inner joins keyed on customer_id and product_id, then a projection that builds customer_name with `||`",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "CRM order report",
   "difficulty": "easy",
   "topics": [
    "joins",
    "strings"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A CRM application stores customers, orders and products. Return one row per order with the customer's name (first and last name separated by a space), the customer's email, the product's name and category, and the order date. Orders whose customer or product is unknown are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "customers(customer_id INTEGER, first_name VARCHAR, last_name VARCHAR, email VARCHAR)\norders(order_id INTEGER, customer_id INTEGER, product_id INTEGER, order_date DATE)\nproducts(product_id INTEGER, product_name VARCHAR, category VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: order_id, customer_name, customer_email, product_name, product_category, order_date. One row per order that matches a customer and a product. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "If a name part is NULL, customer_name is NULL: string concatenation propagates NULL in SQL, pandas and Polars, but DuckDB's concat_ws (and Spark's) silently skip NULL arguments and would return 'Bo'."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 2, \"CRM SAAS Company\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The output uses the statement's names customer_email and product_category; ZillaCode's reference had kept email and category."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-002-crm-orders-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "missing-keys-and-null-name"
   ],
   "hints": [
    "Join orders to customers and to products with inner joins.",
    "Rename email and category to the requested output names."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Two inner joins keyed on customer_id and product_id, then a projection that builds customer_name with `||` (NULL-propagating) and renames the columns to the output contract.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "joins"
   },
   "related_associations": [
    "zillacode/2"
   ],
   "recommendation": {
    "rank": 2,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "order_id",
     "customer_name",
     "customer_email",
     "product_name",
     "product_category",
     "order_date"
    ],
    "exact_schema": [
     "order_id",
     "customer_name",
     "customer_email",
     "product_name",
     "product_category",
     "order_date"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "customers",
     "columns": {
      "customer_id": "INTEGER",
      "first_name": "VARCHAR",
      "last_name": "VARCHAR",
      "email": "VARCHAR"
     },
     "sample_rows": [
      {
       "customer_id": 1,
       "first_name": "John",
       "last_name": "Doe",
       "email": "john.doe@email.com"
      },
      {
       "customer_id": 2,
       "first_name": "Jane",
       "last_name": "Smith",
       "email": "jane.smith@email.com"
      }
     ]
    },
    {
     "name": "orders",
     "columns": {
      "order_id": "INTEGER",
      "customer_id": "INTEGER",
      "product_id": "INTEGER",
      "order_date": "DATE"
     },
     "sample_rows": [
      {
       "order_id": 1001,
       "customer_id": 1,
       "product_id": 101,
       "order_date": "2023-01-10"
      },
      {
       "order_id": 1002,
       "customer_id": 2,
       "product_id": 102,
       "order_date": "2023-01-11"
      }
     ]
    },
    {
     "name": "products",
     "columns": {
      "product_id": "INTEGER",
      "product_name": "VARCHAR",
      "category": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": 101,
       "product_name": "Product A",
       "category": "Category1"
      },
      {
       "product_id": 102,
       "product_name": "Product B",
       "category": "Category2"
      }
     ]
    }
   ],
   "output_schema": {
    "order_id": "value",
    "customer_name": "value",
    "customer_email": "value",
    "product_name": "value",
    "product_category": "value",
    "order_date": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 2 \"CRM SAAS Company\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-002",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The output uses the statement's names customer_email and product_category; ZillaCode's reference had kept email and category.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: order_id, customer_name, customer_email, product_name, product_category, order_date\nSELECT *\nFROM customers\n",
    "supported_operations": [
     "joins",
     "strings"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: order_id, customer_name, customer_email, product_name, product_category, order_date\nSELECT *\nFROM customers\n",
    "supported_operations": [
     "joins",
     "strings"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(customers, orders, products):\n    # Transform the input DataFrames into the result described in the brief.\n    return customers\n\n\ndisplay(etl(pd.DataFrame(customers), pd.DataFrame(orders), pd.DataFrame(products)))\n",
    "supported_operations": [
     "joins",
     "strings"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(customers, orders, products):\n    # Transform the input DataFrames into the result described in the brief.\n    return customers\n\n\ndisplay(etl(pl.DataFrame(customers), pl.DataFrame(orders), pl.DataFrame(products)))\n",
    "supported_operations": [
     "joins",
     "strings"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'customers') }}\n",
    "supported_operations": [
     "joins",
     "strings"
    ],
    "model": "crm_orders"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-003-landlord-income",
   "version": "1",
   "industry": "Property management",
   "learning_objectives": [
    "Remove exact duplicate rows from both tables first (DISTINCT, drop_duplicates, unique).",
    "Aggregate the rent per landlord before joining the names."
   ],
   "optimization": "Deduplicate each feed, sum the rent per landlord, then join the landlord names. Summing after the join would",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Rental income per landlord",
   "difficulty": "medium",
   "topics": [
    "aggregation",
    "deduplication",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A property manager keeps the properties it manages and their landlords. Return, for each landlord who owns at least one property, the landlord's full name (first and last name separated by a space) and the total monthly rent of their properties. The feeds may contain exact duplicate rows (the same record loaded twice): count each property and each landlord once. Properties whose landlord is not in landlords_df are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "properties_df(property_id INTEGER, landlord_id INTEGER, property_type VARCHAR, rent DOUBLE, square_feet INTEGER, city VARCHAR)\nlandlords_df(landlord_id INTEGER, first_name VARCHAR, last_name VARCHAR, email VARCHAR, phone VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: landlord_id, landlord_name, total_rental_income. One row per landlord with at least one property. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Joining before removing duplicate rows multiplies the rent: a landlord listed twice doubles its income. A NULL rent is ignored by SUM, not treated as a missing total."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 3, \"Property Management Company\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The statement's warning about duplicates became a contract: exact duplicate rows count once (ZillaCode's reference summed them)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-003-landlord-income-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "duplicates-and-null-rent"
   ],
   "hints": [
    "Remove exact duplicate rows from both tables first (DISTINCT, drop_duplicates, unique).",
    "Aggregate the rent per landlord before joining the names."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Deduplicate each feed, sum the rent per landlord, then join the landlord names. Summing after the join would count every property once per matching landlord row. ZillaCode's reference pivoted the rent by property type first; the total is the same, so Datapass sums directly.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/3"
   ],
   "recommendation": {
    "rank": 3,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "landlord_id",
     "landlord_name",
     "total_rental_income"
    ],
    "exact_schema": [
     "landlord_id",
     "landlord_name",
     "total_rental_income"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "properties_df",
     "columns": {
      "property_id": "INTEGER",
      "landlord_id": "INTEGER",
      "property_type": "VARCHAR",
      "rent": "DOUBLE",
      "square_feet": "INTEGER",
      "city": "VARCHAR"
     },
     "sample_rows": [
      {
       "property_id": 1,
       "landlord_id": 101,
       "property_type": "Apartment",
       "rent": 1500.0,
       "square_feet": 1000,
       "city": "Seattle"
      },
      {
       "property_id": 2,
       "landlord_id": 101,
       "property_type": "Condo",
       "rent": 1200.0,
       "square_feet": 800,
       "city": "Seattle"
      },
      {
       "property_id": 3,
       "landlord_id": 102,
       "property_type": "House",
       "rent": 2000.0,
       "square_feet": 1500,
       "city": "Bellevue"
      },
      {
       "property_id": 4,
       "landlord_id": 103,
       "property_type": "Apartment",
       "rent": 1800.0,
       "square_feet": 1200,
       "city": "Redmond"
      },
      {
       "property_id": 5,
       "landlord_id": 103,
       "property_type": "Condo",
       "rent": 1000.0,
       "square_feet": 700,
       "city": "Redmond"
      }
     ]
    },
    {
     "name": "landlords_df",
     "columns": {
      "landlord_id": "INTEGER",
      "first_name": "VARCHAR",
      "last_name": "VARCHAR",
      "email": "VARCHAR",
      "phone": "VARCHAR"
     },
     "sample_rows": [
      {
       "landlord_id": 101,
       "first_name": "John",
       "last_name": "Smith",
       "email": "john.smith@example.com",
       "phone": "555-123-4567"
      },
      {
       "landlord_id": 102,
       "first_name": "Jane",
       "last_name": "Doe",
       "email": "jane.doe@example.com",
       "phone": "555-234-5678"
      },
      {
       "landlord_id": 103,
       "first_name": "Bob",
       "last_name": "Johnson",
       "email": "bob.johnson@example.com",
       "phone": "555-345-6789"
      },
      {
       "landlord_id": 104,
       "first_name": "Mary",
       "last_name": "Williams",
       "email": "mary.williams@example.com",
       "phone": "555-456-7890"
      },
      {
       "landlord_id": 105,
       "first_name": "Jack",
       "last_name": "Brown",
       "email": "jack.brown@example.com",
       "phone": "555-567-8901"
      }
     ]
    }
   ],
   "output_schema": {
    "landlord_id": "value",
    "landlord_name": "value",
    "total_rental_income": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 3 \"Property Management Company\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-003",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The statement's warning about duplicates became a contract: exact duplicate rows count once (ZillaCode's reference summed them).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: landlord_id, landlord_name, total_rental_income\nSELECT *\nFROM properties_df\n",
    "supported_operations": [
     "aggregation",
     "deduplication",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: landlord_id, landlord_name, total_rental_income\nSELECT *\nFROM properties_df\n",
    "supported_operations": [
     "aggregation",
     "deduplication",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(properties_df, landlords_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return properties_df\n\n\ndisplay(etl(pd.DataFrame(properties_df), pd.DataFrame(landlords_df)))\n",
    "supported_operations": [
     "aggregation",
     "deduplication",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(properties_df, landlords_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return properties_df\n\n\ndisplay(etl(pl.DataFrame(properties_df), pl.DataFrame(landlords_df)))\n",
    "supported_operations": [
     "aggregation",
     "deduplication",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'properties_df') }}\n",
    "supported_operations": [
     "aggregation",
     "deduplication",
     "joins"
    ],
    "model": "landlord_income"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-004-pii-masking",
   "version": "1",
   "industry": "Social media",
   "learning_objectives": [
    "Cast the phone to text before cutting it.",
    "Split the email on '@' and keep the second part."
   ],
   "optimization": "Two string transformations. The phone is an integer, so convert it to text first; keep NULL-propagating",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Mask personal data",
   "difficulty": "easy",
   "topics": [
    "strings",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A social media company must stop exposing personal data. For every user, return the domain of the email address (the text after '@') and the phone number as text with its first six digits replaced by six asterisks (phone numbers have ten digits, so 5551234567 becomes ******4567). A missing email or phone stays missing.",
   "sections": [
    {
     "title": "Tables",
     "body": "input_df(user_id INTEGER, email VARCHAR, phone BIGINT)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: user_id, email_domain, anon_phone. One row per input row. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "A missing phone must give a NULL anon_phone. DuckDB's concat() skips NULL arguments and returns '******', and in pandas a NULL turns the integer column into floats, so str() gives '5551234567.0'."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 4, \"Social Media PII\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: A NULL email or phone now has a defined result (NULL); ZillaCode's references did not handle NULLs."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-004-pii-masking-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "missing-values"
   ],
   "hints": [
    "Cast the phone to text before cutting it.",
    "Split the email on '@' and keep the second part."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Two string transformations. The phone is an integer, so convert it to text first; keep NULL-propagating concatenation (`||` in SQL) so an unknown phone stays unknown.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "strings"
   },
   "related_associations": [
    "zillacode/4"
   ],
   "recommendation": {
    "rank": 4,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "user_id",
     "email_domain",
     "anon_phone"
    ],
    "exact_schema": [
     "user_id",
     "email_domain",
     "anon_phone"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "input_df",
     "columns": {
      "user_id": "INTEGER",
      "email": "VARCHAR",
      "phone": "BIGINT"
     },
     "sample_rows": [
      {
       "user_id": 1,
       "email": "alice@example.com",
       "phone": 5551234567
      },
      {
       "user_id": 2,
       "email": "bob@domain.net",
       "phone": 5559876543
      },
      {
       "user_id": 3,
       "email": "carol@email.org",
       "phone": 5551239876
      },
      {
       "user_id": 4,
       "email": "dave@site.com",
       "phone": 5554567890
      },
      {
       "user_id": 5,
       "email": "eve@platform.io",
       "phone": 5559871234
      }
     ]
    }
   ],
   "output_schema": {
    "user_id": "value",
    "email_domain": "value",
    "anon_phone": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 4 \"Social Media PII\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-004",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "A NULL email or phone now has a defined result (NULL); ZillaCode's references did not handle NULLs.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: user_id, email_domain, anon_phone\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: user_id, email_domain, anon_phone\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pd.DataFrame(input_df)))\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pl.DataFrame(input_df)))\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'input_df') }}\n",
    "supported_operations": [
     "strings",
     "nulls"
    ],
    "model": "pii_masking"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-005-category-orders",
   "version": "1",
   "industry": "E-commerce",
   "learning_objectives": [
    "Join orders to products first, then group by category.",
    "Count orders, not products."
   ],
   "optimization": "Inner join orders to products, then one GROUP BY category with AVG(price) and COUNT(order_id). Averaging the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Average price and orders per category",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "An e-commerce platform has products (with a category and a price) and orders (one product per order). For each category that has at least one order, return the average price over the category's orders (a product ordered twice counts twice) and the number of orders. Orders for unknown products are ignored.",
   "sections": [
    {
     "title": "Tables",
     "body": "products_df(product_id INTEGER, category VARCHAR, price DOUBLE)\norders_df(order_id INTEGER, product_id INTEGER, quantity INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: category, avg_price, total_orders_count. One row per category with at least one order. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "avg_price is averaged over the joined order rows, not over the catalog: a product nobody ordered does not count, and a product ordered twice weighs twice."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 5, \"E-Commerce Platform\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-005-category-orders-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "unordered-and-unknown"
   ],
   "hints": [
    "Join orders to products first, then group by category.",
    "Count orders, not products."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Inner join orders to products, then one GROUP BY category with AVG(price) and COUNT(order_id). Averaging the products table instead gives a different number as soon as order counts differ between products.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/5"
   ],
   "recommendation": {
    "rank": 5,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "category",
     "avg_price",
     "total_orders_count"
    ],
    "exact_schema": [
     "category",
     "avg_price",
     "total_orders_count"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "products_df",
     "columns": {
      "product_id": "INTEGER",
      "category": "VARCHAR",
      "price": "DOUBLE"
     },
     "sample_rows": [
      {
       "product_id": 1,
       "category": "Apparel",
       "price": 25.99
      },
      {
       "product_id": 2,
       "category": "Apparel",
       "price": 35.99
      },
      {
       "product_id": 3,
       "category": "Footwear",
       "price": 50.0
      },
      {
       "product_id": 4,
       "category": "Footwear",
       "price": 75.0
      },
      {
       "product_id": 5,
       "category": "Apparel",
       "price": 20.0
      }
     ]
    },
    {
     "name": "orders_df",
     "columns": {
      "order_id": "INTEGER",
      "product_id": "INTEGER",
      "quantity": "INTEGER"
     },
     "sample_rows": [
      {
       "order_id": 101,
       "product_id": 1,
       "quantity": 2
      },
      {
       "order_id": 102,
       "product_id": 2,
       "quantity": 1
      },
      {
       "order_id": 103,
       "product_id": 1,
       "quantity": 3
      },
      {
       "order_id": 104,
       "product_id": 3,
       "quantity": 1
      },
      {
       "order_id": 105,
       "product_id": 4,
       "quantity": 2
      }
     ]
    }
   ],
   "output_schema": {
    "category": "value",
    "avg_price": "value",
    "total_orders_count": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 5 \"E-Commerce Platform\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-005",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: category, avg_price, total_orders_count\nSELECT *\nFROM products_df\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: category, avg_price, total_orders_count\nSELECT *\nFROM products_df\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(products_df, orders_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return products_df\n\n\ndisplay(etl(pd.DataFrame(products_df), pd.DataFrame(orders_df)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(products_df, orders_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return products_df\n\n\ndisplay(etl(pl.DataFrame(products_df), pl.DataFrame(orders_df)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nproducts_df = spark.table(\"products_df\")\norders_df = spark.table(\"orders_df\")\nproducts_df\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'products_df') }}\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ],
    "model": "category_orders"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-006-replace-word",
   "version": "1",
   "industry": "Social media",
   "learning_objectives": [
    "A plain string replace is enough; no regular expression is needed.",
    "Keep the other columns and their order."
   ],
   "optimization": "replace(text, 'Python', 'PySpark') replaces all occurrences in DuckDB and Snowflake. Regex functions differ:",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Replace a word in posts",
   "difficulty": "easy",
   "topics": [
    "strings"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return the social media posts unchanged except for their text, where every occurrence of the word \"Python\" is replaced by \"PySpark\". The replacement is case-sensitive: \"python\" stays as it is.",
   "sections": [
    {
     "title": "Tables",
     "body": "social_media(id INTEGER, text VARCHAR, date VARCHAR, likes INTEGER, comments INTEGER, shares INTEGER, platform VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: id, text, date, likes, comments, shares, platform. One row per post. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Replace every occurrence: DuckDB's regexp_replace without the 'g' flag and Polars' str.replace change only the first one. (Snowflake's REGEXP_REPLACE replaces all by default.)"
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 6, \"Correcting Social Media Posts\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The date stays text, as in the statement."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-006-replace-word-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "twice-and-lowercase"
   ],
   "hints": [
    "A plain string replace is enough; no regular expression is needed.",
    "Keep the other columns and their order."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "replace(text, 'Python', 'PySpark') replaces all occurrences in DuckDB and Snowflake. Regex functions differ: DuckDB's regexp_replace needs the 'g' flag and Polars needs str.replace_all.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "strings"
   },
   "related_associations": [
    "zillacode/6"
   ],
   "recommendation": {
    "rank": 6,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "id",
     "text",
     "date",
     "likes",
     "comments",
     "shares",
     "platform"
    ],
    "exact_schema": [
     "id",
     "text",
     "date",
     "likes",
     "comments",
     "shares",
     "platform"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "social_media",
     "columns": {
      "id": "INTEGER",
      "text": "VARCHAR",
      "date": "VARCHAR",
      "likes": "INTEGER",
      "comments": "INTEGER",
      "shares": "INTEGER",
      "platform": "VARCHAR"
     },
     "sample_rows": [
      {
       "id": 1,
       "text": "This is a Python post.",
       "date": "2022-03-01",
       "likes": 10,
       "comments": 3,
       "shares": 2,
       "platform": "Twitter"
      },
      {
       "id": 2,
       "text": "Another post about Python.",
       "date": "2022-03-02",
       "likes": 20,
       "comments": 5,
       "shares": 3,
       "platform": "Instagram"
      },
      {
       "id": 3,
       "text": "Python is great for data analysis.",
       "date": "2022-03-03",
       "likes": 30,
       "comments": 2,
       "shares": 4,
       "platform": "Facebook"
      },
      {
       "id": 4,
       "text": "I'm learning Python for machine learning.",
       "date": "2022-03-04",
       "likes": 40,
       "comments": 7,
       "shares": 5,
       "platform": "Twitter"
      },
      {
       "id": 5,
       "text": "Python vs. R for data science.",
       "date": "2022-03-05",
       "likes": 50,
       "comments": 9,
       "shares": 6,
       "platform": "Instagram"
      },
      {
       "id": 6,
       "text": "Python web development is awesome.",
       "date": "2022-03-06",
       "likes": 60,
       "comments": 1,
       "shares": 1,
       "platform": "Facebook"
      },
      {
       "id": 7,
       "text": "Python for finance.",
       "date": "2022-03-07",
       "likes": 70,
       "comments": 4,
       "shares": 3,
       "platform": "Twitter"
      },
      {
       "id": 8,
       "text": "Python libraries for data visualization.",
       "date": "2022-03-08",
       "likes": 80,
       "comments": 6,
       "shares": 2,
       "platform": "Instagram"
      },
      {
       "id": 9,
       "text": "Why Python is the best programming language.",
       "date": "2022-03-09",
       "likes": 90,
       "comments": 3,
       "shares": 1,
       "platform": "Facebook"
      },
      {
       "id": 10,
       "text": "Python for data engineering.",
       "date": "2022-03-10",
       "likes": 100,
       "comments": 8,
       "shares": 7,
       "platform": "Twitter"
      }
     ]
    }
   ],
   "output_schema": {
    "id": "value",
    "text": "value",
    "date": "value",
    "likes": "value",
    "comments": "value",
    "shares": "value",
    "platform": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 6 \"Correcting Social Media Posts\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-006",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit text replacement projection and global regex replacement avoid duplicate output column names. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The date stays text, as in the statement.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: id, text, date, likes, comments, shares, platform\nSELECT *\nFROM social_media\n",
    "supported_operations": [
     "strings"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: id, text, date, likes, comments, shares, platform\nSELECT *\nFROM social_media\n",
    "supported_operations": [
     "strings"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(social_media):\n    # Transform the input DataFrames into the result described in the brief.\n    return social_media\n\n\ndisplay(etl(pd.DataFrame(social_media)))\n",
    "supported_operations": [
     "strings"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(social_media):\n    # Transform the input DataFrames into the result described in the brief.\n    return social_media\n\n\ndisplay(etl(pl.DataFrame(social_media)))\n",
    "supported_operations": [
     "strings"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'social_media') }}\n",
    "supported_operations": [
     "strings"
    ],
    "model": "replace_word"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-007-top-products",
   "version": "1",
   "industry": "Manufacturing",
   "learning_objectives": [
    "Sum the revenue per product before ranking.",
    "Rank within the category: PARTITION BY category."
   ],
   "optimization": "Aggregate sales per product, join the product attributes, then DENSE_RANK() OVER (PARTITION BY category",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Top products per category without gaps",
   "difficulty": "medium",
   "topics": [
    "window-functions",
    "ranking",
    "aggregation"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A manufacturer wants its best products. Compute each product's revenue (the sum of its sales), rank products within their category by revenue, highest first, with no gaps in the ranking (tied products share a rank and the next rank follows immediately), and return the products ranked 1 to 3 in each category. Products without sales are not ranked.",
   "sections": [
    {
     "title": "Tables",
     "body": "products(product_id INTEGER, category VARCHAR, product_name VARCHAR)\nsales(sale_id INTEGER, product_id INTEGER, quantity INTEGER, revenue DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: category, product_name, revenue, rank. One row per product with a rank of 3 or better; ties can return more than three products per category. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Without gaps means DENSE_RANK: with RANK, two products tied at the top push the next one to rank 3; ROW_NUMBER breaks ties arbitrarily and drops tied products."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 7, \"Manufacturing Plant\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: revenue stays a DOUBLE (the statement said integer, the data has decimals); ties are part of the hidden checks."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-007-top-products-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "ties"
   ],
   "hints": [
    "Sum the revenue per product before ranking.",
    "Rank within the category: PARTITION BY category."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Aggregate sales per product, join the product attributes, then DENSE_RANK() OVER (PARTITION BY category ORDER BY revenue DESC) and keep ranks up to 3. Snowflake can filter the window in the same SELECT with QUALIFY.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/7"
   ],
   "recommendation": {
    "rank": 7,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "category",
     "product_name",
     "revenue",
     "rank"
    ],
    "exact_schema": [
     "category",
     "product_name",
     "revenue",
     "rank"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "products",
     "columns": {
      "product_id": "INTEGER",
      "category": "VARCHAR",
      "product_name": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": 1,
       "category": "A",
       "product_name": "Product1"
      },
      {
       "product_id": 2,
       "category": "A",
       "product_name": "Product2"
      },
      {
       "product_id": 3,
       "category": "A",
       "product_name": "Product3"
      },
      {
       "product_id": 4,
       "category": "B",
       "product_name": "Product4"
      },
      {
       "product_id": 5,
       "category": "B",
       "product_name": "Product5"
      },
      {
       "product_id": 6,
       "category": "B",
       "product_name": "Product6"
      },
      {
       "product_id": 7,
       "category": "C",
       "product_name": "Product7"
      },
      {
       "product_id": 8,
       "category": "C",
       "product_name": "Product8"
      },
      {
       "product_id": 9,
       "category": "C",
       "product_name": "Product9"
      }
     ]
    },
    {
     "name": "sales",
     "columns": {
      "sale_id": "INTEGER",
      "product_id": "INTEGER",
      "quantity": "INTEGER",
      "revenue": "DOUBLE"
     },
     "sample_rows": [
      {
       "sale_id": 1,
       "product_id": 1,
       "quantity": 10,
       "revenue": 100.0
      },
      {
       "sale_id": 2,
       "product_id": 2,
       "quantity": 8,
       "revenue": 120.0
      },
      {
       "sale_id": 3,
       "product_id": 3,
       "quantity": 12,
       "revenue": 180.0
      },
      {
       "sale_id": 4,
       "product_id": 4,
       "quantity": 5,
       "revenue": 50.0
      },
      {
       "sale_id": 5,
       "product_id": 5,
       "quantity": 3,
       "revenue": 30.0
      },
      {
       "sale_id": 6,
       "product_id": 6,
       "quantity": 7,
       "revenue": 70.0
      },
      {
       "sale_id": 7,
       "product_id": 7,
       "quantity": 15,
       "revenue": 150.0
      },
      {
       "sale_id": 8,
       "product_id": 8,
       "quantity": 10,
       "revenue": 100.0
      },
      {
       "sale_id": 9,
       "product_id": 9,
       "quantity": 8,
       "revenue": 80.0
      }
     ]
    }
   ],
   "output_schema": {
    "category": "value",
    "product_name": "value",
    "revenue": "value",
    "rank": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 7 \"Manufacturing Plant\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-007",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "revenue stays a DOUBLE (the statement said integer, the data has decimals); ties are part of the hidden checks.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: category, product_name, revenue, rank\nSELECT *\nFROM products\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "aggregation"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: category, product_name, revenue, rank\nSELECT *\nFROM products\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "aggregation"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(products, sales):\n    # Transform the input DataFrames into the result described in the brief.\n    return products\n\n\ndisplay(etl(pd.DataFrame(products), pd.DataFrame(sales)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "aggregation"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(products, sales):\n    # Transform the input DataFrames into the result described in the brief.\n    return products\n\n\ndisplay(etl(pl.DataFrame(products), pl.DataFrame(sales)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "aggregation"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'products') }}\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "aggregation"
    ],
    "model": "top_products"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-008-calls-per-day",
   "version": "1",
   "industry": "Telecommunications",
   "learning_objectives": [
    "Join the calls to the customers first.",
    "Group by date."
   ],
   "optimization": "Inner join calls to customers (which drops unknown callers), then GROUP BY date with COUNT(DISTINCT cust_id)",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Callers and call time per day",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "distinct"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A telecom company logs calls (the date is text, 'yyyy-MM-dd') and keeps a customer list. For each date, return the number of distinct customers who called and the total call duration in seconds. Only calls from customers present in customers_df count.",
   "sections": [
    {
     "title": "Tables",
     "body": "calls_df(call_id INTEGER, cust_id INTEGER, date VARCHAR, duration INTEGER)\ncustomers_df(cust_id INTEGER, name VARCHAR, state VARCHAR, tenure INTEGER, occupation VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: date, num_customers, total_duration. One row per date with at least one call from a known customer. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "A customer who calls twice on the same day is one customer: COUNT(DISTINCT cust_id), not COUNT(*). Calls from unknown customers are dropped by the join, including their duration."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 8, \"Call Center\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-008-calls-per-day-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "repeat-and-unknown-callers"
   ],
   "hints": [
    "Join the calls to the customers first.",
    "Group by date."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Inner join calls to customers (which drops unknown callers), then GROUP BY date with COUNT(DISTINCT cust_id) and SUM(duration).",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/8"
   ],
   "recommendation": {
    "rank": 8,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "date",
     "num_customers",
     "total_duration"
    ],
    "exact_schema": [
     "date",
     "num_customers",
     "total_duration"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "calls_df",
     "columns": {
      "call_id": "INTEGER",
      "cust_id": "INTEGER",
      "date": "VARCHAR",
      "duration": "INTEGER"
     },
     "sample_rows": [
      {
       "call_id": 1,
       "cust_id": 1,
       "date": "2022-01-01",
       "duration": 100
      },
      {
       "call_id": 2,
       "cust_id": 2,
       "date": "2022-01-01",
       "duration": 200
      },
      {
       "call_id": 3,
       "cust_id": 1,
       "date": "2022-01-02",
       "duration": 150
      },
      {
       "call_id": 4,
       "cust_id": 3,
       "date": "2022-01-02",
       "duration": 300
      },
      {
       "call_id": 5,
       "cust_id": 2,
       "date": "2022-01-03",
       "duration": 50
      }
     ]
    },
    {
     "name": "customers_df",
     "columns": {
      "cust_id": "INTEGER",
      "name": "VARCHAR",
      "state": "VARCHAR",
      "tenure": "INTEGER",
      "occupation": "VARCHAR"
     },
     "sample_rows": [
      {
       "cust_id": 1,
       "name": "Alice",
       "state": "NY",
       "tenure": 10,
       "occupation": "doctor"
      },
      {
       "cust_id": 2,
       "name": "Bob",
       "state": "CA",
       "tenure": 12,
       "occupation": "lawyer"
      },
      {
       "cust_id": 3,
       "name": "Charlie",
       "state": "TX",
       "tenure": 6,
       "occupation": "engineer"
      }
     ]
    }
   ],
   "output_schema": {
    "date": "value",
    "num_customers": "value",
    "total_duration": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 8 \"Call Center\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-008",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: date, num_customers, total_duration\nSELECT *\nFROM calls_df\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: date, num_customers, total_duration\nSELECT *\nFROM calls_df\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(calls_df, customers_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return calls_df\n\n\ndisplay(etl(pd.DataFrame(calls_df), pd.DataFrame(customers_df)))\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(calls_df, customers_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return calls_df\n\n\ndisplay(etl(pl.DataFrame(calls_df), pl.DataFrame(customers_df)))\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ncalls_df = spark.table(\"calls_df\")\ncustomers_df = spark.table(\"customers_df\")\ncalls_df\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'calls_df') }}\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ],
    "model": "calls_per_day"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-009-author-numbers",
   "version": "1",
   "industry": "AI research",
   "learning_objectives": [
    "ROW_NUMBER() OVER (PARTITION BY paper_id ORDER BY author_id).",
    "Keep the authors whose paper_id appears in research_papers without joining its rows."
   ],
   "optimization": "A semi-join keeps each author at most once however many times the paper is listed; ROW_NUMBER numbers the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Number the authors of each paper",
   "difficulty": "easy",
   "topics": [
    "window-functions",
    "semi-join"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A lab tracks its research papers and their authors. For the authors of papers listed in research_papers, number the authors within each paper by author_id in ascending order, starting at 1. author_id is text.",
   "sections": [
    {
     "title": "Tables",
     "body": "research_papers(paper_id VARCHAR, title VARCHAR, year INTEGER)\nauthors(paper_id VARCHAR, author_id VARCHAR, name VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: paper_id, author_id, name, row_number. One row per author row of a known paper. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "author_id is text, so 'A10' sorts before 'A2'. research_papers may list a paper twice: filter with IN or EXISTS (a semi-join) instead of joining, or every author is duplicated."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 9, \"AI Research\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Only authors of papers present in research_papers are numbered (a semi-join); ZillaCode's reference did not read research_papers."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-009-author-numbers-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "text-order-and-duplicate-paper"
   ],
   "hints": [
    "ROW_NUMBER() OVER (PARTITION BY paper_id ORDER BY author_id).",
    "Keep the authors whose paper_id appears in research_papers without joining its rows."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A semi-join keeps each author at most once however many times the paper is listed; ROW_NUMBER numbers the authors of each paper in text order. ZillaCode's reference ignored research_papers; Datapass uses it as the list of known papers.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/9"
   ],
   "recommendation": {
    "rank": 9,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "paper_id",
     "author_id",
     "name",
     "row_number"
    ],
    "exact_schema": [
     "paper_id",
     "author_id",
     "name",
     "row_number"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "research_papers",
     "columns": {
      "paper_id": "VARCHAR",
      "title": "VARCHAR",
      "year": "INTEGER"
     },
     "sample_rows": [
      {
       "paper_id": "P1",
       "title": "Deep Learning Techniques in AI",
       "year": 2019
      },
      {
       "paper_id": "P2",
       "title": "Reinforcement Learning for Robotics",
       "year": 2020
      },
      {
       "paper_id": "P3",
       "title": "Natural Language Processing Advances",
       "year": 2021
      }
     ]
    },
    {
     "name": "authors",
     "columns": {
      "paper_id": "VARCHAR",
      "author_id": "VARCHAR",
      "name": "VARCHAR"
     },
     "sample_rows": [
      {
       "paper_id": "P1",
       "author_id": "A1",
       "name": "Alice Smith"
      },
      {
       "paper_id": "P1",
       "author_id": "A2",
       "name": "Bob Johnson"
      },
      {
       "paper_id": "P2",
       "author_id": "A3",
       "name": "Carol Williams"
      },
      {
       "paper_id": "P2",
       "author_id": "A4",
       "name": "David Brown"
      },
      {
       "paper_id": "P2",
       "author_id": "A5",
       "name": "Eva Davis"
      },
      {
       "paper_id": "P3",
       "author_id": "A6",
       "name": "Frank Wilson"
      },
      {
       "paper_id": "P3",
       "author_id": "A7",
       "name": "Grace Lee"
      }
     ]
    }
   ],
   "output_schema": {
    "paper_id": "value",
    "author_id": "value",
    "name": "value",
    "row_number": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 9 \"AI Research\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-009",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Only authors of papers present in research_papers are numbered (a semi-join); ZillaCode's reference did not read research_papers.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: paper_id, author_id, name, row_number\nSELECT *\nFROM research_papers\n",
    "supported_operations": [
     "window-functions",
     "semi-join"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: paper_id, author_id, name, row_number\nSELECT *\nFROM research_papers\n",
    "supported_operations": [
     "window-functions",
     "semi-join"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(research_papers, authors):\n    # Transform the input DataFrames into the result described in the brief.\n    return research_papers\n\n\ndisplay(etl(pd.DataFrame(research_papers), pd.DataFrame(authors)))\n",
    "supported_operations": [
     "window-functions",
     "semi-join"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(research_papers, authors):\n    # Transform the input DataFrames into the result described in the brief.\n    return research_papers\n\n\ndisplay(etl(pl.DataFrame(research_papers), pl.DataFrame(authors)))\n",
    "supported_operations": [
     "window-functions",
     "semi-join"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nresearch_papers = spark.table(\"research_papers\")\nauthors = spark.table(\"authors\")\nresearch_papers\n",
    "supported_operations": [
     "window-functions",
     "semi-join"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'research_papers') }}\n",
    "supported_operations": [
     "window-functions",
     "semi-join"
    ],
    "model": "author_numbers"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-010-product-summary",
   "version": "1",
   "industry": "Food and beverage",
   "learning_objectives": [
    "Two aggregations (sales per product, stock per product), then two LEFT JOINs.",
    "COALESCE the missing totals to 0."
   ],
   "optimization": "Sales and inventory are both one-to-many from products. Pre-aggregate each to one row per product, LEFT JOIN",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Sales and stock per product",
   "difficulty": "medium",
   "topics": [
    "aggregation",
    "left-join",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For every product, return its name and category, the total quantity sold and revenue (over all its sales), and the total stock across all warehouses. A product without sales or without stock gets 0 in those columns.",
   "sections": [
    {
     "title": "Tables",
     "body": "products(product_id INTEGER, name VARCHAR, category VARCHAR)\nsales(sale_id INTEGER, product_id INTEGER, quantity INTEGER, revenue DOUBLE)\ninventory(product_id INTEGER, stock INTEGER, warehouse VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: product_id, name, category, total_quantity, total_revenue, total_stock. One row per product. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Aggregate sales and inventory separately before joining them: joining both to the product first multiplies each sale by the number of warehouses (fan-out)."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 10, \"Food and Beverage Sales\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: total_revenue stays a DOUBLE: the revenue data has decimals."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-010-product-summary-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "fan-out"
   ],
   "hints": [
    "Two aggregations (sales per product, stock per product), then two LEFT JOINs.",
    "COALESCE the missing totals to 0."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Sales and inventory are both one-to-many from products. Pre-aggregate each to one row per product, LEFT JOIN both to products, and replace missing totals with 0 (COALESCE, or ZEROIFNULL in Snowflake).",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/10"
   ],
   "recommendation": {
    "rank": 10,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "product_id",
     "name",
     "category",
     "total_quantity",
     "total_revenue",
     "total_stock"
    ],
    "exact_schema": [
     "product_id",
     "name",
     "category",
     "total_quantity",
     "total_revenue",
     "total_stock"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "products",
     "columns": {
      "product_id": "INTEGER",
      "name": "VARCHAR",
      "category": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": 1,
       "name": "Apple Juice",
       "category": "Beverages"
      },
      {
       "product_id": 2,
       "name": "Orange Juice",
       "category": "Beverages"
      },
      {
       "product_id": 3,
       "name": "Chocolate Bar",
       "category": "Snacks"
      },
      {
       "product_id": 4,
       "name": "Potato Chips",
       "category": "Snacks"
      },
      {
       "product_id": 5,
       "name": "Fresh Strawberries",
       "category": "Fruits"
      }
     ]
    },
    {
     "name": "sales",
     "columns": {
      "sale_id": "INTEGER",
      "product_id": "INTEGER",
      "quantity": "INTEGER",
      "revenue": "DOUBLE"
     },
     "sample_rows": [
      {
       "sale_id": 1,
       "product_id": 1,
       "quantity": 10,
       "revenue": 20.0
      },
      {
       "sale_id": 2,
       "product_id": 1,
       "quantity": 5,
       "revenue": 10.0
      },
      {
       "sale_id": 3,
       "product_id": 2,
       "quantity": 8,
       "revenue": 16.0
      },
      {
       "sale_id": 4,
       "product_id": 3,
       "quantity": 2,
       "revenue": 4.0
      },
      {
       "sale_id": 5,
       "product_id": 4,
       "quantity": 15,
       "revenue": 30.0
      }
     ]
    },
    {
     "name": "inventory",
     "columns": {
      "product_id": "INTEGER",
      "stock": "INTEGER",
      "warehouse": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": 1,
       "stock": 50,
       "warehouse": "Warehouse1"
      },
      {
       "product_id": 2,
       "stock": 40,
       "warehouse": "Warehouse1"
      },
      {
       "product_id": 3,
       "stock": 30,
       "warehouse": "Warehouse1"
      },
      {
       "product_id": 4,
       "stock": 20,
       "warehouse": "Warehouse1"
      },
      {
       "product_id": 5,
       "stock": 10,
       "warehouse": "Warehouse1"
      }
     ]
    }
   ],
   "output_schema": {
    "product_id": "value",
    "name": "value",
    "category": "value",
    "total_quantity": "value",
    "total_revenue": "value",
    "total_stock": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 10 \"Food and Beverage Sales\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-010",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "total_revenue stays a DOUBLE: the revenue data has decimals.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: product_id, name, category, total_quantity, total_revenue, total_stock\nSELECT *\nFROM products\n",
    "supported_operations": [
     "aggregation",
     "left-join",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: product_id, name, category, total_quantity, total_revenue, total_stock\nSELECT *\nFROM products\n",
    "supported_operations": [
     "aggregation",
     "left-join",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(products, sales, inventory):\n    # Transform the input DataFrames into the result described in the brief.\n    return products\n\n\ndisplay(etl(pd.DataFrame(products), pd.DataFrame(sales), pd.DataFrame(inventory)))\n",
    "supported_operations": [
     "aggregation",
     "left-join",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(products, sales, inventory):\n    # Transform the input DataFrames into the result described in the brief.\n    return products\n\n\ndisplay(etl(pl.DataFrame(products), pl.DataFrame(sales), pl.DataFrame(inventory)))\n",
    "supported_operations": [
     "aggregation",
     "left-join",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nproducts = spark.table(\"products\")\nsales = spark.table(\"sales\")\ninventory = spark.table(\"inventory\")\nproducts\n",
    "supported_operations": [
     "aggregation",
     "left-join",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'products') }}\n",
    "supported_operations": [
     "aggregation",
     "left-join",
     "nulls"
    ],
    "model": "product_summary"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-011-missing-box-office",
   "version": "1",
   "industry": "Film",
   "learning_objectives": [
    "Use IS NULL, not = NULL.",
    "Keep every column in its input order."
   ],
   "optimization": "NULL means unknown, so a comparison with NULL is unknown too and a WHERE clause drops it. IS NULL is the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Movies without box office figures",
   "difficulty": "easy",
   "topics": [
    "filtering",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A film database has one row per movie released in 2022. Return the movies whose box office collection is missing (NULL), with all their columns. A collection of 0 is a known value, not a missing one.",
   "sections": [
    {
     "title": "Tables",
     "body": "movies_df(movie_id INTEGER, movie_title VARCHAR, director_name VARCHAR, release_date DATE, box_office_collection DOUBLE, genre VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: movie_id, movie_title, director_name, release_date, box_office_collection, genre. One row per movie without a box office figure. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "`= NULL` is never true: test with IS NULL (isna() in pandas, is_null() in Polars). Zero is not NULL."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 11, \"Movies\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The hidden check is authored: ZillaCode's second test gave box office figures as text ('1.327B'), which does not fit the numeric column; its lesson (no missing figure, empty result) is kept."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-011-missing-box-office-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "zero-is-not-null"
   ],
   "hints": [
    "Use IS NULL, not = NULL.",
    "Keep every column in its input order."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "NULL means unknown, so a comparison with NULL is unknown too and a WHERE clause drops it. IS NULL is the predicate that asks the question.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "filtering"
   },
   "related_associations": [
    "zillacode/11"
   ],
   "recommendation": {
    "rank": 11,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "movie_id",
     "movie_title",
     "director_name",
     "release_date",
     "box_office_collection",
     "genre"
    ],
    "exact_schema": [
     "movie_id",
     "movie_title",
     "director_name",
     "release_date",
     "box_office_collection",
     "genre"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "movies_df",
     "columns": {
      "movie_id": "INTEGER",
      "movie_title": "VARCHAR",
      "director_name": "VARCHAR",
      "release_date": "DATE",
      "box_office_collection": "DOUBLE",
      "genre": "VARCHAR"
     },
     "sample_rows": [
      {
       "movie_id": 1,
       "movie_title": "The Avengers",
       "director_name": "Joss Whedon",
       "release_date": "2022-05-06",
       "box_office_collection": 1856.45,
       "genre": "Action, Adventure, Sci-Fi"
      },
      {
       "movie_id": 2,
       "movie_title": "Black Panther: Wakanda Forever",
       "director_name": "Ryan Coogler",
       "release_date": "2022-11-10",
       "box_office_collection": null,
       "genre": "Action, Adventure, Drama"
      },
      {
       "movie_id": 3,
       "movie_title": "Jurassic World: Dominion",
       "director_name": "Colin Trevorrow",
       "release_date": "2022-06-10",
       "box_office_collection": 1234.56,
       "genre": "Action, Adventure, Sci-Fi"
      },
      {
       "movie_id": 4,
       "movie_title": "Fantastic Beasts and Where to Find Them 3",
       "director_name": "David Yates",
       "release_date": "2022-11-04",
       "box_office_collection": null,
       "genre": "Adventure, Family, Fantasy"
      },
      {
       "movie_id": 5,
       "movie_title": "Sonic the Hedgehog 2",
       "director_name": "Jeff Fowler",
       "release_date": "2022-04-08",
       "box_office_collection": 789.12,
       "genre": "Action, Adventure, Comedy"
      },
      {
       "movie_id": 6,
       "movie_title": "The Batman",
       "director_name": "Matt Reeves",
       "release_date": "2022-03-04",
       "box_office_collection": 2345.67,
       "genre": "Action, Adventure, Crime"
      },
      {
       "movie_id": 7,
       "movie_title": "Avatar 2",
       "director_name": "James Cameron",
       "release_date": "2022-12-16",
       "box_office_collection": 5678.9,
       "genre": "Action, Adventure, Fantasy, Sci-Fi"
      },
      {
       "movie_id": 8,
       "movie_title": "Doctor Strange in the Multiverse of Madness",
       "director_name": "Sam Raimi",
       "release_date": "2022-03-25",
       "box_office_collection": 4567.89,
       "genre": "Action, Adventure, Fantasy"
      }
     ]
    }
   ],
   "output_schema": {
    "movie_id": "value",
    "movie_title": "value",
    "director_name": "value",
    "release_date": "value",
    "box_office_collection": "value",
    "genre": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 11 \"Movies\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-011",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The hidden check is authored: ZillaCode's second test gave box office figures as text ('1.327B'), which does not fit the numeric column; its lesson (no missing figure, empty result) is kept.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: movie_id, movie_title, director_name, release_date, box_office_collection, genre\nSELECT *\nFROM movies_df\n",
    "supported_operations": [
     "filtering",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: movie_id, movie_title, director_name, release_date, box_office_collection, genre\nSELECT *\nFROM movies_df\n",
    "supported_operations": [
     "filtering",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(movies_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return movies_df\n\n\ndisplay(etl(pd.DataFrame(movies_df)))\n",
    "supported_operations": [
     "filtering",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(movies_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return movies_df\n\n\ndisplay(etl(pl.DataFrame(movies_df)))\n",
    "supported_operations": [
     "filtering",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nmovies_df = spark.table(\"movies_df\")\nmovies_df\n",
    "supported_operations": [
     "filtering",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'movies_df') }}\n",
    "supported_operations": [
     "filtering",
     "nulls"
    ],
    "model": "missing_box_office"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-012-append-customers",
   "version": "1",
   "industry": "Insurance",
   "learning_objectives": [
    "UNION ALL keeps every row.",
    "Both files have the same columns in the same order."
   ],
   "optimization": "UNION ALL appends result sets without comparing rows; UNION also deduplicates, which loses rows here.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Append two customer files",
   "difficulty": "easy",
   "topics": [
    "union"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "An insurance agency keeps its customers in two files with the same columns. Return every row of both files. A customer present in both files appears twice: this is an append, not a deduplication.",
   "sections": [
    {
     "title": "Tables",
     "body": "input_df1(customer_id INTEGER, first_name VARCHAR, last_name VARCHAR, age INTEGER, policy_type VARCHAR)\ninput_df2(customer_id INTEGER, first_name VARCHAR, last_name VARCHAR, age INTEGER, policy_type VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: customer_id, first_name, last_name, age, policy_type. One row per input row of either file. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "UNION removes duplicate rows; appending needs UNION ALL (pd.concat, pl.concat)."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 12, \"Insurance Customers\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-012-append-customers-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "shared-rows"
   ],
   "hints": [
    "UNION ALL keeps every row.",
    "Both files have the same columns in the same order."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "UNION ALL appends result sets without comparing rows; UNION also deduplicates, which loses rows here.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "union"
   },
   "related_associations": [
    "zillacode/12"
   ],
   "recommendation": {
    "rank": 12,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "customer_id",
     "first_name",
     "last_name",
     "age",
     "policy_type"
    ],
    "exact_schema": [
     "customer_id",
     "first_name",
     "last_name",
     "age",
     "policy_type"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "input_df1",
     "columns": {
      "customer_id": "INTEGER",
      "first_name": "VARCHAR",
      "last_name": "VARCHAR",
      "age": "INTEGER",
      "policy_type": "VARCHAR"
     },
     "sample_rows": [
      {
       "customer_id": 1,
       "first_name": "Alice",
       "last_name": "Smith",
       "age": 30,
       "policy_type": "auto"
      },
      {
       "customer_id": 2,
       "first_name": "Bob",
       "last_name": "Johnson",
       "age": 40,
       "policy_type": "home"
      },
      {
       "customer_id": 3,
       "first_name": "Carol",
       "last_name": "Williams",
       "age": 35,
       "policy_type": "life"
      }
     ]
    },
    {
     "name": "input_df2",
     "columns": {
      "customer_id": "INTEGER",
      "first_name": "VARCHAR",
      "last_name": "VARCHAR",
      "age": "INTEGER",
      "policy_type": "VARCHAR"
     },
     "sample_rows": [
      {
       "customer_id": 4,
       "first_name": "Dave",
       "last_name": "Brown",
       "age": 45,
       "policy_type": "auto"
      },
      {
       "customer_id": 5,
       "first_name": "Eve",
       "last_name": "Jones",
       "age": 55,
       "policy_type": "health"
      },
      {
       "customer_id": 6,
       "first_name": "Frank",
       "last_name": "Davis",
       "age": 60,
       "policy_type": "life"
      }
     ]
    }
   ],
   "output_schema": {
    "customer_id": "value",
    "first_name": "value",
    "last_name": "value",
    "age": "value",
    "policy_type": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 12 \"Insurance Customers\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-012",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: customer_id, first_name, last_name, age, policy_type\nSELECT *\nFROM input_df1\n",
    "supported_operations": [
     "union"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: customer_id, first_name, last_name, age, policy_type\nSELECT *\nFROM input_df1\n",
    "supported_operations": [
     "union"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(input_df1, input_df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df1\n\n\ndisplay(etl(pd.DataFrame(input_df1), pd.DataFrame(input_df2)))\n",
    "supported_operations": [
     "union"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(input_df1, input_df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df1\n\n\ndisplay(etl(pl.DataFrame(input_df1), pl.DataFrame(input_df2)))\n",
    "supported_operations": [
     "union"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'input_df1') }}\n",
    "supported_operations": [
     "union"
    ],
    "model": "append_customers"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-013-last-climb",
   "version": "1",
   "industry": "Mountaineering",
   "learning_objectives": [
    "Rank climbs per mountain by climb_date, newest first.",
    "Keep rank 1; filter the mountains with IN / EXISTS so a listed mountain cannot duplicate rows."
   ],
   "optimization": "RANK() OVER (PARTITION BY mountain_name ORDER BY climb_date DESC) = 1 keeps every climb on the latest date.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Last climb of each mountain",
   "difficulty": "medium",
   "topics": [
    "window-functions",
    "ranking",
    "semi-join"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each mountain of mountain_info that has been climbed, return the most recent climb: the climber, the date and the climbing time. When several climbs share the most recent date, return all of them. Climbs of mountains that are not in mountain_info are ignored.",
   "sections": [
    {
     "title": "Tables",
     "body": "mountain_info(name VARCHAR, height INTEGER, country VARCHAR, range VARCHAR)\nmountain_climbers(climber_name VARCHAR, mountain_name VARCHAR, climb_date DATE, climb_time DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: mountain_name, last_climber_name, last_climb_date, last_climb_time. One row per climb on its mountain's most recent climb date. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "ROW_NUMBER keeps one arbitrary climb when two share the last date; RANK (or comparing with the per-mountain MAX date) keeps both."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 13, \"Mountain Climbing\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Output names follow the statement (last_climber_name, last_climb_date, last_climb_time). Ties on the last date keep every climb (ZillaCode's Spark reference ranked ties by climb_time; its SQL kept them all)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-013-last-climb-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "tie-and-unknown-mountain"
   ],
   "hints": [
    "Rank climbs per mountain by climb_date, newest first.",
    "Keep rank 1; filter the mountains with IN / EXISTS so a listed mountain cannot duplicate rows."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "RANK() OVER (PARTITION BY mountain_name ORDER BY climb_date DESC) = 1 keeps every climb on the latest date. Snowflake can filter the window directly with QUALIFY. Comparing climb_date with MAX(climb_date) per mountain gives the same rows.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/13"
   ],
   "recommendation": {
    "rank": 13,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "mountain_name",
     "last_climber_name",
     "last_climb_date",
     "last_climb_time"
    ],
    "exact_schema": [
     "mountain_name",
     "last_climber_name",
     "last_climb_date",
     "last_climb_time"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "mountain_info",
     "columns": {
      "name": "VARCHAR",
      "height": "INTEGER",
      "country": "VARCHAR",
      "range": "VARCHAR"
     },
     "sample_rows": [
      {
       "name": "Mount Everest",
       "height": 8848,
       "country": "Nepal",
       "range": "Himalayas"
      },
      {
       "name": "Mount Kilimanjaro",
       "height": 5895,
       "country": "Tanzania",
       "range": "Kilimanjaro"
      },
      {
       "name": "Mount Denali",
       "height": 6190,
       "country": "USA",
       "range": "Alaska"
      },
      {
       "name": "Mount Fuji",
       "height": 3776,
       "country": "Japan",
       "range": "Fuji"
      },
      {
       "name": "Mont Blanc",
       "height": 4808,
       "country": "France",
       "range": "Alps"
      }
     ]
    },
    {
     "name": "mountain_climbers",
     "columns": {
      "climber_name": "VARCHAR",
      "mountain_name": "VARCHAR",
      "climb_date": "DATE",
      "climb_time": "DOUBLE"
     },
     "sample_rows": [
      {
       "climber_name": "John",
       "mountain_name": "Mount Everest",
       "climb_date": "2020-01-01",
       "climb_time": 8.5
      },
      {
       "climber_name": "Jane",
       "mountain_name": "Mount Everest",
       "climb_date": "2022-02-02",
       "climb_time": 9.0
      },
      {
       "climber_name": "Jim",
       "mountain_name": "Mount Kilimanjaro",
       "climb_date": "2021-03-03",
       "climb_time": 6.0
      },
      {
       "climber_name": "Jess",
       "mountain_name": "Mount Kilimanjaro",
       "climb_date": "2022-04-04",
       "climb_time": 7.0
      },
      {
       "climber_name": "Joe",
       "mountain_name": "Mount Denali",
       "climb_date": "2022-05-05",
       "climb_time": 10.0
      },
      {
       "climber_name": "Jill",
       "mountain_name": "Mount Denali",
       "climb_date": "2021-06-06",
       "climb_time": 11.0
      },
      {
       "climber_name": "Jack",
       "mountain_name": "Mount Fuji",
       "climb_date": "2022-07-07",
       "climb_time": 4.0
      },
      {
       "climber_name": "Jules",
       "mountain_name": "Mount Fuji",
       "climb_date": "2021-08-08",
       "climb_time": 5.0
      },
      {
       "climber_name": "Jean",
       "mountain_name": "Mont Blanc",
       "climb_date": "2020-09-09",
       "climb_time": 12.0
      },
      {
       "climber_name": "Josh",
       "mountain_name": "Mont Blanc",
       "climb_date": "2022-10-10",
       "climb_time": 13.0
      }
     ]
    }
   ],
   "output_schema": {
    "mountain_name": "value",
    "last_climber_name": "value",
    "last_climb_date": "value",
    "last_climb_time": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 13 \"Mountain Climbing\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-013",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit date-only output serialization for climb_date, matching the declared fixture contract after Pandas datetime sorting. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Output names follow the statement (last_climber_name, last_climb_date, last_climb_time). Ties on the last date keep every climb (ZillaCode's Spark reference ranked ties by climb_time; its SQL kept them all).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: mountain_name, last_climber_name, last_climb_date, last_climb_time\nSELECT *\nFROM mountain_info\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "semi-join"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: mountain_name, last_climber_name, last_climb_date, last_climb_time\nSELECT *\nFROM mountain_info\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "semi-join"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(mountain_info, mountain_climbers):\n    # Transform the input DataFrames into the result described in the brief.\n    return mountain_info\n\n\ndisplay(etl(pd.DataFrame(mountain_info), pd.DataFrame(mountain_climbers)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "semi-join"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(mountain_info, mountain_climbers):\n    # Transform the input DataFrames into the result described in the brief.\n    return mountain_info\n\n\ndisplay(etl(pl.DataFrame(mountain_info), pl.DataFrame(mountain_climbers)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "semi-join"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nmountain_info = spark.table(\"mountain_info\")\nmountain_climbers = spark.table(\"mountain_climbers\")\nmountain_info\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "semi-join"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'mountain_info') }}\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "semi-join"
    ],
    "model": "last_climb"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-014-pe-full-outer",
   "version": "1",
   "industry": "Private equity",
   "learning_objectives": [
    "FULL OUTER JOIN firms to funds on firm_id, then to investments on fund_id.",
    "COALESCE the keys from both sides of each join."
   ],
   "optimization": "Two FULL OUTER JOINs keep unmatched rows from every side. The output key columns are the COALESCE of both",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Firms, funds and investments with full outer joins",
   "difficulty": "hard",
   "topics": [
    "full-outer-join",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Combine private equity firms, their funds and the funds' investments, keeping every record of every table (full outer joins): a firm without funds, a fund without a known firm, and an investment without a known fund all appear, with NULLs for the missing side. firm_id and fund_id come from whichever side has them. Missing keys (NULL fund_id or firm_id) never match anything. A row whose columns are all NULL (an empty input record) is dropped.",
   "sections": [
    {
     "title": "Tables",
     "body": "pe_firms(firm_id INTEGER, firm_name VARCHAR, founded_year INTEGER, location VARCHAR)\npe_funds(fund_id INTEGER, firm_id INTEGER, fund_name VARCHAR, fund_size INTEGER, fund_start_year INTEGER, fund_end_year INTEGER)\npe_investments(investment_id INTEGER, fund_id INTEGER, company_name VARCHAR, investment_amount INTEGER, investment_date VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: investment_id, fund_id, firm_id, firm_name, founded_year, location, fund_name, fund_size, fund_start_year, fund_end_year, company_name, investment_amount, investment_date. One row per investment of a fund, plus one row per fund without investments, per firm without funds and per investment without a known fund. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "A LEFT JOIN from firms loses funds and investments that no firm owns. In pandas, merge matches missing keys with each other (NaN == NaN), unlike SQL, Polars and Spark: keep rows without a key out of the merge."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 14, \"Private Equity Firms\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The statement's full outer join is implemented for all three tables (ZillaCode's SQL used left joins from firms, which the fixtures could not tell apart); an edge check covers funds and investments that no firm or fund owns. Integer columns are integers (ZillaCode's second test had 1001.0-style floats)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-014-pe-full-outer-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "orphans-and-null-keys"
   ],
   "hints": [
    "FULL OUTER JOIN firms to funds on firm_id, then to investments on fund_id.",
    "COALESCE the keys from both sides of each join."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Two FULL OUTER JOINs keep unmatched rows from every side. The output key columns are the COALESCE of both sides. SQL never matches NULL keys, but pandas' merge does, so the pandas reference merges only rows that have a fund_id and appends the others.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "full-outer-join"
   },
   "related_associations": [
    "zillacode/14"
   ],
   "recommendation": {
    "rank": 14,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "investment_id",
     "fund_id",
     "firm_id",
     "firm_name",
     "founded_year",
     "location",
     "fund_name",
     "fund_size",
     "fund_start_year",
     "fund_end_year",
     "company_name",
     "investment_amount",
     "investment_date"
    ],
    "exact_schema": [
     "investment_id",
     "fund_id",
     "firm_id",
     "firm_name",
     "founded_year",
     "location",
     "fund_name",
     "fund_size",
     "fund_start_year",
     "fund_end_year",
     "company_name",
     "investment_amount",
     "investment_date"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "pe_firms",
     "columns": {
      "firm_id": "INTEGER",
      "firm_name": "VARCHAR",
      "founded_year": "INTEGER",
      "location": "VARCHAR"
     },
     "sample_rows": [
      {
       "firm_id": 1,
       "firm_name": "ABC Fund",
       "founded_year": 2010,
       "location": "New York"
      },
      {
       "firm_id": 2,
       "firm_name": "XYZ Fund",
       "founded_year": 2005,
       "location": "London"
      },
      {
       "firm_id": 3,
       "firm_name": "DEF Fund",
       "founded_year": 2015,
       "location": "Paris"
      }
     ]
    },
    {
     "name": "pe_funds",
     "columns": {
      "fund_id": "INTEGER",
      "firm_id": "INTEGER",
      "fund_name": "VARCHAR",
      "fund_size": "INTEGER",
      "fund_start_year": "INTEGER",
      "fund_end_year": "INTEGER"
     },
     "sample_rows": [
      {
       "fund_id": 101,
       "firm_id": 1,
       "fund_name": "ABC I",
       "fund_size": 100,
       "fund_start_year": 2010,
       "fund_end_year": 2015
      },
      {
       "fund_id": 102,
       "firm_id": 1,
       "fund_name": "ABC II",
       "fund_size": 150,
       "fund_start_year": 2015,
       "fund_end_year": 2020
      },
      {
       "fund_id": 103,
       "firm_id": 2,
       "fund_name": "XYZ I",
       "fund_size": 200,
       "fund_start_year": 2010,
       "fund_end_year": 2018
      }
     ]
    },
    {
     "name": "pe_investments",
     "columns": {
      "investment_id": "INTEGER",
      "fund_id": "INTEGER",
      "company_name": "VARCHAR",
      "investment_amount": "INTEGER",
      "investment_date": "VARCHAR"
     },
     "sample_rows": [
      {
       "investment_id": 1001,
       "fund_id": 101,
       "company_name": "Company A",
       "investment_amount": 10,
       "investment_date": "2012-05-15"
      },
      {
       "investment_id": 1002,
       "fund_id": 101,
       "company_name": "Company B",
       "investment_amount": 20,
       "investment_date": "2013-06-20"
      },
      {
       "investment_id": 1003,
       "fund_id": 102,
       "company_name": "Company C",
       "investment_amount": 30,
       "investment_date": "2016-07-25"
      }
     ]
    }
   ],
   "output_schema": {
    "investment_id": "value",
    "fund_id": "value",
    "firm_id": "value",
    "firm_name": "value",
    "founded_year": "value",
    "location": "value",
    "fund_name": "value",
    "fund_size": "value",
    "fund_start_year": "value",
    "fund_end_year": "value",
    "company_name": "value",
    "investment_amount": "value",
    "investment_date": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 14 \"Private Equity Firms\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-014",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The statement's full outer join is implemented for all three tables (ZillaCode's SQL used left joins from firms, which the fixtures could not tell apart); an edge check covers funds and investments that no firm or fund owns. Integer columns are integers (ZillaCode's second test had 1001.0-style floats).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: investment_id, fund_id, firm_id, firm_name, founded_year, location, fund_name, fund_size, fund_start_year, fund_end_year, company_name, investment_amount, investment_date\nSELECT *\nFROM pe_firms\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: investment_id, fund_id, firm_id, firm_name, founded_year, location, fund_name, fund_size, fund_start_year, fund_end_year, company_name, investment_amount, investment_date\nSELECT *\nFROM pe_firms\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(pe_firms, pe_funds, pe_investments):\n    # Transform the input DataFrames into the result described in the brief.\n    return pe_firms\n\n\ndisplay(etl(pd.DataFrame(pe_firms), pd.DataFrame(pe_funds), pd.DataFrame(pe_investments)))\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(pe_firms, pe_funds, pe_investments):\n    # Transform the input DataFrames into the result described in the brief.\n    return pe_firms\n\n\ndisplay(etl(pl.DataFrame(pe_firms), pl.DataFrame(pe_funds), pl.DataFrame(pe_investments)))\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\npe_firms = spark.table(\"pe_firms\")\npe_funds = spark.table(\"pe_funds\")\npe_investments = spark.table(\"pe_investments\")\npe_firms\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'pe_firms') }}\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ],
    "model": "pe_full_outer"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-015-best-pages",
   "version": "1",
   "industry": "SEO",
   "learning_objectives": [
    "ROW_NUMBER() OVER (PARTITION BY domain ORDER BY seo_score DESC NULLS LAST, url).",
    "The overall best page is also the best page of its own domain."
   ],
   "optimization": "Rank pages twice with ROW_NUMBER: within the domain and over the whole table, both by score descending with",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Best page per domain and overall",
   "difficulty": "medium",
   "topics": [
    "window-functions",
    "nulls",
    "ordering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each domain, return its best page: the highest SEO score, ties broken by the URL in ascending order. Also return the best page over all domains (same rule) in overall_highest_page and overall_highest_score, but only on the row of the domain that owns it; the other rows have NULL there. A page without a score (NULL) is never better than a scored page.",
   "sections": [
    {
     "title": "Tables",
     "body": "pages(domain VARCHAR, url VARCHAR, seo_score INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: domain, highest_seo_page, highest_seo_score, overall_highest_page, overall_highest_score. One row per domain. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Snowflake sorts NULLs first in a descending order (they are the largest value) and Polars sorts nulls first unless nulls_last=True: without NULLS LAST, an unscored page wins. DuckDB, pandas and Spark put NULLs last by default."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 15, \"SEO Optimization\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Ties are broken by URL and NULL scores rank last, so the result is deterministic (ZillaCode left ties to ROW_NUMBER's arbitrary choice)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-015-best-pages-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "ties-and-null-scores"
   ],
   "hints": [
    "ROW_NUMBER() OVER (PARTITION BY domain ORDER BY seo_score DESC NULLS LAST, url).",
    "The overall best page is also the best page of its own domain."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Rank pages twice with ROW_NUMBER: within the domain and over the whole table, both by score descending with NULLs last and the URL as the tie-breaker. Keep each domain's first page and attach the overall first page on its own domain. Spelling out NULLS LAST makes the query mean the same thing in every engine.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/15"
   ],
   "recommendation": {
    "rank": 15,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "domain",
     "highest_seo_page",
     "highest_seo_score",
     "overall_highest_page",
     "overall_highest_score"
    ],
    "exact_schema": [
     "domain",
     "highest_seo_page",
     "highest_seo_score",
     "overall_highest_page",
     "overall_highest_score"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "pages",
     "columns": {
      "domain": "VARCHAR",
      "url": "VARCHAR",
      "seo_score": "INTEGER"
     },
     "sample_rows": [
      {
       "domain": "example.com",
       "url": "https://www.example.com/page1",
       "seo_score": 88
      },
      {
       "domain": "example.com",
       "url": "https://www.example.com/page2",
       "seo_score": 92
      },
      {
       "domain": "example.com",
       "url": "https://www.example.com/page3",
       "seo_score": 80
      },
      {
       "domain": "example.net",
       "url": "https://www.example.net/page1",
       "seo_score": 75
      },
      {
       "domain": "example.net",
       "url": "https://www.example.net/page2",
       "seo_score": 90
      },
      {
       "domain": "example.org",
       "url": "https://www.example.org/page1",
       "seo_score": 82
      },
      {
       "domain": "example.org",
       "url": "https://www.example.org/page2",
       "seo_score": 85
      }
     ]
    }
   ],
   "output_schema": {
    "domain": "value",
    "highest_seo_page": "value",
    "highest_seo_score": "value",
    "overall_highest_page": "value",
    "overall_highest_score": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 15 \"SEO Optimization\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-015",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Ties are broken by URL and NULL scores rank last, so the result is deterministic (ZillaCode left ties to ROW_NUMBER's arbitrary choice).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: domain, highest_seo_page, highest_seo_score, overall_highest_page, overall_highest_score\nSELECT *\nFROM pages\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "ordering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: domain, highest_seo_page, highest_seo_score, overall_highest_page, overall_highest_score\nSELECT *\nFROM pages\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "ordering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(pages):\n    # Transform the input DataFrames into the result described in the brief.\n    return pages\n\n\ndisplay(etl(pd.DataFrame(pages)))\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "ordering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(pages):\n    # Transform the input DataFrames into the result described in the brief.\n    return pages\n\n\ndisplay(etl(pl.DataFrame(pages)))\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "ordering"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\npages = spark.table(\"pages\")\npages\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "ordering"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'pages') }}\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "ordering"
    ],
    "model": "best_pages"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-016-overtime-pay",
   "version": "1",
   "industry": "Payroll",
   "learning_objectives": [
    "CASE WHEN hours_worked <= 40 THEN ... ELSE ... END (IFF in Snowflake).",
    "Equivalently: LEAST(hours, 40) * rate + GREATEST(hours - 40, 0) * rate * 1.5."
   ],
   "optimization": "Inner join employees to payroll and compute pay with a conditional expression: the base hours at the rate",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Pay with overtime",
   "difficulty": "easy",
   "topics": [
    "conditional-logic",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Compute each employee's pay from their payroll row: up to 40 hours are paid at the hourly rate, and every hour above 40 is paid 1.5 times the rate. Employees without a payroll row, and payroll rows of unknown employees, are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "employees(employee_id INTEGER, name VARCHAR, age INTEGER, position VARCHAR)\npayroll(employee_id INTEGER, hours_worked DOUBLE, hourly_rate DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: employee_id, name, position, pay. One row per employee with a payroll row. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Only the hours above 40 earn 1.5 times the rate; the first 40 hours stay at the normal rate. Exactly 40 hours is not overtime."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 16, \"Running Payroll\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-016-overtime-pay-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "boundaries"
   ],
   "hints": [
    "CASE WHEN hours_worked <= 40 THEN ... ELSE ... END (IFF in Snowflake).",
    "Equivalently: LEAST(hours, 40) * rate + GREATEST(hours - 40, 0) * rate * 1.5."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Inner join employees to payroll and compute pay with a conditional expression: the base hours at the rate plus the overtime hours at 1.5 times the rate.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "conditional-logic"
   },
   "related_associations": [
    "zillacode/16"
   ],
   "recommendation": {
    "rank": 16,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "employee_id",
     "name",
     "position",
     "pay"
    ],
    "exact_schema": [
     "employee_id",
     "name",
     "position",
     "pay"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "employees",
     "columns": {
      "employee_id": "INTEGER",
      "name": "VARCHAR",
      "age": "INTEGER",
      "position": "VARCHAR"
     },
     "sample_rows": [
      {
       "employee_id": 1,
       "name": "Alice",
       "age": 25,
       "position": "Software Engineer"
      },
      {
       "employee_id": 2,
       "name": "Bob",
       "age": 30,
       "position": "Data Analyst"
      },
      {
       "employee_id": 3,
       "name": "Carol",
       "age": 28,
       "position": "Product Manager"
      },
      {
       "employee_id": 4,
       "name": "Dave",
       "age": 24,
       "position": "Software Engineer"
      }
     ]
    },
    {
     "name": "payroll",
     "columns": {
      "employee_id": "INTEGER",
      "hours_worked": "DOUBLE",
      "hourly_rate": "DOUBLE"
     },
     "sample_rows": [
      {
       "employee_id": 1,
       "hours_worked": 45.0,
       "hourly_rate": 30.0
      },
      {
       "employee_id": 2,
       "hours_worked": 38.0,
       "hourly_rate": 25.0
      },
      {
       "employee_id": 3,
       "hours_worked": 41.5,
       "hourly_rate": 35.0
      },
      {
       "employee_id": 4,
       "hours_worked": 40.0,
       "hourly_rate": 28.0
      }
     ]
    }
   ],
   "output_schema": {
    "employee_id": "value",
    "name": "value",
    "position": "value",
    "pay": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 16 \"Running Payroll\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-016",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: employee_id, name, position, pay\nSELECT *\nFROM employees\n",
    "supported_operations": [
     "conditional-logic",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: employee_id, name, position, pay\nSELECT *\nFROM employees\n",
    "supported_operations": [
     "conditional-logic",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(employees, payroll):\n    # Transform the input DataFrames into the result described in the brief.\n    return employees\n\n\ndisplay(etl(pd.DataFrame(employees), pd.DataFrame(payroll)))\n",
    "supported_operations": [
     "conditional-logic",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(employees, payroll):\n    # Transform the input DataFrames into the result described in the brief.\n    return employees\n\n\ndisplay(etl(pl.DataFrame(employees), pl.DataFrame(payroll)))\n",
    "supported_operations": [
     "conditional-logic",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nemployees = spark.table(\"employees\")\npayroll = spark.table(\"payroll\")\nemployees\n",
    "supported_operations": [
     "conditional-logic",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'employees') }}\n",
    "supported_operations": [
     "conditional-logic",
     "joins"
    ],
    "model": "overtime_pay"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-017-extract-digits",
   "version": "1",
   "industry": "Geology",
   "learning_objectives": [
    "Extract the pattern [0-9]+ (the first match).",
    "Replace a missing match with ''."
   ],
   "optimization": "A regular expression extraction of the first match of [0-9]+. Engines disagree on the no-match value (empty",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Extract the age from a description",
   "difficulty": "easy",
   "topics": [
    "regular-expressions",
    "strings",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Rock sample descriptions mix letters and digits, for example 'Basalt_450Ma'. Add a column age with the first run of digits in the description, as text. When the description has no digit, age is the empty string ''.",
   "sections": [
    {
     "title": "Tables",
     "body": "input_df(sample_id VARCHAR, description VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: sample_id, description, age. One row per sample. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Snowflake's REGEXP_SUBSTR returns NULL when nothing matches (DuckDB's regexp_extract returns ''), and pandas' str.extract returns NaN: wrap the extraction in COALESCE / fillna to get ''."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 17, \"Geology Samples\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Output columns follow the statement's order (sample_id, description, age)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-017-extract-digits-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "several-numbers"
   ],
   "hints": [
    "Extract the pattern [0-9]+ (the first match).",
    "Replace a missing match with ''."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A regular expression extraction of the first match of [0-9]+. Engines disagree on the no-match value (empty string in DuckDB, NULL in Snowflake, NaN/null in pandas and Polars), so the contract fixes it to ''.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "regular-expressions"
   },
   "related_associations": [
    "zillacode/17"
   ],
   "recommendation": {
    "rank": 17,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "sample_id",
     "description",
     "age"
    ],
    "exact_schema": [
     "sample_id",
     "description",
     "age"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "input_df",
     "columns": {
      "sample_id": "VARCHAR",
      "description": "VARCHAR"
     },
     "sample_rows": [
      {
       "sample_id": "S1",
       "description": "Basalt_450Ma"
      },
      {
       "sample_id": "S2",
       "description": "Sandstone_300Ma"
      },
      {
       "sample_id": "S3",
       "description": "Limestone"
      },
      {
       "sample_id": "S4",
       "description": "Granite_200Ma"
      },
      {
       "sample_id": "S5",
       "description": "Marble_1800Ma"
      }
     ]
    }
   ],
   "output_schema": {
    "sample_id": "value",
    "description": "value",
    "age": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 17 \"Geology Samples\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-017",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. DuckDB regexp_extract replaces warehouse-only regexp_instr. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Output columns follow the statement's order (sample_id, description, age).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: sample_id, description, age\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: sample_id, description, age\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pd.DataFrame(input_df)))\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pl.DataFrame(input_df)))\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'input_df') }}\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ],
    "model": "extract_digits"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-018-dedupe-then-join",
   "version": "1",
   "industry": "Manufacturing",
   "learning_objectives": [
    "SELECT DISTINCT (drop_duplicates, unique, distinct) on each input first.",
    "Then an inner join on product_id."
   ],
   "optimization": "Deduplicate each input, then join. The duplicates are exact copies, so DISTINCT keeps one of each; deduplicating",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Remove duplicates, then combine",
   "difficulty": "easy",
   "topics": [
    "deduplication",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Both feeds of a manufacturer contain duplicate rows (the same record loaded twice). Remove them, then return each product with each of its manufacturing processes. Products without a process are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "products_df(product_id INTEGER, product_name VARCHAR, category VARCHAR)\nmanufacturing_processes_df(process_id INTEGER, product_id INTEGER, process_name VARCHAR, duration DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: product_id, product_name, category, process_id, process_name, duration. One row per distinct (product, process) pair. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Joining before deduplicating multiplies the duplicates: two copies of a product and two copies of a process give four rows."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 18, \"Factory Duplicates\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Columns are snake_case (ZillaCode used ProductID, ProductName, ...). Duplicates are exact copies, so the result no longer depends on which copy a key-based deduplication keeps."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-018-dedupe-then-join-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "double-duplicates"
   ],
   "hints": [
    "SELECT DISTINCT (drop_duplicates, unique, distinct) on each input first.",
    "Then an inner join on product_id."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Deduplicate each input, then join. The duplicates are exact copies, so DISTINCT keeps one of each; deduplicating on a key alone would silently keep an arbitrary copy if the copies ever differed.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "deduplication"
   },
   "related_associations": [
    "zillacode/18"
   ],
   "recommendation": {
    "rank": 18,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "product_id",
     "product_name",
     "category",
     "process_id",
     "process_name",
     "duration"
    ],
    "exact_schema": [
     "product_id",
     "product_name",
     "category",
     "process_id",
     "process_name",
     "duration"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "products_df",
     "columns": {
      "product_id": "INTEGER",
      "product_name": "VARCHAR",
      "category": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": 1,
       "product_name": "Widget A",
       "category": "Type1"
      },
      {
       "product_id": 2,
       "product_name": "Widget B",
       "category": "Type1"
      },
      {
       "product_id": 3,
       "product_name": "Widget C",
       "category": "Type2"
      },
      {
       "product_id": 4,
       "product_name": "Widget D",
       "category": "Type2"
      },
      {
       "product_id": 1,
       "product_name": "Widget A",
       "category": "Type1"
      }
     ]
    },
    {
     "name": "manufacturing_processes_df",
     "columns": {
      "process_id": "INTEGER",
      "product_id": "INTEGER",
      "process_name": "VARCHAR",
      "duration": "DOUBLE"
     },
     "sample_rows": [
      {
       "process_id": 1001,
       "product_id": 1,
       "process_name": "Cutting",
       "duration": 1.5
      },
      {
       "process_id": 1002,
       "product_id": 2,
       "process_name": "Cutting",
       "duration": 1.6
      },
      {
       "process_id": 1003,
       "product_id": 3,
       "process_name": "Cutting",
       "duration": 1.8
      },
      {
       "process_id": 1004,
       "product_id": 4,
       "process_name": "Cutting",
       "duration": 1.5
      },
      {
       "process_id": 1005,
       "product_id": 1,
       "process_name": "Shaping",
       "duration": 2.0
      }
     ]
    }
   ],
   "output_schema": {
    "product_id": "value",
    "product_name": "value",
    "category": "value",
    "process_id": "value",
    "process_name": "value",
    "duration": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 18 \"Factory Duplicates\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-018",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Multiline source references normalized; explicit output aliases preserve declared case. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Columns are snake_case (ZillaCode used ProductID, ProductName, ...). Duplicates are exact copies, so the result no longer depends on which copy a key-based deduplication keeps.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: product_id, product_name, category, process_id, process_name, duration\nSELECT *\nFROM products_df\n",
    "supported_operations": [
     "deduplication",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: product_id, product_name, category, process_id, process_name, duration\nSELECT *\nFROM products_df\n",
    "supported_operations": [
     "deduplication",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(products_df, manufacturing_processes_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return products_df\n\n\ndisplay(etl(pd.DataFrame(products_df), pd.DataFrame(manufacturing_processes_df)))\n",
    "supported_operations": [
     "deduplication",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(products_df, manufacturing_processes_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return products_df\n\n\ndisplay(etl(pl.DataFrame(products_df), pl.DataFrame(manufacturing_processes_df)))\n",
    "supported_operations": [
     "deduplication",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nproducts_df = spark.table(\"products_df\")\nmanufacturing_processes_df = spark.table(\"manufacturing_processes_df\")\nproducts_df\n",
    "supported_operations": [
     "deduplication",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'products_df') }}\n",
    "supported_operations": [
     "deduplication",
     "joins"
    ],
    "model": "dedupe_then_join"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-019-industry-ranking",
   "version": "1",
   "industry": "Venture capital",
   "learning_objectives": [
    "GROUP BY industry, then ORDER BY total_investment DESC, industry.",
    "Join investments to companies to find their industry."
   ],
   "optimization": "Join, aggregate, and sort with a complete ordering key. A result set has no order unless ORDER BY gives one,",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Investment per industry, largest first",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "ordering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return the total amount invested in each industry, sorted by the total in descending order; industries with the same total are sorted by name in ascending order. Industries without investments are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "companies(company_id INTEGER, company_name VARCHAR, industry VARCHAR)\ninvestments(investment_id INTEGER, company_id INTEGER, amount DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: industry, total_investment. One row per industry with at least one investment. Row order is graded: total_investment descending, then industry ascending."
    },
    {
     "title": "Common pitfall",
     "body": "Row order is part of this contract, so ties need an explicit tie-breaker; without it, two industries with the same total can come out in either order."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 19, \"VC Firms\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The order became a graded contract with an explicit tie-breaker (industry)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-019-industry-ranking-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "tied-totals"
   ],
   "hints": [
    "GROUP BY industry, then ORDER BY total_investment DESC, industry.",
    "Join investments to companies to find their industry."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Join, aggregate, and sort with a complete ordering key. A result set has no order unless ORDER BY gives one, and a sort key with ties leaves the tied rows in an unspecified order.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/19"
   ],
   "recommendation": {
    "rank": 19,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "industry",
     "total_investment"
    ],
    "exact_schema": [
     "industry",
     "total_investment"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "companies",
     "columns": {
      "company_id": "INTEGER",
      "company_name": "VARCHAR",
      "industry": "VARCHAR"
     },
     "sample_rows": [
      {
       "company_id": 1,
       "company_name": "AlphaTech",
       "industry": "Technology"
      },
      {
       "company_id": 2,
       "company_name": "BetaHealth",
       "industry": "Healthcare"
      },
      {
       "company_id": 3,
       "company_name": "GammaEntertainment",
       "industry": "Entertainment"
      },
      {
       "company_id": 4,
       "company_name": "DeltaGreen",
       "industry": "Renewable Energy"
      },
      {
       "company_id": 5,
       "company_name": "EpsilonFinance",
       "industry": "Finance"
      }
     ]
    },
    {
     "name": "investments",
     "columns": {
      "investment_id": "INTEGER",
      "company_id": "INTEGER",
      "amount": "DOUBLE"
     },
     "sample_rows": [
      {
       "investment_id": 1,
       "company_id": 1,
       "amount": 5000000.0
      },
      {
       "investment_id": 2,
       "company_id": 2,
       "amount": 3000000.0
      },
      {
       "investment_id": 3,
       "company_id": 3,
       "amount": 1000000.0
      },
      {
       "investment_id": 4,
       "company_id": 4,
       "amount": 4000000.0
      },
      {
       "investment_id": 5,
       "company_id": 5,
       "amount": 2000000.0
      }
     ]
    }
   ],
   "output_schema": {
    "industry": "value",
    "total_investment": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 19 \"VC Firms\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-019",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The order became a graded contract with an explicit tie-breaker (industry).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: industry, total_investment\nSELECT *\nFROM companies\n",
    "supported_operations": [
     "aggregation",
     "ordering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: industry, total_investment\nSELECT *\nFROM companies\n",
    "supported_operations": [
     "aggregation",
     "ordering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(companies, investments):\n    # Transform the input DataFrames into the result described in the brief.\n    return companies\n\n\ndisplay(etl(pd.DataFrame(companies), pd.DataFrame(investments)))\n",
    "supported_operations": [
     "aggregation",
     "ordering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(companies, investments):\n    # Transform the input DataFrames into the result described in the brief.\n    return companies\n\n\ndisplay(etl(pl.DataFrame(companies), pl.DataFrame(investments)))\n",
    "supported_operations": [
     "aggregation",
     "ordering"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ncompanies = spark.table(\"companies\")\ninvestments = spark.table(\"investments\")\ncompanies\n",
    "supported_operations": [
     "aggregation",
     "ordering"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-020-project-summary",
   "version": "1",
   "industry": "Construction",
   "learning_objectives": [
    "Two pre-aggregations, then LEFT JOINs to projects.",
    "DATEDIFF(day, start_date, end_date) in Snowflake; date_diff('day', ...) or end_date - start_date in DuckDB."
   ],
   "optimization": "Employees and equipment are independent one-to-many tables: aggregate each per project, LEFT JOIN them to the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Project duration, staff and equipment",
   "difficulty": "medium",
   "topics": [
    "aggregation",
    "dates",
    "left-join"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For every project, return its duration in days (end_date minus start_date), its number of employees, its number of distinct roles and the total cost of its equipment. A project without employees or equipment gets 0 in those columns. A missing role is not a role.",
   "sections": [
    {
     "title": "Tables",
     "body": "projects(project_id INTEGER, project_name VARCHAR, start_date DATE, end_date DATE, budget INTEGER)\nemployees(employee_id INTEGER, first_name VARCHAR, last_name VARCHAR, role VARCHAR, project_id INTEGER)\nequipment(equipment_id INTEGER, equipment_name VARCHAR, project_id INTEGER, cost INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: project_id, project_name, start_date, end_date, duration_days, total_employees, unique_roles, total_equipment_cost. One row per project. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Aggregate employees and equipment separately: joining both to the project first multiplies the employees by the equipment rows. COUNT(DISTINCT role) ignores NULL, but Polars' n_unique() counts null as a value."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 20, \"Construction Company\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: A project without employees or equipment gets 0 instead of NULL (ZillaCode's reference returned NULL from its left joins)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-020-project-summary-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "fan-out-and-null-role"
   ],
   "hints": [
    "Two pre-aggregations, then LEFT JOINs to projects.",
    "DATEDIFF(day, start_date, end_date) in Snowflake; date_diff('day', ...) or end_date - start_date in DuckDB."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Employees and equipment are independent one-to-many tables: aggregate each per project, LEFT JOIN them to the projects, turn missing counts into 0, and compute the duration from the two dates.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/20"
   ],
   "recommendation": {
    "rank": 20,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "project_id",
     "project_name",
     "start_date",
     "end_date",
     "duration_days",
     "total_employees",
     "unique_roles",
     "total_equipment_cost"
    ],
    "exact_schema": [
     "project_id",
     "project_name",
     "start_date",
     "end_date",
     "duration_days",
     "total_employees",
     "unique_roles",
     "total_equipment_cost"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "projects",
     "columns": {
      "project_id": "INTEGER",
      "project_name": "VARCHAR",
      "start_date": "DATE",
      "end_date": "DATE",
      "budget": "INTEGER"
     },
     "sample_rows": [
      {
       "project_id": 1,
       "project_name": "Skyscraper",
       "start_date": "2022-01-01",
       "end_date": "2022-12-31",
       "budget": 15000000
      },
      {
       "project_id": 2,
       "project_name": "Bridge",
       "start_date": "2022-03-01",
       "end_date": "2022-08-31",
       "budget": 5000000
      },
      {
       "project_id": 3,
       "project_name": "Tunnel",
       "start_date": "2022-06-01",
       "end_date": "2023-01-31",
       "budget": 10000000
      }
     ]
    },
    {
     "name": "employees",
     "columns": {
      "employee_id": "INTEGER",
      "first_name": "VARCHAR",
      "last_name": "VARCHAR",
      "role": "VARCHAR",
      "project_id": "INTEGER"
     },
     "sample_rows": [
      {
       "employee_id": 1,
       "first_name": "John",
       "last_name": "Doe",
       "role": "Engineer",
       "project_id": 1
      },
      {
       "employee_id": 2,
       "first_name": "Jane",
       "last_name": "Smith",
       "role": "Architect",
       "project_id": 1
      },
      {
       "employee_id": 3,
       "first_name": "Jim",
       "last_name": "Brown",
       "role": "Project Manager",
       "project_id": 1
      },
      {
       "employee_id": 4,
       "first_name": "Emily",
       "last_name": "Davis",
       "role": "Engineer",
       "project_id": 2
      },
      {
       "employee_id": 5,
       "first_name": "Alan",
       "last_name": "Johnson",
       "role": "Architect",
       "project_id": 2
      }
     ]
    },
    {
     "name": "equipment",
     "columns": {
      "equipment_id": "INTEGER",
      "equipment_name": "VARCHAR",
      "project_id": "INTEGER",
      "cost": "INTEGER"
     },
     "sample_rows": [
      {
       "equipment_id": 1,
       "equipment_name": "Crane",
       "project_id": 1,
       "cost": 25000
      },
      {
       "equipment_id": 2,
       "equipment_name": "Excavator",
       "project_id": 1,
       "cost": 15000
      },
      {
       "equipment_id": 3,
       "equipment_name": "Bulldozer",
       "project_id": 2,
       "cost": 20000
      },
      {
       "equipment_id": 4,
       "equipment_name": "Loader",
       "project_id": 2,
       "cost": 10000
      },
      {
       "equipment_id": 5,
       "equipment_name": "Crane",
       "project_id": 3,
       "cost": 25000
      }
     ]
    }
   ],
   "output_schema": {
    "project_id": "value",
    "project_name": "value",
    "start_date": "value",
    "end_date": "value",
    "duration_days": "value",
    "total_employees": "value",
    "unique_roles": "value",
    "total_equipment_cost": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 20 \"Construction Company\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-020",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit date-only output serialization for start_date/end_date; duration arithmetic still uses real datetime subtraction. Date-duration operands are explicitly cast from fixture VARCHAR to DATE. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "A project without employees or equipment gets 0 instead of NULL (ZillaCode's reference returned NULL from its left joins).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: project_id, project_name, start_date, end_date, duration_days, total_employees, unique_roles, total_equipment_cost\nSELECT *\nFROM projects\n",
    "supported_operations": [
     "aggregation",
     "dates",
     "left-join"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: project_id, project_name, start_date, end_date, duration_days, total_employees, unique_roles, total_equipment_cost\nSELECT *\nFROM projects\n",
    "supported_operations": [
     "aggregation",
     "dates",
     "left-join"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(projects, employees, equipment):\n    # Transform the input DataFrames into the result described in the brief.\n    return projects\n\n\ndisplay(etl(pd.DataFrame(projects), pd.DataFrame(employees), pd.DataFrame(equipment)))\n",
    "supported_operations": [
     "aggregation",
     "dates",
     "left-join"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(projects, employees, equipment):\n    # Transform the input DataFrames into the result described in the brief.\n    return projects\n\n\ndisplay(etl(pl.DataFrame(projects), pl.DataFrame(employees), pl.DataFrame(equipment)))\n",
    "supported_operations": [
     "aggregation",
     "dates",
     "left-join"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'projects') }}\n",
    "supported_operations": [
     "aggregation",
     "dates",
     "left-join"
    ],
    "model": "project_summary"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-021-previous-product",
   "version": "1",
   "industry": "Retail",
   "learning_objectives": [
    "LAG(product_id) OVER (PARTITION BY customer_id ORDER BY date, product_id).",
    "COALESCE(previous_product, 'None') only inside the concatenation."
   ],
   "optimization": "LAG reads the previous row of the same customer in the window order; the second sort key makes same-day",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Previous product of each customer",
   "difficulty": "medium",
   "topics": [
    "window-functions",
    "nulls",
    "strings"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each transaction, add previous_product: the product of the same customer's previous transaction in chronological order (date, then product_id for transactions on the same day), or NULL for their first one. Also add date_and_product: the date and the previous product separated by a space, with the text 'None' when there is no previous product. The date is ISO text ('YYYY-MM-DD').",
   "sections": [
    {
     "title": "Tables",
     "body": "transactions(customer_id VARCHAR, product_id VARCHAR, quantity INTEGER, date VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: customer_id, product_id, quantity, date, previous_product, date_and_product. One row per transaction. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "previous_product stays NULL; only the display column shows 'None'. Concatenating the NULL directly makes date_and_product NULL (SQL, Snowflake CONCAT) or NaN (pandas)."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 21, \"Retail Stores\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: previous_product stays NULL and only date_and_product shows 'None' (CodeDELeet's correction of ZillaCode). Same-day transactions are ordered by product_id. Output columns follow the statement's order."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-021-previous-product-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "same-day"
   ],
   "hints": [
    "LAG(product_id) OVER (PARTITION BY customer_id ORDER BY date, product_id).",
    "COALESCE(previous_product, 'None') only inside the concatenation."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "LAG reads the previous row of the same customer in the window order; the second sort key makes same-day transactions deterministic. The NULL is replaced only where it is displayed.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/21"
   ],
   "recommendation": {
    "rank": 21,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "customer_id",
     "product_id",
     "quantity",
     "date",
     "previous_product",
     "date_and_product"
    ],
    "exact_schema": [
     "customer_id",
     "product_id",
     "quantity",
     "date",
     "previous_product",
     "date_and_product"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "transactions",
     "columns": {
      "customer_id": "VARCHAR",
      "product_id": "VARCHAR",
      "quantity": "INTEGER",
      "date": "VARCHAR"
     },
     "sample_rows": [
      {
       "customer_id": "CUST1",
       "product_id": "PROD1",
       "quantity": 2,
       "date": "2023-01-01"
      },
      {
       "customer_id": "CUST1",
       "product_id": "PROD2",
       "quantity": 1,
       "date": "2023-01-05"
      },
      {
       "customer_id": "CUST2",
       "product_id": "PROD3",
       "quantity": 5,
       "date": "2023-02-03"
      },
      {
       "customer_id": "CUST3",
       "product_id": "PROD1",
       "quantity": 4,
       "date": "2023-02-07"
      },
      {
       "customer_id": "CUST1",
       "product_id": "PROD3",
       "quantity": 3,
       "date": "2023-02-10"
      }
     ]
    }
   ],
   "output_schema": {
    "customer_id": "value",
    "product_id": "value",
    "quantity": "value",
    "date": "value",
    "previous_product": "value",
    "date_and_product": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 21 \"Retail Stores\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-021",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Corrected reference contradiction: previous_product stays null; only its concatenated display uses the string None. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "previous_product stays NULL and only date_and_product shows 'None' (CodeDELeet's correction of ZillaCode). Same-day transactions are ordered by product_id. Output columns follow the statement's order.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: customer_id, product_id, quantity, date, previous_product, date_and_product\nSELECT *\nFROM transactions\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "strings"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: customer_id, product_id, quantity, date, previous_product, date_and_product\nSELECT *\nFROM transactions\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "strings"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(transactions):\n    # Transform the input DataFrames into the result described in the brief.\n    return transactions\n\n\ndisplay(etl(pd.DataFrame(transactions)))\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "strings"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(transactions):\n    # Transform the input DataFrames into the result described in the brief.\n    return transactions\n\n\ndisplay(etl(pl.DataFrame(transactions)))\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "strings"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'transactions') }}\n",
    "supported_operations": [
     "window-functions",
     "nulls",
     "strings"
    ],
    "model": "previous_product"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-022-active-watch-time",
   "version": "1",
   "industry": "Video streaming",
   "learning_objectives": [
    "WHERE EXISTS (SELECT 1 FROM subscription_df ... ) keeps each viewing once.",
    "Compare dates as dates; handle 'ongoing' explicitly."
   ],
   "optimization": "The question is whether a covering subscription exists, not how many there are: a correlated EXISTS (or a",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Watch time during active subscriptions",
   "difficulty": "hard",
   "topics": [
    "semi-join",
    "dates",
    "aggregation"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A streaming service wants the minutes each user watched while subscribed. A viewing counts if its date falls inside one of the user's subscriptions, start and end days included; subscription_end is a date as text, or 'ongoing' for an open subscription. A viewing covered by two overlapping subscriptions counts once. Return the users with at least one counted viewing and their total minutes.",
   "sections": [
    {
     "title": "Tables",
     "body": "user_behavior_df(user_id VARCHAR, watch_duration INTEGER, date DATE)\nsubscription_df(user_id VARCHAR, subscription_start DATE, subscription_end VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: user_id, total_watch_time. One row per user with at least one viewing during a subscription. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Joining viewings to subscriptions counts a viewing once per matching subscription, so overlapping subscriptions double it: test coverage with EXISTS (a semi-join) instead. 'ongoing' is not a date: TRY_CAST (TRY_TO_DATE in Snowflake) turns it into NULL instead of failing."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 22, \"Video Streaming Platform\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Columns are snake_case (ZillaCode used userId, watchDuration, ...). A viewing covered by overlapping subscriptions counts once (ZillaCode's join would count it twice); an edge check covers it."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-022-active-watch-time-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "overlap-and-bounds"
   ],
   "hints": [
    "WHERE EXISTS (SELECT 1 FROM subscription_df ... ) keeps each viewing once.",
    "Compare dates as dates; handle 'ongoing' explicitly."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "The question is whether a covering subscription exists, not how many there are: a correlated EXISTS (or a semi-join) keeps each viewing row once. The end date needs a safe conversion because of 'ongoing'.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "semi-join"
   },
   "related_associations": [
    "zillacode/22"
   ],
   "recommendation": {
    "rank": 22,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "user_id",
     "total_watch_time"
    ],
    "exact_schema": [
     "user_id",
     "total_watch_time"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "user_behavior_df",
     "columns": {
      "user_id": "VARCHAR",
      "watch_duration": "INTEGER",
      "date": "DATE"
     },
     "sample_rows": [
      {
       "user_id": "U1",
       "watch_duration": 45,
       "date": "2023-01-01"
      },
      {
       "user_id": "U1",
       "watch_duration": 60,
       "date": "2023-01-02"
      },
      {
       "user_id": "U2",
       "watch_duration": 70,
       "date": "2023-01-03"
      },
      {
       "user_id": "U3",
       "watch_duration": 30,
       "date": "2023-01-04"
      },
      {
       "user_id": "U2",
       "watch_duration": 50,
       "date": "2023-01-02"
      }
     ]
    },
    {
     "name": "subscription_df",
     "columns": {
      "user_id": "VARCHAR",
      "subscription_start": "DATE",
      "subscription_end": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U1",
       "subscription_start": "2023-01-01",
       "subscription_end": "2023-01-10"
      },
      {
       "user_id": "U2",
       "subscription_start": "2023-01-02",
       "subscription_end": "ongoing"
      },
      {
       "user_id": "U3",
       "subscription_start": "2022-12-25",
       "subscription_end": "2023-01-05"
      }
     ]
    }
   ],
   "output_schema": {
    "user_id": "value",
    "total_watch_time": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 22 \"Video Streaming Platform\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-022",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. TRY_CAST handles missing/invalid subscription end dates; all date comparisons are typed. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Columns are snake_case (ZillaCode used userId, watchDuration, ...). A viewing covered by overlapping subscriptions counts once (ZillaCode's join would count it twice); an edge check covers it.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: user_id, total_watch_time\nSELECT *\nFROM user_behavior_df\n",
    "supported_operations": [
     "semi-join",
     "dates",
     "aggregation"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: user_id, total_watch_time\nSELECT *\nFROM user_behavior_df\n",
    "supported_operations": [
     "semi-join",
     "dates",
     "aggregation"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(user_behavior_df, subscription_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return user_behavior_df\n\n\ndisplay(etl(pd.DataFrame(user_behavior_df), pd.DataFrame(subscription_df)))\n",
    "supported_operations": [
     "semi-join",
     "dates",
     "aggregation"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(user_behavior_df, subscription_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return user_behavior_df\n\n\ndisplay(etl(pl.DataFrame(user_behavior_df), pl.DataFrame(subscription_df)))\n",
    "supported_operations": [
     "semi-join",
     "dates",
     "aggregation"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'user_behavior_df') }}\n",
    "supported_operations": [
     "semi-join",
     "dates",
     "aggregation"
    ],
    "model": "active_watch_time"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-023-model-metrics",
   "version": "1",
   "industry": "Machine learning",
   "learning_objectives": [
    "Sum uses per model in one aggregation, average accuracy per type in another.",
    "Or AVG(accuracy) OVER (PARTITION BY model_type) on df_models before joining."
   ],
   "optimization": "The two measures live at different grains (model and model type) and must be computed before joining, or the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Model usage and accuracy per type",
   "difficulty": "medium",
   "topics": [
    "aggregation",
    "window-functions",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For every model that has usage records, return its attributes, its total number of uses, and the average accuracy of all the models of its type in df_models (models without usage included in that average). Usage of unknown models is ignored.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_models(model_id VARCHAR, model_name VARCHAR, model_type VARCHAR, accuracy DOUBLE)\ndf_usage(model_id VARCHAR, date DATE, uses INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: model_id, model_name, model_type, accuracy, total_uses, average_accuracy. One row per model with at least one usage record. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Average the accuracy over df_models, one row per model: averaging after the join with usage weighs a model by its number of usage days, and drops the unused models from the average."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 23, \"Machine Learning Metrics\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Columns are snake_case (ZillaCode used Model_ID, Total_Uses, ...)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-023-model-metrics-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "unused-and-weighted"
   ],
   "hints": [
    "Sum uses per model in one aggregation, average accuracy per type in another.",
    "Or AVG(accuracy) OVER (PARTITION BY model_type) on df_models before joining."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "The two measures live at different grains (model and model type) and must be computed before joining, or the usage rows repeat the models. A window function over df_models gives the type average on every model row.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/23"
   ],
   "recommendation": {
    "rank": 23,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "model_id",
     "model_name",
     "model_type",
     "accuracy",
     "total_uses",
     "average_accuracy"
    ],
    "exact_schema": [
     "model_id",
     "model_name",
     "model_type",
     "accuracy",
     "total_uses",
     "average_accuracy"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_models",
     "columns": {
      "model_id": "VARCHAR",
      "model_name": "VARCHAR",
      "model_type": "VARCHAR",
      "accuracy": "DOUBLE"
     },
     "sample_rows": [
      {
       "model_id": "M1",
       "model_name": "ModelA",
       "model_type": "Type1",
       "accuracy": 0.85
      },
      {
       "model_id": "M2",
       "model_name": "ModelB",
       "model_type": "Type2",
       "accuracy": 0.78
      },
      {
       "model_id": "M3",
       "model_name": "ModelC",
       "model_type": "Type1",
       "accuracy": 0.88
      },
      {
       "model_id": "M4",
       "model_name": "ModelD",
       "model_type": "Type3",
       "accuracy": 0.92
      }
     ]
    },
    {
     "name": "df_usage",
     "columns": {
      "model_id": "VARCHAR",
      "date": "DATE",
      "uses": "INTEGER"
     },
     "sample_rows": [
      {
       "model_id": "M1",
       "date": "2023-01-01",
       "uses": 100
      },
      {
       "model_id": "M1",
       "date": "2023-01-02",
       "uses": 120
      },
      {
       "model_id": "M2",
       "date": "2023-01-01",
       "uses": 200
      },
      {
       "model_id": "M3",
       "date": "2023-01-01",
       "uses": 150
      },
      {
       "model_id": "M4",
       "date": "2023-01-02",
       "uses": 130
      }
     ]
    }
   ],
   "output_schema": {
    "model_id": "value",
    "model_name": "value",
    "model_type": "value",
    "accuracy": "value",
    "total_uses": "value",
    "average_accuracy": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 23 \"Machine Learning Metrics\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-023",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Columns are snake_case (ZillaCode used Model_ID, Total_Uses, ...).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: model_id, model_name, model_type, accuracy, total_uses, average_accuracy\nSELECT *\nFROM df_models\n",
    "supported_operations": [
     "aggregation",
     "window-functions",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: model_id, model_name, model_type, accuracy, total_uses, average_accuracy\nSELECT *\nFROM df_models\n",
    "supported_operations": [
     "aggregation",
     "window-functions",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_models, df_usage):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_models\n\n\ndisplay(etl(pd.DataFrame(df_models), pd.DataFrame(df_usage)))\n",
    "supported_operations": [
     "aggregation",
     "window-functions",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_models, df_usage):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_models\n\n\ndisplay(etl(pl.DataFrame(df_models), pl.DataFrame(df_usage)))\n",
    "supported_operations": [
     "aggregation",
     "window-functions",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf_models = spark.table(\"df_models\")\ndf_usage = spark.table(\"df_usage\")\ndf_models\n",
    "supported_operations": [
     "aggregation",
     "window-functions",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df_models') }}\n",
    "supported_operations": [
     "aggregation",
     "window-functions",
     "joins"
    ],
    "model": "model_metrics"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-024-churn-timeline",
   "version": "1",
   "industry": "Software",
   "learning_objectives": [
    "SELECT DISTINCT from the activities, then FULL OUTER JOIN the three tables on user_id.",
    "ORDER BY user_id, activity_date DESC NULLS LAST, activity_type."
   ],
   "optimization": "Deduplicate the activities, full-outer-join the three tables on the user (COALESCE the key), and sort with an",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Churn timeline without duplicate activities",
   "difficulty": "hard",
   "topics": [
    "full-outer-join",
    "deduplication",
    "ordering",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Combine user accounts, activities and exit surveys into one timeline: one row per distinct activity of a user (activities logged twice with the same user, date and type count once), with the account and exit survey columns repeated. A user present in only some of the tables still appears, with NULLs for the missing columns. Sort by user_id ascending, then activity_date descending with a missing activity date last, then activity_type ascending.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_accounts(user_id VARCHAR, account_created_date DATE, location VARCHAR)\ndf_activities(user_id VARCHAR, activity_date DATE, activity_type VARCHAR)\ndf_exit_surveys(user_id VARCHAR, exit_date DATE, exit_reason VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: user_id, account_created_date, location, activity_date, activity_type, exit_date, exit_reason. One row per distinct activity, or one row for a user without activities. Row order is graded: user_id ascending, activity_date descending with NULLs last, activity_type ascending."
    },
    {
     "title": "Common pitfall",
     "body": "Descending sorts disagree on NULLs: Snowflake puts them first, DuckDB, pandas and Spark last, and Polars first unless nulls_last=True. Say NULLS LAST explicitly."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 24, \"Customer Churn\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Every user of any table appears (the statement's rule; ZillaCode's SQL started from accounts). The sort places a missing activity date last and breaks same-day ties by activity_type; row order is graded."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-024-churn-timeline-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "null-date-and-orphans"
   ],
   "hints": [
    "SELECT DISTINCT from the activities, then FULL OUTER JOIN the three tables on user_id.",
    "ORDER BY user_id, activity_date DESC NULLS LAST, activity_type."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Deduplicate the activities, full-outer-join the three tables on the user (COALESCE the key), and sort with an explicit NULL placement so the order means the same thing on every engine.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "full-outer-join"
   },
   "related_associations": [
    "zillacode/24"
   ],
   "recommendation": {
    "rank": 24,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "user_id",
     "account_created_date",
     "location",
     "activity_date",
     "activity_type",
     "exit_date",
     "exit_reason"
    ],
    "exact_schema": [
     "user_id",
     "account_created_date",
     "location",
     "activity_date",
     "activity_type",
     "exit_date",
     "exit_reason"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_accounts",
     "columns": {
      "user_id": "VARCHAR",
      "account_created_date": "DATE",
      "location": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U001",
       "account_created_date": "2023-01-01",
       "location": "New York"
      },
      {
       "user_id": "U002",
       "account_created_date": "2023-01-05",
       "location": "Chicago"
      },
      {
       "user_id": "U003",
       "account_created_date": "2023-01-10",
       "location": "San Francisco"
      }
     ]
    },
    {
     "name": "df_activities",
     "columns": {
      "user_id": "VARCHAR",
      "activity_date": "DATE",
      "activity_type": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U001",
       "activity_date": "2023-02-01",
       "activity_type": "Login"
      },
      {
       "user_id": "U001",
       "activity_date": "2023-02-01",
       "activity_type": "Login"
      },
      {
       "user_id": "U002",
       "activity_date": "2023-02-05",
       "activity_type": "File Upload"
      },
      {
       "user_id": "U002",
       "activity_date": "2023-02-05",
       "activity_type": "File Upload"
      },
      {
       "user_id": "U003",
       "activity_date": "2023-02-10",
       "activity_type": "Logout"
      }
     ]
    },
    {
     "name": "df_exit_surveys",
     "columns": {
      "user_id": "VARCHAR",
      "exit_date": "DATE",
      "exit_reason": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U001",
       "exit_date": "2023-03-01",
       "exit_reason": "Moved to a competitor"
      },
      {
       "user_id": "U002",
       "exit_date": "2023-03-05",
       "exit_reason": "Not user-friendly"
      },
      {
       "user_id": "U003",
       "exit_date": "2023-03-10",
       "exit_reason": "High pricing"
      }
     ]
    }
   ],
   "output_schema": {
    "user_id": "value",
    "account_created_date": "value",
    "location": "value",
    "activity_date": "value",
    "activity_type": "value",
    "exit_date": "value",
    "exit_reason": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 24 \"Customer Churn\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-024",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Every user of any table appears (the statement's rule; ZillaCode's SQL started from accounts). The sort places a missing activity date last and breaks same-day ties by activity_type; row order is graded.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: user_id, account_created_date, location, activity_date, activity_type, exit_date, exit_reason\nSELECT *\nFROM df_accounts\n",
    "supported_operations": [
     "full-outer-join",
     "deduplication",
     "ordering",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: user_id, account_created_date, location, activity_date, activity_type, exit_date, exit_reason\nSELECT *\nFROM df_accounts\n",
    "supported_operations": [
     "full-outer-join",
     "deduplication",
     "ordering",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_accounts, df_activities, df_exit_surveys):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_accounts\n\n\ndisplay(etl(pd.DataFrame(df_accounts), pd.DataFrame(df_activities), pd.DataFrame(df_exit_surveys)))\n",
    "supported_operations": [
     "full-outer-join",
     "deduplication",
     "ordering",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_accounts, df_activities, df_exit_surveys):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_accounts\n\n\ndisplay(etl(pl.DataFrame(df_accounts), pl.DataFrame(df_activities), pl.DataFrame(df_exit_surveys)))\n",
    "supported_operations": [
     "full-outer-join",
     "deduplication",
     "ordering",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf_accounts = spark.table(\"df_accounts\")\ndf_activities = spark.table(\"df_activities\")\ndf_exit_surveys = spark.table(\"df_exit_surveys\")\ndf_accounts\n",
    "supported_operations": [
     "full-outer-join",
     "deduplication",
     "ordering",
     "nulls"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-025-minerals-per-location",
   "version": "1",
   "industry": "Mining",
   "learning_objectives": [
    "JOIN extraction ON mines.id = extraction.mine_id.",
    "GROUP BY location, mineral ORDER BY location, mineral."
   ],
   "optimization": "Join on the differently named keys, aggregate at the (location, mineral) grain and sort by both.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Mineral output per location",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "joins",
    "ordering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A mining company records extractions per mine. Return the total quantity of each mineral extracted in each location (several mines can share a location), sorted by location and then by mineral. Extractions from unknown mines are ignored.",
   "sections": [
    {
     "title": "Tables",
     "body": "mines(id INTEGER, name VARCHAR, location VARCHAR)\nextraction(mine_id INTEGER, date DATE, mineral VARCHAR, quantity DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: location, mineral, total_quantity. One row per (location, mineral). Row order is graded: location ascending, then mineral ascending."
    },
    {
     "title": "Common pitfall",
     "body": "Group by location, not by mine: two mines in the same location must add up. The join keys have different names (mines.id, extraction.mine_id)."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 25, \"Mining Corporation\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-025-minerals-per-location-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "shared-location"
   ],
   "hints": [
    "JOIN extraction ON mines.id = extraction.mine_id.",
    "GROUP BY location, mineral ORDER BY location, mineral."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Join on the differently named keys, aggregate at the (location, mineral) grain and sort by both.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/25"
   ],
   "recommendation": {
    "rank": 25,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "location",
     "mineral",
     "total_quantity"
    ],
    "exact_schema": [
     "location",
     "mineral",
     "total_quantity"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "mines",
     "columns": {
      "id": "INTEGER",
      "name": "VARCHAR",
      "location": "VARCHAR"
     },
     "sample_rows": [
      {
       "id": 1,
       "name": "Mine Alpha",
       "location": "Australia"
      },
      {
       "id": 2,
       "name": "Mine Beta",
       "location": "Canada"
      },
      {
       "id": 3,
       "name": "Mine Gamma",
       "location": "South Africa"
      }
     ]
    },
    {
     "name": "extraction",
     "columns": {
      "mine_id": "INTEGER",
      "date": "DATE",
      "mineral": "VARCHAR",
      "quantity": "DOUBLE"
     },
     "sample_rows": [
      {
       "mine_id": 1,
       "date": "2023-06-30",
       "mineral": "Gold",
       "quantity": 1000.0
      },
      {
       "mine_id": 2,
       "date": "2023-06-30",
       "mineral": "Silver",
       "quantity": 1200.0
      },
      {
       "mine_id": 3,
       "date": "2023-06-30",
       "mineral": "Diamond",
       "quantity": 800.0
      },
      {
       "mine_id": 1,
       "date": "2023-06-29",
       "mineral": "Gold",
       "quantity": 900.0
      },
      {
       "mine_id": 2,
       "date": "2023-06-29",
       "mineral": "Silver",
       "quantity": 1300.0
      },
      {
       "mine_id": 3,
       "date": "2023-06-29",
       "mineral": "Diamond",
       "quantity": 750.0
      }
     ]
    }
   ],
   "output_schema": {
    "location": "value",
    "mineral": "value",
    "total_quantity": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 25 \"Mining Corporation\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-025",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: location, mineral, total_quantity\nSELECT *\nFROM mines\n",
    "supported_operations": [
     "aggregation",
     "joins",
     "ordering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: location, mineral, total_quantity\nSELECT *\nFROM mines\n",
    "supported_operations": [
     "aggregation",
     "joins",
     "ordering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(mines, extraction):\n    # Transform the input DataFrames into the result described in the brief.\n    return mines\n\n\ndisplay(etl(pd.DataFrame(mines), pd.DataFrame(extraction)))\n",
    "supported_operations": [
     "aggregation",
     "joins",
     "ordering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(mines, extraction):\n    # Transform the input DataFrames into the result described in the brief.\n    return mines\n\n\ndisplay(etl(pl.DataFrame(mines), pl.DataFrame(extraction)))\n",
    "supported_operations": [
     "aggregation",
     "joins",
     "ordering"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nmines = spark.table(\"mines\")\nextraction = spark.table(\"extraction\")\nmines\n",
    "supported_operations": [
     "aggregation",
     "joins",
     "ordering"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-026-discount-tags",
   "version": "1",
   "industry": "Consumer goods",
   "learning_objectives": [
    "The pattern \\[([0-9]{1,3})%\\s*off\\] captures the percentage.",
    "Divide by 100 and turn a missing match into 0."
   ],
   "optimization": "A regular expression locates the tag and a capture group (or a second extraction) keeps its digits. A missing",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Discounts hidden in descriptions",
   "difficulty": "medium",
   "topics": [
    "regular-expressions",
    "strings",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Product descriptions may carry a discount tag such as '[10% off]': a percentage of one to three digits, '%', optional spaces, then 'off', in square brackets, in lower case. Add a column discount with the first tag's percentage as a decimal (0.10 for 10%), or 0 when the description has no tag. Other numbers in the text are not discounts.",
   "sections": [
    {
     "title": "Tables",
     "body": "df(store_id VARCHAR, product_name VARCHAR, category VARCHAR, sold_units INTEGER, description VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: store_id, product_name, category, sold_units, description, discount. One row per input row. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Extract the digits of the tag, not the first number of the description ('Pack of 6 [10% off]' is 10%). Snowflake's REGEXP_SUBSTR has no capture group in this subset: extract the tag, then its digits."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 26, \"Consumer Goods Discount Confusion\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Columns are snake_case (ZillaCode used StoreID, Discount, ...). ZillaCode had no SQL answer for this problem; CodeDELeet authored one, and Datapass's references follow the statement."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-026-discount-tags-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "tag-variants"
   ],
   "hints": [
    "The pattern \\[([0-9]{1,3})%\\s*off\\] captures the percentage.",
    "Divide by 100 and turn a missing match into 0."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A regular expression locates the tag and a capture group (or a second extraction) keeps its digits. A missing match is NULL (Snowflake), '' (DuckDB) or NaN (pandas), so the conversion must be safe (TRY_CAST) and the result defaulted to 0.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "regular-expressions"
   },
   "related_associations": [
    "zillacode/26"
   ],
   "recommendation": {
    "rank": 26,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "store_id",
     "product_name",
     "category",
     "sold_units",
     "description",
     "discount"
    ],
    "exact_schema": [
     "store_id",
     "product_name",
     "category",
     "sold_units",
     "description",
     "discount"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df",
     "columns": {
      "store_id": "VARCHAR",
      "product_name": "VARCHAR",
      "category": "VARCHAR",
      "sold_units": "INTEGER",
      "description": "VARCHAR"
     },
     "sample_rows": [
      {
       "store_id": "S101",
       "product_name": "Biscuits",
       "category": "Food",
       "sold_units": 120,
       "description": "Tasty Biscuits [10% off]"
      },
      {
       "store_id": "S102",
       "product_name": "Shampoo",
       "category": "Hygiene",
       "sold_units": 85,
       "description": "Smoothens Hair [5% off]"
      },
      {
       "store_id": "S103",
       "product_name": "Banana",
       "category": "Food",
       "sold_units": 150,
       "description": "Fresh Bananas"
      },
      {
       "store_id": "S101",
       "product_name": "Toothpaste",
       "category": "Hygiene",
       "sold_units": 300,
       "description": "Protects Teeth"
      },
      {
       "store_id": "S102",
       "product_name": "Shirt",
       "category": "Clothes",
       "sold_units": 65,
       "description": "Cotton Shirts [20% off]"
      }
     ]
    }
   ],
   "output_schema": {
    "store_id": "value",
    "product_name": "value",
    "category": "value",
    "sold_units": "value",
    "description": "value",
    "discount": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 26 \"Consumer Goods Discount Confusion\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-026",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. The upstream SQL answer was absent. Authored regex extraction implements the published discount transformation. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Columns are snake_case (ZillaCode used StoreID, Discount, ...). ZillaCode had no SQL answer for this problem; CodeDELeet authored one, and Datapass's references follow the statement.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: store_id, product_name, category, sold_units, description, discount\nSELECT *\nFROM df\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: store_id, product_name, category, sold_units, description, discount\nSELECT *\nFROM df\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df):\n    # Transform the input DataFrames into the result described in the brief.\n    return df\n\n\ndisplay(etl(pd.DataFrame(df)))\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df):\n    # Transform the input DataFrames into the result described in the brief.\n    return df\n\n\ndisplay(etl(pl.DataFrame(df)))\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df') }}\n",
    "supported_operations": [
     "regular-expressions",
     "strings",
     "nulls"
    ],
    "model": "discount_tags"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-027-mortgage-rate",
   "version": "1",
   "industry": "Mortgages",
   "learning_objectives": [
    "Join user_mortgages to mortgage_details, then group by mortgage_type.",
    "SUM(interest_rate) / COUNT(DISTINCT user_id)."
   ],
   "optimization": "Implement the business definition exactly: a sum over the joined rows divided by a distinct count. It equals",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Rate of each mortgage type",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "distinct"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each mortgage type held by at least one user, return rate_of_mortgage, defined by the lender as the sum of the interest rates of the users' mortgages of that type divided by the number of distinct users holding that type. A user holding two mortgages of the same type adds both rates but counts once.",
   "sections": [
    {
     "title": "Tables",
     "body": "mortgage_details(mortgage_id VARCHAR, mortgage_type VARCHAR, interest_rate DOUBLE)\nuser_mortgages(user_id VARCHAR, mortgage_id VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: mortgage_type, rate_of_mortgage. One row per mortgage type held by at least one user. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "This is not AVG(interest_rate): the denominator counts distinct users, not mortgages."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 27, \"Mortgage Interest Rates\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Tables and columns are snake_case (ZillaCode used MortgageDetails, RateOfMortgage, ...)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-027-mortgage-rate-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "two-mortgages-one-user"
   ],
   "hints": [
    "Join user_mortgages to mortgage_details, then group by mortgage_type.",
    "SUM(interest_rate) / COUNT(DISTINCT user_id)."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Implement the business definition exactly: a sum over the joined rows divided by a distinct count. It equals the average only when no user holds two mortgages of the same type.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/27"
   ],
   "recommendation": {
    "rank": 27,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "mortgage_type",
     "rate_of_mortgage"
    ],
    "exact_schema": [
     "mortgage_type",
     "rate_of_mortgage"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "mortgage_details",
     "columns": {
      "mortgage_id": "VARCHAR",
      "mortgage_type": "VARCHAR",
      "interest_rate": "DOUBLE"
     },
     "sample_rows": [
      {
       "mortgage_id": "M1",
       "mortgage_type": "Fixed",
       "interest_rate": 4.5
      },
      {
       "mortgage_id": "M2",
       "mortgage_type": "Variable",
       "interest_rate": 3.2
      },
      {
       "mortgage_id": "M3",
       "mortgage_type": "Adjustable",
       "interest_rate": 2.8
      }
     ]
    },
    {
     "name": "user_mortgages",
     "columns": {
      "user_id": "VARCHAR",
      "mortgage_id": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U1",
       "mortgage_id": "M1"
      },
      {
       "user_id": "U2",
       "mortgage_id": "M1"
      },
      {
       "user_id": "U3",
       "mortgage_id": "M2"
      },
      {
       "user_id": "U4",
       "mortgage_id": "M3"
      }
     ]
    }
   ],
   "output_schema": {
    "mortgage_type": "value",
    "rate_of_mortgage": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 27 \"Mortgage Interest Rates\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-027",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Tables and columns are snake_case (ZillaCode used MortgageDetails, RateOfMortgage, ...).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: mortgage_type, rate_of_mortgage\nSELECT *\nFROM mortgage_details\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: mortgage_type, rate_of_mortgage\nSELECT *\nFROM mortgage_details\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(mortgage_details, user_mortgages):\n    # Transform the input DataFrames into the result described in the brief.\n    return mortgage_details\n\n\ndisplay(etl(pd.DataFrame(mortgage_details), pd.DataFrame(user_mortgages)))\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(mortgage_details, user_mortgages):\n    # Transform the input DataFrames into the result described in the brief.\n    return mortgage_details\n\n\ndisplay(etl(pl.DataFrame(mortgage_details), pl.DataFrame(user_mortgages)))\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nmortgage_details = spark.table(\"mortgage_details\")\nuser_mortgages = spark.table(\"user_mortgages\")\nmortgage_details\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'mortgage_details') }}\n",
    "supported_operations": [
     "aggregation",
     "distinct"
    ],
    "model": "mortgage_rate"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-028-serial-numbers",
   "version": "1",
   "industry": "Manufacturing",
   "learning_objectives": [
    "ROW_NUMBER() OVER (ORDER BY manufacturing_date, product_id).",
    "Number after the join, not before."
   ],
   "optimization": "ROW_NUMBER over the whole joined result, ordered by the date and the product id. Numbering df1 before the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Serial numbers by manufacturing date",
   "difficulty": "easy",
   "topics": [
    "window-functions",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Join the manufacturing records (df1) to the product details (df2) on product_id and number the joined rows 1, 2, 3, ... by manufacturing_date ascending; rows with the same date are numbered in product_id order. Records without product details are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "df1(product_id VARCHAR, manufacturing_date DATE, manufacturing_location VARCHAR)\ndf2(product_id VARCHAR, product_name VARCHAR, product_type VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: product_id, manufacturing_date, manufacturing_location, product_name, product_type, row_number. One row per manufacturing record with product details. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "The numbering is global (no PARTITION BY) and needs a tie-breaker for equal dates to be deterministic."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 28, \"Organizing Manufacturing Parts\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Same-date rows are numbered in product_id order (ZillaCode left ties to the engine)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-028-serial-numbers-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "same-date-and-missing-details"
   ],
   "hints": [
    "ROW_NUMBER() OVER (ORDER BY manufacturing_date, product_id).",
    "Number after the join, not before."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "ROW_NUMBER over the whole joined result, ordered by the date and the product id. Numbering df1 before the join would leave gaps where records have no product details.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/28"
   ],
   "recommendation": {
    "rank": 28,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "product_id",
     "manufacturing_date",
     "manufacturing_location",
     "product_name",
     "product_type",
     "row_number"
    ],
    "exact_schema": [
     "product_id",
     "manufacturing_date",
     "manufacturing_location",
     "product_name",
     "product_type",
     "row_number"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df1",
     "columns": {
      "product_id": "VARCHAR",
      "manufacturing_date": "DATE",
      "manufacturing_location": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": "P1",
       "manufacturing_date": "2023-01-01",
       "manufacturing_location": "Location_A"
      },
      {
       "product_id": "P2",
       "manufacturing_date": "2023-01-02",
       "manufacturing_location": "Location_B"
      },
      {
       "product_id": "P3",
       "manufacturing_date": "2023-01-03",
       "manufacturing_location": "Location_C"
      }
     ]
    },
    {
     "name": "df2",
     "columns": {
      "product_id": "VARCHAR",
      "product_name": "VARCHAR",
      "product_type": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": "P1",
       "product_name": "Widget_A",
       "product_type": "Widget"
      },
      {
       "product_id": "P2",
       "product_name": "Gadget_B",
       "product_type": "Gadget"
      },
      {
       "product_id": "P3",
       "product_name": "Device_C",
       "product_type": "Device"
      }
     ]
    }
   ],
   "output_schema": {
    "product_id": "value",
    "manufacturing_date": "value",
    "manufacturing_location": "value",
    "product_name": "value",
    "product_type": "value",
    "row_number": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 28 \"Organizing Manufacturing Parts\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-028",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit join projection removes the duplicate product_id column. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Same-date rows are numbered in product_id order (ZillaCode left ties to the engine).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: product_id, manufacturing_date, manufacturing_location, product_name, product_type, row_number\nSELECT *\nFROM df1\n",
    "supported_operations": [
     "window-functions",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: product_id, manufacturing_date, manufacturing_location, product_name, product_type, row_number\nSELECT *\nFROM df1\n",
    "supported_operations": [
     "window-functions",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df1, df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return df1\n\n\ndisplay(etl(pd.DataFrame(df1), pd.DataFrame(df2)))\n",
    "supported_operations": [
     "window-functions",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df1, df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return df1\n\n\ndisplay(etl(pl.DataFrame(df1), pl.DataFrame(df2)))\n",
    "supported_operations": [
     "window-functions",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf1 = spark.table(\"df1\")\ndf2 = spark.table(\"df2\")\ndf1\n",
    "supported_operations": [
     "window-functions",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df1') }}\n",
    "supported_operations": [
     "window-functions",
     "joins"
    ],
    "model": "serial_numbers"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-029-budget-variance",
   "version": "1",
   "industry": "Public sector",
   "learning_objectives": [
    "VAR_SAMP per department in each table, then an inner join.",
    "TRUNC then cast to an integer."
   ],
   "optimization": "Aggregate each table per department with the sample variance, truncate, and join the two results. The NULL for",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Variance of budgets and spending",
   "difficulty": "medium",
   "topics": [
    "aggregation",
    "statistics",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each department present in both tables, return the sample variance of its yearly budget and of its yearly spending, each truncated toward zero to an integer. A department with a single year has no sample variance (NULL).",
   "sections": [
    {
     "title": "Tables",
     "body": "budget_df(department VARCHAR, year INTEGER, budget DOUBLE)\nspending_df(department VARCHAR, year INTEGER, spending DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: department, budget_variance, spending_variance. One row per department present in both tables. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Sample variance divides by n - 1 (VAR_SAMP, pandas' var(), Polars' var()); VAR_POP divides by n. With one value it is undefined, and truncating is not rounding."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 29, \"Government Budgeting\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Both variances are truncated integers, as the statement's schema says (ZillaCode truncated only the spending variance). Columns are snake_case."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-029-budget-variance-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "single-year-and-truncation"
   ],
   "hints": [
    "VAR_SAMP per department in each table, then an inner join.",
    "TRUNC then cast to an integer."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Aggregate each table per department with the sample variance, truncate, and join the two results. The NULL for single-year departments must survive the truncation (pandas' astype(int) would fail on NaN).",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/29"
   ],
   "recommendation": {
    "rank": 29,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "department",
     "budget_variance",
     "spending_variance"
    ],
    "exact_schema": [
     "department",
     "budget_variance",
     "spending_variance"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "budget_df",
     "columns": {
      "department": "VARCHAR",
      "year": "INTEGER",
      "budget": "DOUBLE"
     },
     "sample_rows": [
      {
       "department": "Health",
       "year": 2019,
       "budget": 750.0
      },
      {
       "department": "Education",
       "year": 2019,
       "budget": 500.0
      },
      {
       "department": "Health",
       "year": 2020,
       "budget": 800.0
      },
      {
       "department": "Education",
       "year": 2020,
       "budget": 550.0
      }
     ]
    },
    {
     "name": "spending_df",
     "columns": {
      "department": "VARCHAR",
      "year": "INTEGER",
      "spending": "DOUBLE"
     },
     "sample_rows": [
      {
       "department": "Health",
       "year": 2019,
       "spending": 700.0
      },
      {
       "department": "Education",
       "year": 2019,
       "spending": 450.0
      },
      {
       "department": "Health",
       "year": 2020,
       "spending": 780.0
      },
      {
       "department": "Education",
       "year": 2020,
       "spending": 540.0
      }
     ]
    }
   ],
   "output_schema": {
    "department": "value",
    "budget_variance": "value",
    "spending_variance": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 29 \"Government Budgeting\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-029",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Sample variance with truncation before integer conversion matches the Pandas contract. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Both variances are truncated integers, as the statement's schema says (ZillaCode truncated only the spending variance). Columns are snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: department, budget_variance, spending_variance\nSELECT *\nFROM budget_df\n",
    "supported_operations": [
     "aggregation",
     "statistics",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: department, budget_variance, spending_variance\nSELECT *\nFROM budget_df\n",
    "supported_operations": [
     "aggregation",
     "statistics",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(budget_df, spending_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return budget_df\n\n\ndisplay(etl(pd.DataFrame(budget_df), pd.DataFrame(spending_df)))\n",
    "supported_operations": [
     "aggregation",
     "statistics",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(budget_df, spending_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return budget_df\n\n\ndisplay(etl(pl.DataFrame(budget_df), pl.DataFrame(spending_df)))\n",
    "supported_operations": [
     "aggregation",
     "statistics",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'budget_df') }}\n",
    "supported_operations": [
     "aggregation",
     "statistics",
     "nulls"
    ],
    "model": "budget_variance"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-030-valid-keys",
   "version": "1",
   "industry": "Accounting",
   "learning_objectives": [
    "COUNT(*) OVER (PARTITION BY transaction_id) = 1 marks unique ids.",
    "Check the shape with a regular expression and the calendar with TRY_CAST (TRY_TO_DATE in Snowflake)."
   ],
   "optimization": "Data quality rules as filters: key positivity, key uniqueness over the whole feed (a window count), and a date",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Drop invalid and duplicate keys",
   "difficulty": "hard",
   "topics": [
    "data-quality",
    "window-functions",
    "dates"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Clean two feeds before combining them. A transaction is valid when its transaction_id is greater than 0 and appears only once in df_transactions (every copy of a duplicated id is dropped), and its date is a real calendar date written 'YYYY-MM-DD'. A client is valid when its client_id is greater than 0 and appears only once in df_clients. Return the valid transactions of valid clients with the client's name and industry.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_transactions(transaction_id INTEGER, client_id INTEGER, date VARCHAR, amount DOUBLE)\ndf_clients(client_id INTEGER, client_name VARCHAR, industry VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: transaction_id, client_id, date, amount, client_name, industry. One row per valid transaction of a valid client. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "A duplicated key is ambiguous: keeping 'the first' copy depends on the engine's row order. Count the copies over the whole feed (COUNT(*) OVER (PARTITION BY key)) before filtering. '2023-25-01' has the right shape but is not a date."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 30, \"Careful Handling Transactional Data\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Every copy of a duplicated key is dropped, as the statement says (ZillaCode kept an arbitrary first copy), and a date must be a real calendar date (ZillaCode kept '2023-25-01'). amount stays a DOUBLE. Columns are snake_case."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-030-valid-keys-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "duplicates-and-bad-dates"
   ],
   "hints": [
    "COUNT(*) OVER (PARTITION BY transaction_id) = 1 marks unique ids.",
    "Check the shape with a regular expression and the calendar with TRY_CAST (TRY_TO_DATE in Snowflake)."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Data quality rules as filters: key positivity, key uniqueness over the whole feed (a window count), and a date that both matches the text format and parses to a real date. Then an inner join keeps transactions of valid clients only.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "data-quality"
   },
   "related_associations": [
    "zillacode/30"
   ],
   "recommendation": {
    "rank": 30,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "transaction_id",
     "client_id",
     "date",
     "amount",
     "client_name",
     "industry"
    ],
    "exact_schema": [
     "transaction_id",
     "client_id",
     "date",
     "amount",
     "client_name",
     "industry"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_transactions",
     "columns": {
      "transaction_id": "INTEGER",
      "client_id": "INTEGER",
      "date": "VARCHAR",
      "amount": "DOUBLE"
     },
     "sample_rows": [
      {
       "transaction_id": 1,
       "client_id": 1,
       "date": "2023-07-01",
       "amount": 100.0
      },
      {
       "transaction_id": 2,
       "client_id": 1,
       "date": "2023-07-02",
       "amount": 150.0
      },
      {
       "transaction_id": 2,
       "client_id": 2,
       "date": "2023-07-01",
       "amount": 200.0
      },
      {
       "transaction_id": 3,
       "client_id": 3,
       "date": "2023-07-03",
       "amount": 250.0
      },
      {
       "transaction_id": -4,
       "client_id": 4,
       "date": "2023-07-04",
       "amount": 300.0
      },
      {
       "transaction_id": 5,
       "client_id": 2,
       "date": "2023-25-01",
       "amount": 350.0
      },
      {
       "transaction_id": 6,
       "client_id": 1,
       "date": "2023-07-02",
       "amount": 400.0
      },
      {
       "transaction_id": 7,
       "client_id": 6,
       "date": "2023-07-01",
       "amount": 450.0
      },
      {
       "transaction_id": 8,
       "client_id": 7,
       "date": "2023-07-03",
       "amount": 500.0
      },
      {
       "transaction_id": 9,
       "client_id": -8,
       "date": "2023-07-04",
       "amount": 550.0
      }
     ]
    },
    {
     "name": "df_clients",
     "columns": {
      "client_id": "INTEGER",
      "client_name": "VARCHAR",
      "industry": "VARCHAR"
     },
     "sample_rows": [
      {
       "client_id": 1,
       "client_name": "Client1",
       "industry": "Tech"
      },
      {
       "client_id": 2,
       "client_name": "Client2",
       "industry": "Finance"
      },
      {
       "client_id": 3,
       "client_name": "Client3",
       "industry": "Real Estate"
      },
      {
       "client_id": 4,
       "client_name": "Client4",
       "industry": "Healthcare"
      },
      {
       "client_id": 5,
       "client_name": "Client5",
       "industry": "Tech"
      },
      {
       "client_id": 1,
       "client_name": "Client6",
       "industry": "Finance"
      },
      {
       "client_id": 6,
       "client_name": "Client7",
       "industry": "Real Estate"
      },
      {
       "client_id": -7,
       "client_name": "Client8",
       "industry": "Healthcare"
      },
      {
       "client_id": 8,
       "client_name": "Client9",
       "industry": "Tech"
      },
      {
       "client_id": 2,
       "client_name": "Client10",
       "industry": "Finance"
      }
     ]
    }
   ],
   "output_schema": {
    "transaction_id": "value",
    "client_id": "value",
    "date": "value",
    "amount": "value",
    "client_name": "value",
    "industry": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 30 \"Careful Handling Transactional Data\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-030",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Every copy of a duplicated key is dropped, as the statement says (ZillaCode kept an arbitrary first copy), and a date must be a real calendar date (ZillaCode kept '2023-25-01'). amount stays a DOUBLE. Columns are snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: transaction_id, client_id, date, amount, client_name, industry\nSELECT *\nFROM df_transactions\n",
    "supported_operations": [
     "data-quality",
     "window-functions",
     "dates"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: transaction_id, client_id, date, amount, client_name, industry\nSELECT *\nFROM df_transactions\n",
    "supported_operations": [
     "data-quality",
     "window-functions",
     "dates"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_transactions, df_clients):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_transactions\n\n\ndisplay(etl(pd.DataFrame(df_transactions), pd.DataFrame(df_clients)))\n",
    "supported_operations": [
     "data-quality",
     "window-functions",
     "dates"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_transactions, df_clients):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_transactions\n\n\ndisplay(etl(pl.DataFrame(df_transactions), pl.DataFrame(df_clients)))\n",
    "supported_operations": [
     "data-quality",
     "window-functions",
     "dates"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df_transactions') }}\n",
    "supported_operations": [
     "data-quality",
     "window-functions",
     "dates"
    ],
    "model": "valid_keys"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-031-weekend-orders",
   "version": "1",
   "industry": "E-commerce",
   "learning_objectives": [
    "Parse with a format and a safe function: try_strptime (DuckDB), TRY_TO_DATE(x, 'MM/DD/YYYY') (Snowflake).",
    "Keep order_date as the original text in the output."
   ],
   "optimization": "Parse the text date safely, then test the day of week with the engine's own numbering. An unparseable date",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Weekend orders from text dates",
   "difficulty": "medium",
   "topics": [
    "dates",
    "nulls",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Orders store their date as text in 'MM/DD/YYYY' format, and some dates are wrong. Return one row per order line with the user, the product's name and category, the original order_date text, and is_weekend: true for a Saturday or Sunday, false for another day, and NULL when order_date is not a valid 'MM/DD/YYYY' date (the order is kept). Order lines of unknown products are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_orders(order_id VARCHAR, product_id VARCHAR, user_id VARCHAR, order_date VARCHAR)\ndf_products(product_id VARCHAR, product_name VARCHAR, category VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: user_id, product_name, category, order_date, is_weekend. One row per order line with a known product. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Day numbering differs: DuckDB and Snowflake's DAYOFWEEK give Sunday = 0 and Saturday = 6, pandas' dayofweek gives Monday = 0 and Sunday = 6, Polars' weekday() Monday = 1 and Sunday = 7. A failed parse is NULL (NaT), and comparing it must not turn into false."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 31, \"Ecommerce Datetimes\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: An invalid date keeps its order line with a NULL is_weekend (the statement asked to handle wrong formats without a rule). order_id is text, as the statement says. is_weekend is a Boolean (CodeDELeet's correction of ZillaCode's 0/1)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-031-weekend-orders-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "weekdays-and-bad-dates"
   ],
   "hints": [
    "Parse with a format and a safe function: try_strptime (DuckDB), TRY_TO_DATE(x, 'MM/DD/YYYY') (Snowflake).",
    "Keep order_date as the original text in the output."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Parse the text date safely, then test the day of week with the engine's own numbering. An unparseable date stays NULL through the comparison, so is_weekend is NULL rather than false.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "dates"
   },
   "related_associations": [
    "zillacode/31"
   ],
   "recommendation": {
    "rank": 31,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "user_id",
     "product_name",
     "category",
     "order_date",
     "is_weekend"
    ],
    "exact_schema": [
     "user_id",
     "product_name",
     "category",
     "order_date",
     "is_weekend"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_orders",
     "columns": {
      "order_id": "VARCHAR",
      "product_id": "VARCHAR",
      "user_id": "VARCHAR",
      "order_date": "VARCHAR"
     },
     "sample_rows": [
      {
       "order_id": "1",
       "product_id": "P001",
       "user_id": "U001",
       "order_date": "02/25/2023"
      },
      {
       "order_id": "2",
       "product_id": "P002",
       "user_id": "U001",
       "order_date": "03/14/2023"
      },
      {
       "order_id": "3",
       "product_id": "P001",
       "user_id": "U002",
       "order_date": "03/16/2023"
      },
      {
       "order_id": "4",
       "product_id": "P003",
       "user_id": "U002",
       "order_date": "03/18/2023"
      },
      {
       "order_id": "5",
       "product_id": "P004",
       "user_id": "U003",
       "order_date": "04/01/2023"
      }
     ]
    },
    {
     "name": "df_products",
     "columns": {
      "product_id": "VARCHAR",
      "product_name": "VARCHAR",
      "category": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": "P001",
       "product_name": "Product 1",
       "category": "Electronics"
      },
      {
       "product_id": "P002",
       "product_name": "Product 2",
       "category": "Clothing"
      },
      {
       "product_id": "P003",
       "product_name": "Product 3",
       "category": "Home Goods"
      },
      {
       "product_id": "P004",
       "product_name": "Product 4",
       "category": "Books"
      }
     ]
    }
   ],
   "output_schema": {
    "user_id": "value",
    "product_name": "value",
    "category": "value",
    "order_date": "value",
    "is_weekend": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 31 \"Ecommerce Datetimes\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-031",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Normalized is_weekend expected type from legacy 0/1 serialization to a Boolean contract. DuckDB day-of-week uses Sunday=0; retain the original date-string output. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "An invalid date keeps its order line with a NULL is_weekend (the statement asked to handle wrong formats without a rule). order_id is text, as the statement says. is_weekend is a Boolean (CodeDELeet's correction of ZillaCode's 0/1).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: user_id, product_name, category, order_date, is_weekend\nSELECT *\nFROM df_orders\n",
    "supported_operations": [
     "dates",
     "nulls",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: user_id, product_name, category, order_date, is_weekend\nSELECT *\nFROM df_orders\n",
    "supported_operations": [
     "dates",
     "nulls",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_orders, df_products):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_orders\n\n\ndisplay(etl(pd.DataFrame(df_orders), pd.DataFrame(df_products)))\n",
    "supported_operations": [
     "dates",
     "nulls",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_orders, df_products):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_orders\n\n\ndisplay(etl(pl.DataFrame(df_orders), pl.DataFrame(df_products)))\n",
    "supported_operations": [
     "dates",
     "nulls",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df_orders') }}\n",
    "supported_operations": [
     "dates",
     "nulls",
     "joins"
    ],
    "model": "weekend_orders"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-032-anomalous-rides",
   "version": "1",
   "industry": "Amusement parks",
   "learning_objectives": [
    "Average per ride first, then average those averages.",
    "CROSS JOIN the one-row overall average to every ride."
   ],
   "optimization": "Two levels of aggregation: per ride, then over the per-ride results. A one-row result can be attached to every",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Rides with anomalous ratings",
   "difficulty": "medium",
   "topics": [
    "aggregation",
    "cross-join",
    "conditional-logic"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each ride that has ratings, return its average rating and is_anomalous: true when that average is below half, or above one and a half times, the overall average. The overall average is the average of the rides' averages (each ride weighs the same), over the rides of the rides table. Ratings of unknown rides are ignored; a NULL rating is not a rating.",
   "sections": [
    {
     "title": "Tables",
     "body": "rides(ride_id VARCHAR, ride_name VARCHAR, type VARCHAR, capacity INTEGER)\nvisitors(visitor_id VARCHAR, ride_id VARCHAR, timestamp VARCHAR, rating INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: ride_id, ride_name, average_rating, is_anomalous. One row per ride with at least one rating. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "The overall average is the mean of the ride averages, not AVG(rating) over all visits: a ride with many visits would otherwise dominate it."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 32, \"Amusement Park Outlier\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The output keeps the statement's columns (ZillaCode also returned type and capacity). Ratings of rides missing from rides are ignored; is_anomalous is a Boolean (CodeDELeet's correction of 0/1)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-032-anomalous-rides-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "weighted-average-trap"
   ],
   "hints": [
    "Average per ride first, then average those averages.",
    "CROSS JOIN the one-row overall average to every ride."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Two levels of aggregation: per ride, then over the per-ride results. A one-row result can be attached to every row with a CROSS JOIN (or a scalar subquery).",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/32"
   ],
   "recommendation": {
    "rank": 32,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "ride_id",
     "ride_name",
     "average_rating",
     "is_anomalous"
    ],
    "exact_schema": [
     "ride_id",
     "ride_name",
     "average_rating",
     "is_anomalous"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "rides",
     "columns": {
      "ride_id": "VARCHAR",
      "ride_name": "VARCHAR",
      "type": "VARCHAR",
      "capacity": "INTEGER"
     },
     "sample_rows": [
      {
       "ride_id": "r1",
       "ride_name": "Roller Coaster",
       "type": "Thrill",
       "capacity": 24
      },
      {
       "ride_id": "r2",
       "ride_name": "Ferris Wheel",
       "type": "Observation",
       "capacity": 60
      },
      {
       "ride_id": "r3",
       "ride_name": "Log Flume",
       "type": "Water",
       "capacity": 16
      },
      {
       "ride_id": "r4",
       "ride_name": "Bumper Cars",
       "type": "Family",
       "capacity": 20
      },
      {
       "ride_id": "r5",
       "ride_name": "Merry-Go-Round",
       "type": "Classic",
       "capacity": 40
      }
     ]
    },
    {
     "name": "visitors",
     "columns": {
      "visitor_id": "VARCHAR",
      "ride_id": "VARCHAR",
      "timestamp": "VARCHAR",
      "rating": "INTEGER"
     },
     "sample_rows": [
      {
       "visitor_id": "v1",
       "ride_id": "r1",
       "timestamp": "2023-07-01 10:00:00",
       "rating": 5
      },
      {
       "visitor_id": "v2",
       "ride_id": "r1",
       "timestamp": "2023-07-01 10:30:00",
       "rating": 4
      },
      {
       "visitor_id": "v1",
       "ride_id": "r2",
       "timestamp": "2023-07-01 11:00:00",
       "rating": 3
      },
      {
       "visitor_id": "v3",
       "ride_id": "r3",
       "timestamp": "2023-07-01 11:30:00",
       "rating": 2
      },
      {
       "visitor_id": "v4",
       "ride_id": "r4",
       "timestamp": "2023-07-01 12:00:00",
       "rating": 5
      }
     ]
    }
   ],
   "output_schema": {
    "ride_id": "value",
    "ride_name": "value",
    "average_rating": "value",
    "is_anomalous": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 32 \"Amusement Park Outlier\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-032",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Normalized is_anomalous expected type from legacy 0/1 serialization to a Boolean contract. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The output keeps the statement's columns (ZillaCode also returned type and capacity). Ratings of rides missing from rides are ignored; is_anomalous is a Boolean (CodeDELeet's correction of 0/1).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: ride_id, ride_name, average_rating, is_anomalous\nSELECT *\nFROM rides\n",
    "supported_operations": [
     "aggregation",
     "cross-join",
     "conditional-logic"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: ride_id, ride_name, average_rating, is_anomalous\nSELECT *\nFROM rides\n",
    "supported_operations": [
     "aggregation",
     "cross-join",
     "conditional-logic"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(rides, visitors):\n    # Transform the input DataFrames into the result described in the brief.\n    return rides\n\n\ndisplay(etl(pd.DataFrame(rides), pd.DataFrame(visitors)))\n",
    "supported_operations": [
     "aggregation",
     "cross-join",
     "conditional-logic"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(rides, visitors):\n    # Transform the input DataFrames into the result described in the brief.\n    return rides\n\n\ndisplay(etl(pl.DataFrame(rides), pl.DataFrame(visitors)))\n",
    "supported_operations": [
     "aggregation",
     "cross-join",
     "conditional-logic"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'rides') }}\n",
    "supported_operations": [
     "aggregation",
     "cross-join",
     "conditional-logic"
    ],
    "model": "anomalous_rides"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-033-status-labels",
   "version": "1",
   "industry": "Aerospace",
   "learning_objectives": [
    "LEFT JOIN company_df so equipment without a known company stays.",
    "CASE WHEN active AND country = 'USA' THEN ... WHEN active THEN 'Foreign Active' ELSE 'Inactive' END."
   ],
   "optimization": "Order the CASE branches so the NULL country needs no comparison of its own: the first branch tests the positive",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Status labels with unknown companies",
   "difficulty": "easy",
   "topics": [
    "conditional-logic",
    "left-join",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return every piece of equipment with its company's name and country and a status_label: 'Domestic Active' when the status is 'active' and the country is 'USA', 'Foreign Active' when the status is 'active' and the country is anything else (including unknown, when the company is missing), and 'Inactive' otherwise.",
   "sections": [
    {
     "title": "Tables",
     "body": "aerospace_df(id VARCHAR, name VARCHAR, type VARCHAR, status VARCHAR, company_id VARCHAR)\ncompany_df(id VARCHAR, name VARCHAR, country VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: id, equipment_name, equipment_type, equipment_status, company_name, country, status_label. One row per piece of equipment. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "country != 'USA' is NULL, not true, when the company is unknown, so a CASE falls through to 'Inactive'. Test the USA case first and let every other active row be foreign."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 33, \"Aerospace Equipment\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Active equipment of an unknown company is 'Foreign Active', as the statement's 'not USA' reads (ZillaCode's SQL gave 'Inactive', its pandas answer 'Foreign Active')."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-033-status-labels-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "unknown-company"
   ],
   "hints": [
    "LEFT JOIN company_df so equipment without a known company stays.",
    "CASE WHEN active AND country = 'USA' THEN ... WHEN active THEN 'Foreign Active' ELSE 'Inactive' END."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Order the CASE branches so the NULL country needs no comparison of its own: the first branch tests the positive USA condition, the second catches every other active row. `country IS DISTINCT FROM 'USA'` expresses the same.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "conditional-logic"
   },
   "related_associations": [
    "zillacode/33"
   ],
   "recommendation": {
    "rank": 33,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "id",
     "equipment_name",
     "equipment_type",
     "equipment_status",
     "company_name",
     "country",
     "status_label"
    ],
    "exact_schema": [
     "id",
     "equipment_name",
     "equipment_type",
     "equipment_status",
     "company_name",
     "country",
     "status_label"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "aerospace_df",
     "columns": {
      "id": "VARCHAR",
      "name": "VARCHAR",
      "type": "VARCHAR",
      "status": "VARCHAR",
      "company_id": "VARCHAR"
     },
     "sample_rows": [
      {
       "id": "A1",
       "name": "Falcon 9",
       "type": "Rocket",
       "status": "active",
       "company_id": "C1"
      },
      {
       "id": "A2",
       "name": "Starship",
       "type": "Rocket",
       "status": "active",
       "company_id": "C1"
      },
      {
       "id": "A3",
       "name": "Hubble",
       "type": "Telescope",
       "status": "active",
       "company_id": "C2"
      },
      {
       "id": "A4",
       "name": "Galileo",
       "type": "Satellite",
       "status": "inactive",
       "company_id": "C3"
      },
      {
       "id": "A5",
       "name": "Voyager 1",
       "type": "Probe",
       "status": "active",
       "company_id": "C3"
      }
     ]
    },
    {
     "name": "company_df",
     "columns": {
      "id": "VARCHAR",
      "name": "VARCHAR",
      "country": "VARCHAR"
     },
     "sample_rows": [
      {
       "id": "C1",
       "name": "SpaceX",
       "country": "USA"
      },
      {
       "id": "C2",
       "name": "NASA",
       "country": "USA"
      },
      {
       "id": "C3",
       "name": "European Space Agency",
       "country": "Europe"
      }
     ]
    }
   ],
   "output_schema": {
    "id": "value",
    "equipment_name": "value",
    "equipment_type": "value",
    "equipment_status": "value",
    "company_name": "value",
    "country": "value",
    "status_label": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 33 \"Aerospace Equipment\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-033",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Active equipment of an unknown company is 'Foreign Active', as the statement's 'not USA' reads (ZillaCode's SQL gave 'Inactive', its pandas answer 'Foreign Active').",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: id, equipment_name, equipment_type, equipment_status, company_name, country, status_label\nSELECT *\nFROM aerospace_df\n",
    "supported_operations": [
     "conditional-logic",
     "left-join",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: id, equipment_name, equipment_type, equipment_status, company_name, country, status_label\nSELECT *\nFROM aerospace_df\n",
    "supported_operations": [
     "conditional-logic",
     "left-join",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(aerospace_df, company_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return aerospace_df\n\n\ndisplay(etl(pd.DataFrame(aerospace_df), pd.DataFrame(company_df)))\n",
    "supported_operations": [
     "conditional-logic",
     "left-join",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(aerospace_df, company_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return aerospace_df\n\n\ndisplay(etl(pl.DataFrame(aerospace_df), pl.DataFrame(company_df)))\n",
    "supported_operations": [
     "conditional-logic",
     "left-join",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\naerospace_df = spark.table(\"aerospace_df\")\ncompany_df = spark.table(\"company_df\")\naerospace_df\n",
    "supported_operations": [
     "conditional-logic",
     "left-join",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'aerospace_df') }}\n",
    "supported_operations": [
     "conditional-logic",
     "left-join",
     "nulls"
    ],
    "model": "status_labels"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-034-interaction-log",
   "version": "1",
   "industry": "Web analytics",
   "learning_objectives": [
    "Three SELECTs with a literal interaction_type, glued with UNION ALL.",
    "Alias the time column to interaction_time in every branch."
   ],
   "optimization": "Align the three schemas by renaming and add the event type as a literal before appending with UNION ALL.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "One log of visits, likes and comments",
   "difficulty": "easy",
   "topics": [
    "union"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Combine visits, likes and comments into one interaction log: user_id, page_id, interaction_time (the event's timestamp, as ISO text) and interaction_type ('visit', 'like' or 'comment'). Every event is kept, including identical events.",
   "sections": [
    {
     "title": "Tables",
     "body": "page_visits(user_id VARCHAR, page_id VARCHAR, visit_time VARCHAR)\npage_likes(user_id VARCHAR, page_id VARCHAR, like_time VARCHAR)\npage_comments(user_id VARCHAR, page_id VARCHAR, comment_time VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: user_id, page_id, interaction_time, interaction_type. One row per event of any of the three tables. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "UNION would merge identical events; UNION ALL keeps them. Each branch renames its own time column."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 34, \"User Interactions\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Timestamps are ISO text, which sorts chronologically in every engine. Row order is not graded (ZillaCode's references sorted by time without the statement asking for it)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-034-interaction-log-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "identical-events"
   ],
   "hints": [
    "Three SELECTs with a literal interaction_type, glued with UNION ALL.",
    "Alias the time column to interaction_time in every branch."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Align the three schemas by renaming and add the event type as a literal before appending with UNION ALL.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "union"
   },
   "related_associations": [
    "zillacode/34"
   ],
   "recommendation": {
    "rank": 34,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "user_id",
     "page_id",
     "interaction_time",
     "interaction_type"
    ],
    "exact_schema": [
     "user_id",
     "page_id",
     "interaction_time",
     "interaction_type"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "page_visits",
     "columns": {
      "user_id": "VARCHAR",
      "page_id": "VARCHAR",
      "visit_time": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U1",
       "page_id": "P1",
       "visit_time": "2023-01-01 12:00:00"
      },
      {
       "user_id": "U2",
       "page_id": "P3",
       "visit_time": "2023-01-02 15:30:00"
      },
      {
       "user_id": "U3",
       "page_id": "P2",
       "visit_time": "2023-01-03 10:45:00"
      }
     ]
    },
    {
     "name": "page_likes",
     "columns": {
      "user_id": "VARCHAR",
      "page_id": "VARCHAR",
      "like_time": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U1",
       "page_id": "P2",
       "like_time": "2023-01-02 14:20:00"
      },
      {
       "user_id": "U2",
       "page_id": "P1",
       "like_time": "2023-01-03 16:40:00"
      },
      {
       "user_id": "U3",
       "page_id": "P3",
       "like_time": "2023-01-04 18:55:00"
      }
     ]
    },
    {
     "name": "page_comments",
     "columns": {
      "user_id": "VARCHAR",
      "page_id": "VARCHAR",
      "comment_time": "VARCHAR"
     },
     "sample_rows": [
      {
       "user_id": "U1",
       "page_id": "P3",
       "comment_time": "2023-01-03 13:00:00"
      },
      {
       "user_id": "U2",
       "page_id": "P2",
       "comment_time": "2023-01-04 17:10:00"
      },
      {
       "user_id": "U3",
       "page_id": "P1",
       "comment_time": "2023-01-05 19:25:00"
      }
     ]
    }
   ],
   "output_schema": {
    "user_id": "value",
    "page_id": "value",
    "interaction_time": "value",
    "interaction_type": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 34 \"User Interactions\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-034",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Direct UNION ALL avoids CTE/base-table name shadowing. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Timestamps are ISO text, which sorts chronologically in every engine. Row order is not graded (ZillaCode's references sorted by time without the statement asking for it).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: user_id, page_id, interaction_time, interaction_type\nSELECT *\nFROM page_visits\n",
    "supported_operations": [
     "union"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: user_id, page_id, interaction_time, interaction_type\nSELECT *\nFROM page_visits\n",
    "supported_operations": [
     "union"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(page_visits, page_likes, page_comments):\n    # Transform the input DataFrames into the result described in the brief.\n    return page_visits\n\n\ndisplay(etl(pd.DataFrame(page_visits), pd.DataFrame(page_likes), pd.DataFrame(page_comments)))\n",
    "supported_operations": [
     "union"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(page_visits, page_likes, page_comments):\n    # Transform the input DataFrames into the result described in the brief.\n    return page_visits\n\n\ndisplay(etl(pl.DataFrame(page_visits), pl.DataFrame(page_likes), pl.DataFrame(page_comments)))\n",
    "supported_operations": [
     "union"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'page_visits') }}\n",
    "supported_operations": [
     "union"
    ],
    "model": "interaction_log"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-035-daily-category-sales",
   "version": "1",
   "industry": "Outdoor retail",
   "learning_objectives": [
    "Join sales to products, then GROUP BY date, product_category.",
    "SUM(quantity_sold)."
   ],
   "optimization": "Join to find each sale's category and aggregate at the (date, category) grain.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Daily quantity per category",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return the total quantity sold for each product category on each day. Sales of unknown products are ignored.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_sales(sales_id VARCHAR, product_id VARCHAR, date DATE, quantity_sold INTEGER)\ndf_products(product_id VARCHAR, product_name VARCHAR, product_category VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: date, product_category, total_quantity. One row per (date, product_category) with sales. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Group by the category, not the product: two products of one category sold on the same day add up."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 35, \"Camping Supplies\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-035-daily-category-sales-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "same-category-same-day"
   ],
   "hints": [
    "Join sales to products, then GROUP BY date, product_category.",
    "SUM(quantity_sold)."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Join to find each sale's category and aggregate at the (date, category) grain.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/35"
   ],
   "recommendation": {
    "rank": 35,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "date",
     "product_category",
     "total_quantity"
    ],
    "exact_schema": [
     "date",
     "product_category",
     "total_quantity"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_sales",
     "columns": {
      "sales_id": "VARCHAR",
      "product_id": "VARCHAR",
      "date": "DATE",
      "quantity_sold": "INTEGER"
     },
     "sample_rows": [
      {
       "sales_id": "S1",
       "product_id": "P1",
       "date": "2023-06-01",
       "quantity_sold": 10
      },
      {
       "sales_id": "S2",
       "product_id": "P2",
       "date": "2023-06-02",
       "quantity_sold": 15
      },
      {
       "sales_id": "S3",
       "product_id": "P3",
       "date": "2023-06-02",
       "quantity_sold": 20
      },
      {
       "sales_id": "S4",
       "product_id": "P4",
       "date": "2023-06-01",
       "quantity_sold": 12
      },
      {
       "sales_id": "S5",
       "product_id": "P5",
       "date": "2023-06-03",
       "quantity_sold": 25
      }
     ]
    },
    {
     "name": "df_products",
     "columns": {
      "product_id": "VARCHAR",
      "product_name": "VARCHAR",
      "product_category": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": "P1",
       "product_name": "Camping Tent",
       "product_category": "Camping"
      },
      {
       "product_id": "P2",
       "product_name": "Hiking Shoes",
       "product_category": "Hiking"
      },
      {
       "product_id": "P3",
       "product_name": "Fishing Rod",
       "product_category": "Fishing"
      },
      {
       "product_id": "P4",
       "product_name": "Insulated Bottle",
       "product_category": "Hiking"
      },
      {
       "product_id": "P5",
       "product_name": "Outdoor Grill",
       "product_category": "Camping"
      }
     ]
    }
   ],
   "output_schema": {
    "date": "value",
    "product_category": "value",
    "total_quantity": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 35 \"Camping Supplies\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-035",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: date, product_category, total_quantity\nSELECT *\nFROM df_sales\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: date, product_category, total_quantity\nSELECT *\nFROM df_sales\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_sales, df_products):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_sales\n\n\ndisplay(etl(pd.DataFrame(df_sales), pd.DataFrame(df_products)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_sales, df_products):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_sales\n\n\ndisplay(etl(pl.DataFrame(df_sales), pl.DataFrame(df_products)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf_sales = spark.table(\"df_sales\")\ndf_products = spark.table(\"df_products\")\ndf_sales\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df_sales') }}\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ],
    "model": "daily_category_sales"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-036-vc-above-limit",
   "version": "1",
   "industry": "Venture capital",
   "learning_objectives": [
    "AVG(funding) per vc_id, join the investors, keep avg_funding > funding_limit.",
    "Or GROUP BY the investor columns and use HAVING."
   ],
   "optimization": "Aggregate per investor, then filter on the aggregate (HAVING, or a WHERE on the aggregated subquery).",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Investors above their funding limit",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "having",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Each venture capitalist has a funding limit. Return the investors whose funded start-ups have an average funding strictly above their limit, with that average. A NULL funding amount is not part of the average.",
   "sections": [
    {
     "title": "Tables",
     "body": "venture_capitalist_df(vc_id VARCHAR, vc_name VARCHAR, funding_limit DOUBLE)\nfunded_startups_df(startup_id VARCHAR, startup_name VARCHAR, vc_id VARCHAR, funding DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: vc_id, vc_name, avg_funding. One row per investor whose average funding is above their limit. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "The limit differs per investor, so compare after aggregating; an average equal to the limit is not above it."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 36, \"Funded Startups\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The output keeps the statement's columns (ZillaCode's reference also returned funding_limit)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-036-vc-above-limit-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "equal-to-limit"
   ],
   "hints": [
    "AVG(funding) per vc_id, join the investors, keep avg_funding > funding_limit.",
    "Or GROUP BY the investor columns and use HAVING."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Aggregate per investor, then filter on the aggregate (HAVING, or a WHERE on the aggregated subquery).",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/36"
   ],
   "recommendation": {
    "rank": 36,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "vc_id",
     "vc_name",
     "avg_funding"
    ],
    "exact_schema": [
     "vc_id",
     "vc_name",
     "avg_funding"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "venture_capitalist_df",
     "columns": {
      "vc_id": "VARCHAR",
      "vc_name": "VARCHAR",
      "funding_limit": "DOUBLE"
     },
     "sample_rows": [
      {
       "vc_id": "VC1",
       "vc_name": "VC Firm 1",
       "funding_limit": 1.5
      },
      {
       "vc_id": "VC2",
       "vc_name": "VC Firm 2",
       "funding_limit": 2.0
      },
      {
       "vc_id": "VC3",
       "vc_name": "VC Firm 3",
       "funding_limit": 1.75
      },
      {
       "vc_id": "VC4",
       "vc_name": "VC Firm 4",
       "funding_limit": 2.5
      }
     ]
    },
    {
     "name": "funded_startups_df",
     "columns": {
      "startup_id": "VARCHAR",
      "startup_name": "VARCHAR",
      "vc_id": "VARCHAR",
      "funding": "DOUBLE"
     },
     "sample_rows": [
      {
       "startup_id": "S1",
       "startup_name": "Startup 1",
       "vc_id": "VC1",
       "funding": 2.0
      },
      {
       "startup_id": "S2",
       "startup_name": "Startup 2",
       "vc_id": "VC1",
       "funding": 1.0
      },
      {
       "startup_id": "S3",
       "startup_name": "Startup 3",
       "vc_id": "VC2",
       "funding": 2.5
      },
      {
       "startup_id": "S4",
       "startup_name": "Startup 4",
       "vc_id": "VC2",
       "funding": 2.0
      },
      {
       "startup_id": "S5",
       "startup_name": "Startup 5",
       "vc_id": "VC3",
       "funding": 1.8
      },
      {
       "startup_id": "S6",
       "startup_name": "Startup 6",
       "vc_id": "VC3",
       "funding": 1.7
      },
      {
       "startup_id": "S7",
       "startup_name": "Startup 7",
       "vc_id": "VC4",
       "funding": 3.0
      },
      {
       "startup_id": "S8",
       "startup_name": "Startup 8",
       "vc_id": "VC4",
       "funding": 2.0
      }
     ]
    }
   ],
   "output_schema": {
    "vc_id": "value",
    "vc_name": "value",
    "avg_funding": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 36 \"Funded Startups\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-036",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The output keeps the statement's columns (ZillaCode's reference also returned funding_limit).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: vc_id, vc_name, avg_funding\nSELECT *\nFROM venture_capitalist_df\n",
    "supported_operations": [
     "aggregation",
     "having",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: vc_id, vc_name, avg_funding\nSELECT *\nFROM venture_capitalist_df\n",
    "supported_operations": [
     "aggregation",
     "having",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(venture_capitalist_df, funded_startups_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return venture_capitalist_df\n\n\ndisplay(etl(pd.DataFrame(venture_capitalist_df), pd.DataFrame(funded_startups_df)))\n",
    "supported_operations": [
     "aggregation",
     "having",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(venture_capitalist_df, funded_startups_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return venture_capitalist_df\n\n\ndisplay(etl(pl.DataFrame(venture_capitalist_df), pl.DataFrame(funded_startups_df)))\n",
    "supported_operations": [
     "aggregation",
     "having",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nventure_capitalist_df = spark.table(\"venture_capitalist_df\")\nfunded_startups_df = spark.table(\"funded_startups_df\")\nventure_capitalist_df\n",
    "supported_operations": [
     "aggregation",
     "having",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'venture_capitalist_df') }}\n",
    "supported_operations": [
     "aggregation",
     "having",
     "joins"
    ],
    "model": "vc_above_limit"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-037-latest-maintenance",
   "version": "1",
   "industry": "Pharmaceuticals",
   "learning_objectives": [
    "MAX(maintenance_date) and DENSE_RANK() over PARTITION BY equipment_id, in the same pass.",
    "Keep the rows on the latest date, then take MIN(rank) per equipment."
   ],
   "optimization": "Window functions compute both facts over the equipment's full history; filtering to the latest date must come",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Latest maintenance and its cost rank",
   "difficulty": "hard",
   "topics": [
    "window-functions",
    "ranking",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each piece of equipment with at least one maintenance record, return its latest maintenance date and maintenance_cost_rank: rank all of that equipment's maintenance records by cost, highest first, with a dense rank (a NULL cost ranks last), and report the rank of the latest record. When several records share the latest date, report the best (smallest) of their ranks. Maintenance of unknown equipment is ignored.",
   "sections": [
    {
     "title": "Tables",
     "body": "df1(equipment_id VARCHAR, equipment_name VARCHAR, purchase_date DATE)\ndf2(equipment_id VARCHAR, maintenance_date DATE, maintenance_cost DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: equipment_id, equipment_name, purchase_date, latest_maintenance_date, maintenance_cost_rank. One row per equipment with at least one maintenance record. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Rank within each piece of equipment (PARTITION BY equipment_id) and before keeping the latest records. Snowflake ranks a NULL cost first in a descending order: write NULLS LAST."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 37, \"Pharmaceutical Equipment\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The rank is per equipment, as CodeDELeet's correction established (ZillaCode's pandas answer hard-coded 1). Several records on the latest date give one row with the best rank; a NULL cost ranks last."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-037-latest-maintenance-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "tie-and-null-cost"
   ],
   "hints": [
    "MAX(maintenance_date) and DENSE_RANK() over PARTITION BY equipment_id, in the same pass.",
    "Keep the rows on the latest date, then take MIN(rank) per equipment."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Window functions compute both facts over the equipment's full history; filtering to the latest date must come after ranking, or every record would rank 1. CodeDELeet clarified this contract from ZillaCode's examples, where ranks are per equipment rather than global.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/37"
   ],
   "recommendation": {
    "rank": 37,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "equipment_id",
     "equipment_name",
     "purchase_date",
     "latest_maintenance_date",
     "maintenance_cost_rank"
    ],
    "exact_schema": [
     "equipment_id",
     "equipment_name",
     "purchase_date",
     "latest_maintenance_date",
     "maintenance_cost_rank"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df1",
     "columns": {
      "equipment_id": "VARCHAR",
      "equipment_name": "VARCHAR",
      "purchase_date": "DATE"
     },
     "sample_rows": [
      {
       "equipment_id": "EQ001",
       "equipment_name": "Mixer",
       "purchase_date": "2020-01-01"
      },
      {
       "equipment_id": "EQ002",
       "equipment_name": "Centrifuge",
       "purchase_date": "2020-02-01"
      },
      {
       "equipment_id": "EQ003",
       "equipment_name": "Pipette",
       "purchase_date": "2020-03-01"
      }
     ]
    },
    {
     "name": "df2",
     "columns": {
      "equipment_id": "VARCHAR",
      "maintenance_date": "DATE",
      "maintenance_cost": "DOUBLE"
     },
     "sample_rows": [
      {
       "equipment_id": "EQ001",
       "maintenance_date": "2021-06-01",
       "maintenance_cost": 500.0
      },
      {
       "equipment_id": "EQ002",
       "maintenance_date": "2021-07-01",
       "maintenance_cost": 400.0
      },
      {
       "equipment_id": "EQ001",
       "maintenance_date": "2021-07-02",
       "maintenance_cost": 600.0
      }
     ]
    }
   ],
   "output_schema": {
    "equipment_id": "value",
    "equipment_name": "value",
    "purchase_date": "value",
    "latest_maintenance_date": "value",
    "maintenance_cost_rank": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 37 \"Pharmaceutical Equipment\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-037",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Pandas and SQL now calculate per-equipment dense maintenance-cost rank, matching Spark instead of hard-coding 1. Wording clarified: ranks are within equipment, not global. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The rank is per equipment, as CodeDELeet's correction established (ZillaCode's pandas answer hard-coded 1). Several records on the latest date give one row with the best rank; a NULL cost ranks last.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: equipment_id, equipment_name, purchase_date, latest_maintenance_date, maintenance_cost_rank\nSELECT *\nFROM df1\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: equipment_id, equipment_name, purchase_date, latest_maintenance_date, maintenance_cost_rank\nSELECT *\nFROM df1\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df1, df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return df1\n\n\ndisplay(etl(pd.DataFrame(df1), pd.DataFrame(df2)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df1, df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return df1\n\n\ndisplay(etl(pl.DataFrame(df1), pl.DataFrame(df2)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df1') }}\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "nulls"
    ],
    "model": "latest_maintenance"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-038-cross-join",
   "version": "1",
   "industry": "Banking",
   "learning_objectives": [
    "CROSS JOIN (pandas merge(how='cross'), Polars join(how='cross')).",
    "Drop or rename the transaction's cust_id before combining."
   ],
   "optimization": "A cross join produces n x m rows without a condition; the duplicate column name must be resolved explicitly.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Every transaction with every customer",
   "difficulty": "easy",
   "topics": [
    "cross-join"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For an analysis grid, pair every transaction with every customer (a cross join). Return the transaction's columns followed by the customer's; the cust_id column is the customer's, not the transaction's.",
   "sections": [
    {
     "title": "Tables",
     "body": "transactions(trans_id INTEGER, trans_amt DOUBLE, date VARCHAR, cust_id INTEGER)\ncustomers(cust_id INTEGER, first_name VARCHAR, last_name VARCHAR, age INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: trans_id, trans_amt, date, cust_id, first_name, last_name, age. One row per (transaction, customer) pair. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Both tables have cust_id: select the customer's one explicitly. Joining on cust_id is not a cross join."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 38, \"Customer Cross Join\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The customer-side cust_id is kept explicitly (CodeDELeet's correction of duplicate keys)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-038-cross-join-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "duplicate-customer-row"
   ],
   "hints": [
    "CROSS JOIN (pandas merge(how='cross'), Polars join(how='cross')).",
    "Drop or rename the transaction's cust_id before combining."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A cross join produces n x m rows without a condition; the duplicate column name must be resolved explicitly.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "cross-join"
   },
   "related_associations": [
    "zillacode/38"
   ],
   "recommendation": {
    "rank": 38,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "trans_id",
     "trans_amt",
     "date",
     "cust_id",
     "first_name",
     "last_name",
     "age"
    ],
    "exact_schema": [
     "trans_id",
     "trans_amt",
     "date",
     "cust_id",
     "first_name",
     "last_name",
     "age"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "transactions",
     "columns": {
      "trans_id": "INTEGER",
      "trans_amt": "DOUBLE",
      "date": "VARCHAR",
      "cust_id": "INTEGER"
     },
     "sample_rows": [
      {
       "trans_id": 1,
       "trans_amt": 500.0,
       "date": "2023-07-01",
       "cust_id": 1001
      },
      {
       "trans_id": 2,
       "trans_amt": 200.0,
       "date": "2023-07-02",
       "cust_id": 1002
      },
      {
       "trans_id": 3,
       "trans_amt": 300.0,
       "date": "2023-07-03",
       "cust_id": 1003
      }
     ]
    },
    {
     "name": "customers",
     "columns": {
      "cust_id": "INTEGER",
      "first_name": "VARCHAR",
      "last_name": "VARCHAR",
      "age": "INTEGER"
     },
     "sample_rows": [
      {
       "cust_id": 1001,
       "first_name": "John",
       "last_name": "Doe",
       "age": 30
      },
      {
       "cust_id": 1002,
       "first_name": "Jane",
       "last_name": "Smith",
       "age": 40
      },
      {
       "cust_id": 1003,
       "first_name": "Bob",
       "last_name": "Johnson",
       "age": 50
      }
     ]
    }
   ],
   "output_schema": {
    "trans_id": "value",
    "trans_amt": "value",
    "date": "value",
    "cust_id": "value",
    "first_name": "value",
    "last_name": "value",
    "age": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 38 \"Customer Cross Join\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-038",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicitly drop transaction-side cust_id before cross join, matching the source output instead of silently overwriting duplicate JSON keys. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The customer-side cust_id is kept explicitly (CodeDELeet's correction of duplicate keys).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: trans_id, trans_amt, date, cust_id, first_name, last_name, age\nSELECT *\nFROM transactions\n",
    "supported_operations": [
     "cross-join"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: trans_id, trans_amt, date, cust_id, first_name, last_name, age\nSELECT *\nFROM transactions\n",
    "supported_operations": [
     "cross-join"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(transactions, customers):\n    # Transform the input DataFrames into the result described in the brief.\n    return transactions\n\n\ndisplay(etl(pd.DataFrame(transactions), pd.DataFrame(customers)))\n",
    "supported_operations": [
     "cross-join"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(transactions, customers):\n    # Transform the input DataFrames into the result described in the brief.\n    return transactions\n\n\ndisplay(etl(pl.DataFrame(transactions), pl.DataFrame(customers)))\n",
    "supported_operations": [
     "cross-join"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'transactions') }}\n",
    "supported_operations": [
     "cross-join"
    ],
    "model": "cross_join"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-039-self-interactions",
   "version": "1",
   "industry": "Social media",
   "learning_objectives": [
    "WHERE user1_id = user2_id, then GROUP BY user1_id.",
    "Alias user1_id to user_id."
   ],
   "optimization": "A filter comparing two columns of the same row, then a count per user. Equality with NULL is unknown, so rows with a missing user drop out.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Users who interact with themselves",
   "difficulty": "easy",
   "topics": [
    "filtering",
    "aggregation",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "An interaction links user1_id to user2_id. A self-interaction has the same user on both sides. Return each user with at least one self-interaction and their number of self-interactions. An interaction with a missing user is not a self-interaction.",
   "sections": [
    {
     "title": "Tables",
     "body": "input_df(interaction_id INTEGER, user1_id INTEGER, user2_id INTEGER, interaction_type VARCHAR, timestamp VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: user_id, self_interaction_count. One row per user with at least one self-interaction. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "NULL = NULL is not true: two missing users are not the same user (IS NOT DISTINCT FROM would count them). pandas and Polars agree with SQL here."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 39, \"User Interactions\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The output column is user_id, as the statement says (ZillaCode's reference kept user1_id)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-039-self-interactions-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "missing-users"
   ],
   "hints": [
    "WHERE user1_id = user2_id, then GROUP BY user1_id.",
    "Alias user1_id to user_id."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A filter comparing two columns of the same row, then a count per user. Equality with NULL is unknown, so rows with a missing user drop out.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "filtering"
   },
   "related_associations": [
    "zillacode/39"
   ],
   "recommendation": {
    "rank": 39,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "user_id",
     "self_interaction_count"
    ],
    "exact_schema": [
     "user_id",
     "self_interaction_count"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "input_df",
     "columns": {
      "interaction_id": "INTEGER",
      "user1_id": "INTEGER",
      "user2_id": "INTEGER",
      "interaction_type": "VARCHAR",
      "timestamp": "VARCHAR"
     },
     "sample_rows": [
      {
       "interaction_id": 1,
       "user1_id": 1001,
       "user2_id": 2002,
       "interaction_type": "like",
       "timestamp": "2023-01-01 10:00:00"
      },
      {
       "interaction_id": 2,
       "user1_id": 1002,
       "user2_id": 1002,
       "interaction_type": "comment",
       "timestamp": "2023-01-01 11:00:00"
      },
      {
       "interaction_id": 3,
       "user1_id": 1003,
       "user2_id": 2003,
       "interaction_type": "share",
       "timestamp": "2023-01-02 10:00:00"
      },
      {
       "interaction_id": 4,
       "user1_id": 1004,
       "user2_id": 1004,
       "interaction_type": "like",
       "timestamp": "2023-01-02 11:00:00"
      },
      {
       "interaction_id": 5,
       "user1_id": 1005,
       "user2_id": 2005,
       "interaction_type": "comment",
       "timestamp": "2023-01-03 10:00:00"
      }
     ]
    }
   ],
   "output_schema": {
    "user_id": "value",
    "self_interaction_count": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 39 \"User Interactions\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-039",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The output column is user_id, as the statement says (ZillaCode's reference kept user1_id).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: user_id, self_interaction_count\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "filtering",
     "aggregation",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: user_id, self_interaction_count\nSELECT *\nFROM input_df\n",
    "supported_operations": [
     "filtering",
     "aggregation",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pd.DataFrame(input_df)))\n",
    "supported_operations": [
     "filtering",
     "aggregation",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(input_df):\n    # Transform the input DataFrames into the result described in the brief.\n    return input_df\n\n\ndisplay(etl(pl.DataFrame(input_df)))\n",
    "supported_operations": [
     "filtering",
     "aggregation",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ninput_df = spark.table(\"input_df\")\ninput_df\n",
    "supported_operations": [
     "filtering",
     "aggregation",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'input_df') }}\n",
    "supported_operations": [
     "filtering",
     "aggregation",
     "nulls"
    ],
    "model": "self_interactions"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-040-count-list-items",
   "version": "1",
   "industry": "Background checks",
   "learning_objectives": [
    "LENGTH(x) - LENGTH(REPLACE(x, ',', '')) + 1 counts the items.",
    "Handle NULL and '' first."
   ],
   "optimization": "Counting separators works in every engine (Snowflake's subset has no arrays): the item count is the number of",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Count items in comma-separated fields",
   "difficulty": "easy",
   "topics": [
    "strings",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Four text fields hold comma-separated lists (crimes, jobs, degrees, addresses). Return each check with the number of items in each list: the number of commas plus one, or 0 when the field is NULL or empty.",
   "sections": [
    {
     "title": "Tables",
     "body": "background_checks(check_id INTEGER, full_name VARCHAR, dob DATE, criminal_record VARCHAR, employment_history VARCHAR, education_history VARCHAR, address VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: check_id, full_name, dob, crime_count, jobs_count, degrees_count, places_lived_count. One row per check. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Splitting '' gives one empty item and splitting NULL gives NULL: both must count as 0."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 40, \"Background Checks\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: NULL and empty fields count 0 items explicitly (ZillaCode's references differed on them)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-040-count-list-items-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "null-and-empty"
   ],
   "hints": [
    "LENGTH(x) - LENGTH(REPLACE(x, ',', '')) + 1 counts the items.",
    "Handle NULL and '' first."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Counting separators works in every engine (Snowflake's subset has no arrays): the item count is the number of commas plus one, with NULL and the empty string defined as 0 items.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "strings"
   },
   "related_associations": [
    "zillacode/40"
   ],
   "recommendation": {
    "rank": 40,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "check_id",
     "full_name",
     "dob",
     "crime_count",
     "jobs_count",
     "degrees_count",
     "places_lived_count"
    ],
    "exact_schema": [
     "check_id",
     "full_name",
     "dob",
     "crime_count",
     "jobs_count",
     "degrees_count",
     "places_lived_count"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "background_checks",
     "columns": {
      "check_id": "INTEGER",
      "full_name": "VARCHAR",
      "dob": "DATE",
      "criminal_record": "VARCHAR",
      "employment_history": "VARCHAR",
      "education_history": "VARCHAR",
      "address": "VARCHAR"
     },
     "sample_rows": [
      {
       "check_id": 1,
       "full_name": "John Doe",
       "dob": "1980-05-05",
       "criminal_record": "Theft,Assault",
       "employment_history": "ABC Corp,XYZ Corp",
       "education_history": "B.A. in English,M.A. in English",
       "address": "123 Main St,456 Pine St"
      },
      {
       "check_id": 2,
       "full_name": "Jane Smith",
       "dob": "1982-07-12",
       "criminal_record": "DUI",
       "employment_history": "DEF Corp,GHI Corp,JKL Corp",
       "education_history": "B.Sc. in Physics",
       "address": "789 Oak St"
      },
      {
       "check_id": 3,
       "full_name": "Bob Johnson",
       "dob": "1975-10-20",
       "criminal_record": "Theft,Fraud,Embezzlement",
       "employment_history": "MNO Corp",
       "education_history": "B.A. in History,M.A. in History",
       "address": "321 Cedar St,654 Elm St"
      }
     ]
    }
   ],
   "output_schema": {
    "check_id": "value",
    "full_name": "value",
    "dob": "value",
    "crime_count": "value",
    "jobs_count": "value",
    "degrees_count": "value",
    "places_lived_count": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 40 \"Background Checks\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-040",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "NULL and empty fields count 0 items explicitly (ZillaCode's references differed on them).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: check_id, full_name, dob, crime_count, jobs_count, degrees_count, places_lived_count\nSELECT *\nFROM background_checks\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: check_id, full_name, dob, crime_count, jobs_count, degrees_count, places_lived_count\nSELECT *\nFROM background_checks\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(background_checks):\n    # Transform the input DataFrames into the result described in the brief.\n    return background_checks\n\n\ndisplay(etl(pd.DataFrame(background_checks)))\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(background_checks):\n    # Transform the input DataFrames into the result described in the brief.\n    return background_checks\n\n\ndisplay(etl(pl.DataFrame(background_checks)))\n",
    "supported_operations": [
     "strings",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'background_checks') }}\n",
    "supported_operations": [
     "strings",
     "nulls"
    ],
    "model": "count_list_items"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-041-height-per-floor",
   "version": "1",
   "industry": "Architecture",
   "learning_objectives": [
    "CASE WHEN floors = 0 THEN 0 ELSE ROUND(height_m / floors, 2) END.",
    "IFF(floors = 0, 0, ...) in Snowflake."
   ],
   "optimization": "Guard the division with the zero case only; NULL then propagates through the division to NULL. Snowflake",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Average height per floor",
   "difficulty": "easy",
   "topics": [
    "arithmetic",
    "conditional-logic",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each building, return the average height per floor (height_m divided by floors) rounded to two decimals. A building with 0 floors gets 0; a building whose number of floors is unknown (NULL) gets NULL.",
   "sections": [
    {
     "title": "Tables",
     "body": "buildings(id INTEGER, name VARCHAR, city VARCHAR, country VARCHAR, height_m DOUBLE, floors INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: id, name, city, country, avg_height_per_floor. One row per building. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "CASE WHEN floors != 0 ... ELSE 0 also sends NULL floors to 0, because the comparison is unknown. Test floors = 0 instead. Snowflake raises an error on division by zero, so the zero case must not divide."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 41, \"Architectural Points of Interest\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: A NULL number of floors gives NULL (ZillaCode's references gave 0 or NULL depending on the engine)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-041-height-per-floor-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "zero-and-unknown-floors"
   ],
   "hints": [
    "CASE WHEN floors = 0 THEN 0 ELSE ROUND(height_m / floors, 2) END.",
    "IFF(floors = 0, 0, ...) in Snowflake."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Guard the division with the zero case only; NULL then propagates through the division to NULL. Snowflake evaluates the guarded branch lazily, so no 'Division by zero' error is raised.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "arithmetic"
   },
   "related_associations": [
    "zillacode/41"
   ],
   "recommendation": {
    "rank": 41,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "id",
     "name",
     "city",
     "country",
     "avg_height_per_floor"
    ],
    "exact_schema": [
     "id",
     "name",
     "city",
     "country",
     "avg_height_per_floor"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "buildings",
     "columns": {
      "id": "INTEGER",
      "name": "VARCHAR",
      "city": "VARCHAR",
      "country": "VARCHAR",
      "height_m": "DOUBLE",
      "floors": "INTEGER"
     },
     "sample_rows": [
      {
       "id": 1,
       "name": "One World Trade Center",
       "city": "New York",
       "country": "USA",
       "height_m": 541.3,
       "floors": 104
      },
      {
       "id": 2,
       "name": "Willis Tower",
       "city": "Chicago",
       "country": "USA",
       "height_m": 442.1,
       "floors": 108
      },
      {
       "id": 3,
       "name": "Burj Khalifa",
       "city": "Dubai",
       "country": "UAE",
       "height_m": 828.0,
       "floors": 163
      },
      {
       "id": 4,
       "name": "The Shard",
       "city": "London",
       "country": "UK",
       "height_m": 309.6,
       "floors": 72
      },
      {
       "id": 5,
       "name": "Abraj Al-Bait Clock Tower",
       "city": "Mecca",
       "country": "Saudi Arabia",
       "height_m": 601.0,
       "floors": 120
      }
     ]
    }
   ],
   "output_schema": {
    "id": "value",
    "name": "value",
    "city": "value",
    "country": "value",
    "avg_height_per_floor": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 41 \"Architectural Points of Interest\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-041",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "A NULL number of floors gives NULL (ZillaCode's references gave 0 or NULL depending on the engine).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: id, name, city, country, avg_height_per_floor\nSELECT *\nFROM buildings\n",
    "supported_operations": [
     "arithmetic",
     "conditional-logic",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: id, name, city, country, avg_height_per_floor\nSELECT *\nFROM buildings\n",
    "supported_operations": [
     "arithmetic",
     "conditional-logic",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(buildings):\n    # Transform the input DataFrames into the result described in the brief.\n    return buildings\n\n\ndisplay(etl(pd.DataFrame(buildings)))\n",
    "supported_operations": [
     "arithmetic",
     "conditional-logic",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(buildings):\n    # Transform the input DataFrames into the result described in the brief.\n    return buildings\n\n\ndisplay(etl(pl.DataFrame(buildings)))\n",
    "supported_operations": [
     "arithmetic",
     "conditional-logic",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'buildings') }}\n",
    "supported_operations": [
     "arithmetic",
     "conditional-logic",
     "nulls"
    ],
    "model": "height_per_floor"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-042-species-by-climate",
   "version": "1",
   "industry": "Zoology",
   "learning_objectives": [
    "Join animals to regions, GROUP BY species, climate.",
    "FLOOR(AVG(weight)) then cast to an integer."
   ],
   "optimization": "Join to derive the climate, aggregate at the (species, climate) grain, and truncate the weight average.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Species statistics per climate",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each species in each climate, return the average age, the average weight truncated to a whole number, and the number of animals. An animal's climate comes from its region; animals of regions without a climate are left out. A missing weight is not part of the average.",
   "sections": [
    {
     "title": "Tables",
     "body": "animal_data(id VARCHAR, species VARCHAR, age INTEGER, weight DOUBLE, region VARCHAR)\nregion_data(region VARCHAR, climate VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: species, climate, avg_age, avg_weight, total_animals. One row per (species, climate). Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Truncating is not rounding: 205.7 becomes 205. Group by species and climate, not species alone."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 42, \"Zoology\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Tables and columns are snake_case (ZillaCode used AnimalData, AvgWeight, ...)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-042-species-by-climate-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "two-climates-and-null-weight"
   ],
   "hints": [
    "Join animals to regions, GROUP BY species, climate.",
    "FLOOR(AVG(weight)) then cast to an integer."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Join to derive the climate, aggregate at the (species, climate) grain, and truncate the weight average.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/42"
   ],
   "recommendation": {
    "rank": 42,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "species",
     "climate",
     "avg_age",
     "avg_weight",
     "total_animals"
    ],
    "exact_schema": [
     "species",
     "climate",
     "avg_age",
     "avg_weight",
     "total_animals"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "animal_data",
     "columns": {
      "id": "VARCHAR",
      "species": "VARCHAR",
      "age": "INTEGER",
      "weight": "DOUBLE",
      "region": "VARCHAR"
     },
     "sample_rows": [
      {
       "id": "A1",
       "species": "Lion",
       "age": 10,
       "weight": 200.5,
       "region": "Africa"
      },
      {
       "id": "A2",
       "species": "Tiger",
       "age": 5,
       "weight": 150.3,
       "region": "Asia"
      },
      {
       "id": "A3",
       "species": "Bear",
       "age": 7,
       "weight": 180.2,
       "region": "North America"
      },
      {
       "id": "A4",
       "species": "Lion",
       "age": 12,
       "weight": 205.7,
       "region": "Africa"
      },
      {
       "id": "A5",
       "species": "Tiger",
       "age": 6,
       "weight": 155.1,
       "region": "Asia"
      }
     ]
    },
    {
     "name": "region_data",
     "columns": {
      "region": "VARCHAR",
      "climate": "VARCHAR"
     },
     "sample_rows": [
      {
       "region": "Africa",
       "climate": "Hot"
      },
      {
       "region": "Asia",
       "climate": "Temperate"
      },
      {
       "region": "North America",
       "climate": "Cold"
      }
     ]
    }
   ],
   "output_schema": {
    "species": "value",
    "climate": "value",
    "avg_age": "value",
    "avg_weight": "value",
    "total_animals": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 42 \"Zoology\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-042",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Tables and columns are snake_case (ZillaCode used AnimalData, AvgWeight, ...).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: species, climate, avg_age, avg_weight, total_animals\nSELECT *\nFROM animal_data\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: species, climate, avg_age, avg_weight, total_animals\nSELECT *\nFROM animal_data\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(animal_data, region_data):\n    # Transform the input DataFrames into the result described in the brief.\n    return animal_data\n\n\ndisplay(etl(pd.DataFrame(animal_data), pd.DataFrame(region_data)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(animal_data, region_data):\n    # Transform the input DataFrames into the result described in the brief.\n    return animal_data\n\n\ndisplay(etl(pl.DataFrame(animal_data), pl.DataFrame(region_data)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'animal_data') }}\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ],
    "model": "species_by_climate"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-043-top-observations",
   "version": "1",
   "industry": "Herpetology",
   "learning_objectives": [
    "RANK() OVER (ORDER BY count DESC) <= 3.",
    "Sort the final result by count DESC, obs_id."
   ],
   "optimization": "Top-N with ties is a ranking problem: RANK gives tied rows the same rank, so every observation tied with the",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Top three observations with ties",
   "difficulty": "medium",
   "topics": [
    "window-functions",
    "ranking",
    "ordering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Join the observations to their species and return the three largest observations by count. Observations tied with the third one are all returned (a rank, not a row limit). Sort by count descending, then by obs_id.",
   "sections": [
    {
     "title": "Tables",
     "body": "observations(obs_id INTEGER, species_id INTEGER, location_id INTEGER, count INTEGER)\nspecies(species_id INTEGER, species_name VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`."
    },
    {
     "title": "Output",
     "body": "Columns in this order: obs_id, species_id, species_name, location_id, count. One row per observation ranked 3 or better. Row order is graded: count descending, then obs_id ascending."
    },
    {
     "title": "Common pitfall",
     "body": "LIMIT 3 (or head(3)) cuts tied observations arbitrarily; RANK() <= 3 keeps every tie."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 43, \"Herpetology\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Output columns follow the statement; the order is graded with obs_id breaking ties."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-043-top-observations-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "tie-at-third"
   ],
   "hints": [
    "RANK() OVER (ORDER BY count DESC) <= 3.",
    "Sort the final result by count DESC, obs_id."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Top-N with ties is a ranking problem: RANK gives tied rows the same rank, so every observation tied with the third one passes the filter. ORDER BY then fixes the output order.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "window-functions"
   },
   "related_associations": [
    "zillacode/43"
   ],
   "recommendation": {
    "rank": 43,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "obs_id",
     "species_id",
     "species_name",
     "location_id",
     "count"
    ],
    "exact_schema": [
     "obs_id",
     "species_id",
     "species_name",
     "location_id",
     "count"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "observations",
     "columns": {
      "obs_id": "INTEGER",
      "species_id": "INTEGER",
      "location_id": "INTEGER",
      "count": "INTEGER"
     },
     "sample_rows": [
      {
       "obs_id": 1,
       "species_id": 100,
       "location_id": 1,
       "count": 55
      },
      {
       "obs_id": 2,
       "species_id": 101,
       "location_id": 2,
       "count": 35
      },
      {
       "obs_id": 3,
       "species_id": 100,
       "location_id": 1,
       "count": 45
      }
     ]
    },
    {
     "name": "species",
     "columns": {
      "species_id": "INTEGER",
      "species_name": "VARCHAR"
     },
     "sample_rows": [
      {
       "species_id": 100,
       "species_name": "Python"
      },
      {
       "species_id": 101,
       "species_name": "Gecko"
      },
      {
       "species_id": 102,
       "species_name": "Frog"
      }
     ]
    }
   ],
   "output_schema": {
    "obs_id": "value",
    "species_id": "value",
    "species_name": "value",
    "location_id": "value",
    "count": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 43 \"Herpetology\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-043",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Output columns follow the statement; the order is graded with obs_id breaking ties.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: obs_id, species_id, species_name, location_id, count\nSELECT *\nFROM observations\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "ordering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: obs_id, species_id, species_name, location_id, count\nSELECT *\nFROM observations\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "ordering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(observations, species):\n    # Transform the input DataFrames into the result described in the brief.\n    return observations\n\n\ndisplay(etl(pd.DataFrame(observations), pd.DataFrame(species)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "ordering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(observations, species):\n    # Transform the input DataFrames into the result described in the brief.\n    return observations\n\n\ndisplay(etl(pl.DataFrame(observations), pl.DataFrame(species)))\n",
    "supported_operations": [
     "window-functions",
     "ranking",
     "ordering"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-044-portfolio-value",
   "version": "1",
   "industry": "Private equity",
   "learning_objectives": [
    "Join on company, then SUM(shares * closing_price) GROUP BY pe_firm, date."
   ],
   "optimization": "Join holdings to the day's prices and aggregate the per-holding value at the (firm, date) grain.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Daily portfolio value per firm",
   "difficulty": "easy",
   "topics": [
    "aggregation",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "A private equity firm holds shares of companies; prices gives each company's closing price per day. Return each firm's portfolio value per day: the sum over its holdings of shares times the closing price that day. A holding without a price on a day adds nothing that day.",
   "sections": [
    {
     "title": "Tables",
     "body": "portfolio(pe_firm VARCHAR, company VARCHAR, shares INTEGER)\nprices(date DATE, company VARCHAR, closing_price DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: pe_firm, date, portfolio_value. One row per (firm, date) with at least one priced holding. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Multiply per holding before summing: SUM(shares) * SUM(price) mixes companies."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 44, \"PE Portfolio Values\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: portfolio_value stays a DOUBLE (prices have decimals). Columns are snake_case (pe_firm)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-044-portfolio-value-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "missing-price"
   ],
   "hints": [
    "Join on company, then SUM(shares * closing_price) GROUP BY pe_firm, date."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Join holdings to the day's prices and aggregate the per-holding value at the (firm, date) grain.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "aggregation"
   },
   "related_associations": [
    "zillacode/44"
   ],
   "recommendation": {
    "rank": 44,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "pe_firm",
     "date",
     "portfolio_value"
    ],
    "exact_schema": [
     "pe_firm",
     "date",
     "portfolio_value"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "portfolio",
     "columns": {
      "pe_firm": "VARCHAR",
      "company": "VARCHAR",
      "shares": "INTEGER"
     },
     "sample_rows": [
      {
       "pe_firm": "Alpha",
       "company": "A",
       "shares": 1000
      },
      {
       "pe_firm": "Alpha",
       "company": "B",
       "shares": 2000
      },
      {
       "pe_firm": "Beta",
       "company": "A",
       "shares": 1500
      },
      {
       "pe_firm": "Beta",
       "company": "C",
       "shares": 2500
      },
      {
       "pe_firm": "Gamma",
       "company": "B",
       "shares": 1200
      },
      {
       "pe_firm": "Gamma",
       "company": "C",
       "shares": 1300
      }
     ]
    },
    {
     "name": "prices",
     "columns": {
      "date": "DATE",
      "company": "VARCHAR",
      "closing_price": "DOUBLE"
     },
     "sample_rows": [
      {
       "date": "2023-01-01",
       "company": "A",
       "closing_price": 50.0
      },
      {
       "date": "2023-01-01",
       "company": "B",
       "closing_price": 20.0
      },
      {
       "date": "2023-01-01",
       "company": "C",
       "closing_price": 30.0
      },
      {
       "date": "2023-01-02",
       "company": "A",
       "closing_price": 52.0
      },
      {
       "date": "2023-01-02",
       "company": "B",
       "closing_price": 21.0
      },
      {
       "date": "2023-01-02",
       "company": "C",
       "closing_price": 31.0
      }
     ]
    }
   ],
   "output_schema": {
    "pe_firm": "value",
    "date": "value",
    "portfolio_value": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 44 \"PE Portfolio Values\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-044",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "portfolio_value stays a DOUBLE (prices have decimals). Columns are snake_case (pe_firm).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: pe_firm, date, portfolio_value\nSELECT *\nFROM portfolio\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: pe_firm, date, portfolio_value\nSELECT *\nFROM portfolio\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(portfolio, prices):\n    # Transform the input DataFrames into the result described in the brief.\n    return portfolio\n\n\ndisplay(etl(pd.DataFrame(portfolio), pd.DataFrame(prices)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(portfolio, prices):\n    # Transform the input DataFrames into the result described in the brief.\n    return portfolio\n\n\ndisplay(etl(pl.DataFrame(portfolio), pl.DataFrame(prices)))\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\nportfolio = spark.table(\"portfolio\")\nprices = spark.table(\"prices\")\nportfolio\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'portfolio') }}\n",
    "supported_operations": [
     "aggregation",
     "joins"
    ],
    "model": "portfolio_value"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-045-gdp-growth",
   "version": "1",
   "industry": "Economics",
   "learning_objectives": [
    "UNION (not UNION ALL) removes the duplicated facts.",
    "Join each year to year - 1 of the same country; a LEFT JOIN leaves NULL when it is missing."
   ],
   "optimization": "Deduplicate with UNION, then self-join on (country, year - 1) so a gap year gives NULL instead of comparing",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Year-over-year GDP growth",
   "difficulty": "medium",
   "topics": [
    "self-join",
    "union",
    "ordering",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Two files hold yearly GDP per country and may both contain the same country-year (the same fact twice). For each country and year, return the growth rate from the previous calendar year, (gdp - previous gdp) / previous gdp * 100, rounded to two decimals. It is NULL when the previous year is missing or its GDP is 0. Sort by country, then year.",
   "sections": [
    {
     "title": "Tables",
     "body": "df1(country VARCHAR, year INTEGER, gdp DOUBLE)\ndf2(country VARCHAR, year INTEGER, gdp DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`."
    },
    {
     "title": "Output",
     "body": "Columns in this order: country, year, gdp_growth_rate. One row per distinct (country, year). Row order is graded: country ascending, then year ascending."
    },
    {
     "title": "Common pitfall",
     "body": "LAG takes the previous row, which is not the previous year when a year is missing, and duplicate country-years from the two files would compare a year with itself (0% growth)."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 45, \"GDP Growth Rate\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The growth compares with the previous calendar year, as the statement says; duplicated country-years count once (ZillaCode's LAG compared duplicates and non-consecutive years). Columns are snake_case."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-045-gdp-growth-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "gap-year-and-duplicates"
   ],
   "hints": [
    "UNION (not UNION ALL) removes the duplicated facts.",
    "Join each year to year - 1 of the same country; a LEFT JOIN leaves NULL when it is missing."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Deduplicate with UNION, then self-join on (country, year - 1) so a gap year gives NULL instead of comparing with an older year. NULLIF guards the division (Snowflake would raise on a GDP of 0).",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "self-join"
   },
   "related_associations": [
    "zillacode/45"
   ],
   "recommendation": {
    "rank": 45,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "country",
     "year",
     "gdp_growth_rate"
    ],
    "exact_schema": [
     "country",
     "year",
     "gdp_growth_rate"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df1",
     "columns": {
      "country": "VARCHAR",
      "year": "INTEGER",
      "gdp": "DOUBLE"
     },
     "sample_rows": [
      {
       "country": "USA",
       "year": 2018,
       "gdp": 20544.34
      },
      {
       "country": "USA",
       "year": 2019,
       "gdp": 21427.7
      },
      {
       "country": "China",
       "year": 2018,
       "gdp": 13894.04
      }
     ]
    },
    {
     "name": "df2",
     "columns": {
      "country": "VARCHAR",
      "year": "INTEGER",
      "gdp": "DOUBLE"
     },
     "sample_rows": [
      {
       "country": "USA",
       "year": 2010,
       "gdp": 14992.05
      },
      {
       "country": "USA",
       "year": 2011,
       "gdp": 15542.58
      },
      {
       "country": "USA",
       "year": 2012,
       "gdp": 16197.01
      },
      {
       "country": "USA",
       "year": 2013,
       "gdp": 16784.85
      },
      {
       "country": "USA",
       "year": 2014,
       "gdp": 17527.44
      },
      {
       "country": "USA",
       "year": 2015,
       "gdp": 18238.3
      },
      {
       "country": "USA",
       "year": 2016,
       "gdp": 18715.03
      },
      {
       "country": "USA",
       "year": 2017,
       "gdp": 19542.68
      },
      {
       "country": "USA",
       "year": 2018,
       "gdp": 20544.34
      },
      {
       "country": "USA",
       "year": 2019,
       "gdp": 21427.7
      }
     ]
    }
   ],
   "output_schema": {
    "country": "value",
    "year": "value",
    "gdp_growth_rate": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 45 \"GDP Growth Rate\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-045",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The growth compares with the previous calendar year, as the statement says; duplicated country-years count once (ZillaCode's LAG compared duplicates and non-consecutive years). Columns are snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: country, year, gdp_growth_rate\nSELECT *\nFROM df1\n",
    "supported_operations": [
     "self-join",
     "union",
     "ordering",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: country, year, gdp_growth_rate\nSELECT *\nFROM df1\n",
    "supported_operations": [
     "self-join",
     "union",
     "ordering",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df1, df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return df1\n\n\ndisplay(etl(pd.DataFrame(df1), pd.DataFrame(df2)))\n",
    "supported_operations": [
     "self-join",
     "union",
     "ordering",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df1, df2):\n    # Transform the input DataFrames into the result described in the brief.\n    return df1\n\n\ndisplay(etl(pl.DataFrame(df1), pl.DataFrame(df2)))\n",
    "supported_operations": [
     "self-join",
     "union",
     "ordering",
     "nulls"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-046-gas-law",
   "version": "1",
   "industry": "Thermodynamics",
   "learning_objectives": [
    "Inner join on experiment_id, multiply, ORDER BY experiment_id."
   ],
   "optimization": "An inner join keeps the experiments measured in both tables; the product is computed per row.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Pressure times temperature per experiment",
   "difficulty": "easy",
   "topics": [
    "joins",
    "arithmetic",
    "ordering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For each experiment present in both tables, return result = pressure * temperature, sorted by experiment_id. Experiments missing from either table are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_temperature(experiment_id INTEGER, temperature DOUBLE)\ndf_pressure(experiment_id INTEGER, pressure DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: experiment_id, result. One row per experiment present in both tables. Row order is graded: experiment_id ascending."
    },
    {
     "title": "Common pitfall",
     "body": "Only experiments in both tables count: an outer join would add rows with a NULL result."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 46, \"Thermodynamics\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Columns are snake_case (ZillaCode used ExperimentID, Result, ...)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-046-gas-law-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "one-sided"
   ],
   "hints": [
    "Inner join on experiment_id, multiply, ORDER BY experiment_id."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "An inner join keeps the experiments measured in both tables; the product is computed per row.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "joins"
   },
   "related_associations": [
    "zillacode/46"
   ],
   "recommendation": {
    "rank": 46,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "experiment_id",
     "result"
    ],
    "exact_schema": [
     "experiment_id",
     "result"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_temperature",
     "columns": {
      "experiment_id": "INTEGER",
      "temperature": "DOUBLE"
     },
     "sample_rows": [
      {
       "experiment_id": 1,
       "temperature": 273.15
      },
      {
       "experiment_id": 2,
       "temperature": 293.15
      },
      {
       "experiment_id": 3,
       "temperature": 313.15
      }
     ]
    },
    {
     "name": "df_pressure",
     "columns": {
      "experiment_id": "INTEGER",
      "pressure": "DOUBLE"
     },
     "sample_rows": [
      {
       "experiment_id": 1,
       "pressure": 1.0
      },
      {
       "experiment_id": 3,
       "pressure": 2.0
      },
      {
       "experiment_id": 4,
       "pressure": 1.5
      }
     ]
    }
   ],
   "output_schema": {
    "experiment_id": "value",
    "result": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 46 \"Thermodynamics\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-046",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Columns are snake_case (ZillaCode used ExperimentID, Result, ...).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: experiment_id, result\nSELECT *\nFROM df_temperature\n",
    "supported_operations": [
     "joins",
     "arithmetic",
     "ordering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: experiment_id, result\nSELECT *\nFROM df_temperature\n",
    "supported_operations": [
     "joins",
     "arithmetic",
     "ordering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_temperature, df_pressure):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_temperature\n\n\ndisplay(etl(pd.DataFrame(df_temperature), pd.DataFrame(df_pressure)))\n",
    "supported_operations": [
     "joins",
     "arithmetic",
     "ordering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_temperature, df_pressure):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_temperature\n\n\ndisplay(etl(pl.DataFrame(df_temperature), pl.DataFrame(df_pressure)))\n",
    "supported_operations": [
     "joins",
     "arithmetic",
     "ordering"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf_temperature = spark.table(\"df_temperature\")\ndf_pressure = spark.table(\"df_pressure\")\ndf_temperature\n",
    "supported_operations": [
     "joins",
     "arithmetic",
     "ordering"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-047-experiments-full-join",
   "version": "1",
   "industry": "Materials engineering",
   "learning_objectives": [
    "FULL OUTER JOIN ... ON e.material_id = m.material_id.",
    "COALESCE(e.material_id, m.material_id)."
   ],
   "optimization": "A full outer join keeps both sides' unmatched rows; the join key must be merged from both sides.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Experiments and materials, all of them",
   "difficulty": "easy",
   "topics": [
    "full-outer-join",
    "nulls"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Join experiments to materials on material_id with a full outer join: every experiment and every material appears, with NULLs for the missing side. material_id comes from whichever side has it; an experiment without a material_id matches nothing.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_experiments(experiment_id INTEGER, material_id INTEGER, experiment_date VARCHAR, experiment_results DOUBLE)\ndf_materials(material_id INTEGER, material_name VARCHAR, material_type VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: experiment_id, material_id, material_name, material_type, experiment_date, experiment_results. One row per matched pair, plus one per unmatched experiment and per unmatched material. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "Selecting e.material_id loses the id of materials without experiments: COALESCE both sides (USING does it for you)."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 47, \"Materials Engineering\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Output columns follow the statement's order."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-047-experiments-full-join-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "unmatched-both-sides"
   ],
   "hints": [
    "FULL OUTER JOIN ... ON e.material_id = m.material_id.",
    "COALESCE(e.material_id, m.material_id)."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A full outer join keeps both sides' unmatched rows; the join key must be merged from both sides.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "full-outer-join"
   },
   "related_associations": [
    "zillacode/47"
   ],
   "recommendation": {
    "rank": 47,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "experiment_id",
     "material_id",
     "material_name",
     "material_type",
     "experiment_date",
     "experiment_results"
    ],
    "exact_schema": [
     "experiment_id",
     "material_id",
     "material_name",
     "material_type",
     "experiment_date",
     "experiment_results"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_experiments",
     "columns": {
      "experiment_id": "INTEGER",
      "material_id": "INTEGER",
      "experiment_date": "VARCHAR",
      "experiment_results": "DOUBLE"
     },
     "sample_rows": [
      {
       "experiment_id": 1,
       "material_id": 101,
       "experiment_date": "2023-07-01",
       "experiment_results": 7.6
      },
      {
       "experiment_id": 2,
       "material_id": 102,
       "experiment_date": "2023-07-02",
       "experiment_results": 8.3
      },
      {
       "experiment_id": 3,
       "material_id": 103,
       "experiment_date": "2023-07-03",
       "experiment_results": 6.9
      },
      {
       "experiment_id": 4,
       "material_id": 101,
       "experiment_date": "2023-07-04",
       "experiment_results": 7.2
      }
     ]
    },
    {
     "name": "df_materials",
     "columns": {
      "material_id": "INTEGER",
      "material_name": "VARCHAR",
      "material_type": "VARCHAR"
     },
     "sample_rows": [
      {
       "material_id": 101,
       "material_name": "Material A",
       "material_type": "Type X"
      },
      {
       "material_id": 102,
       "material_name": "Material B",
       "material_type": "Type Y"
      },
      {
       "material_id": 104,
       "material_name": "Material C",
       "material_type": "Type Z"
      }
     ]
    }
   ],
   "output_schema": {
    "experiment_id": "value",
    "material_id": "value",
    "material_name": "value",
    "material_type": "value",
    "experiment_date": "value",
    "experiment_results": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 47 \"Materials Engineering\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-047",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Output columns follow the statement's order.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: experiment_id, material_id, material_name, material_type, experiment_date, experiment_results\nSELECT *\nFROM df_experiments\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: experiment_id, material_id, material_name, material_type, experiment_date, experiment_results\nSELECT *\nFROM df_experiments\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_experiments, df_materials):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_experiments\n\n\ndisplay(etl(pd.DataFrame(df_experiments), pd.DataFrame(df_materials)))\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_experiments, df_materials):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_experiments\n\n\ndisplay(etl(pl.DataFrame(df_experiments), pl.DataFrame(df_materials)))\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf_experiments = spark.table(\"df_experiments\")\ndf_materials = spark.table(\"df_materials\")\ndf_experiments\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df_experiments') }}\n",
    "supported_operations": [
     "full-outer-join",
     "nulls"
    ],
    "model": "experiments_full_join"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-048-split-fields",
   "version": "1",
   "industry": "Flooring retail",
   "learning_objectives": [
    "first_name: SPLIT_PART(full_name, ' ', 1).",
    "last_name: the substring after the first space (SUBSTR with POSITION / strpos)."
   ],
   "optimization": "Splitting at the first separator is a different operation from taking the second field: use the separator's",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Split names and product info",
   "difficulty": "easy",
   "topics": [
    "strings",
    "joins"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return every order with its customer and product details. Split full_name at its first space: first_name is the text before it and last_name everything after it ('Mary Ann Lee' gives 'Mary' and 'Ann Lee'). Split product_info ('type,color') at the comma into product_type and product_color. Orders of unknown customers or products are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "customers(customer_id INTEGER, full_name VARCHAR, location VARCHAR)\norders(order_id INTEGER, customer_id INTEGER, product_id INTEGER, quantity INTEGER)\nproducts(product_id INTEGER, product_info VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: order_id, customer_id, first_name, last_name, location, product_id, product_type, product_color, quantity. One row per order with a known customer and product. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "SPLIT_PART(full_name, ' ', 2) returns only the second word; a last name can contain spaces."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 48, \"Floors \"R\" Us\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: last_name is everything after the first space (ZillaCode took the second word)."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-048-split-fields-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "long-names"
   ],
   "hints": [
    "first_name: SPLIT_PART(full_name, ' ', 1).",
    "last_name: the substring after the first space (SUBSTR with POSITION / strpos)."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Splitting at the first separator is a different operation from taking the second field: use the separator's position for the remainder. The product info has exactly one comma, so SPLIT_PART works there.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "strings"
   },
   "related_associations": [
    "zillacode/48"
   ],
   "recommendation": {
    "rank": 48,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "order_id",
     "customer_id",
     "first_name",
     "last_name",
     "location",
     "product_id",
     "product_type",
     "product_color",
     "quantity"
    ],
    "exact_schema": [
     "order_id",
     "customer_id",
     "first_name",
     "last_name",
     "location",
     "product_id",
     "product_type",
     "product_color",
     "quantity"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "customers",
     "columns": {
      "customer_id": "INTEGER",
      "full_name": "VARCHAR",
      "location": "VARCHAR"
     },
     "sample_rows": [
      {
       "customer_id": 1,
       "full_name": "John Doe",
       "location": "Texas"
      },
      {
       "customer_id": 2,
       "full_name": "Jane Smith",
       "location": "California"
      }
     ]
    },
    {
     "name": "orders",
     "columns": {
      "order_id": "INTEGER",
      "customer_id": "INTEGER",
      "product_id": "INTEGER",
      "quantity": "INTEGER"
     },
     "sample_rows": [
      {
       "order_id": 1001,
       "customer_id": 1,
       "product_id": 101,
       "quantity": 5
      },
      {
       "order_id": 1002,
       "customer_id": 2,
       "product_id": 102,
       "quantity": 2
      }
     ]
    },
    {
     "name": "products",
     "columns": {
      "product_id": "INTEGER",
      "product_info": "VARCHAR"
     },
     "sample_rows": [
      {
       "product_id": 101,
       "product_info": "Carpet,Red"
      },
      {
       "product_id": 102,
       "product_info": "Tile,Blue"
      }
     ]
    }
   ],
   "output_schema": {
    "order_id": "value",
    "customer_id": "value",
    "first_name": "value",
    "last_name": "value",
    "location": "value",
    "product_id": "value",
    "product_type": "value",
    "product_color": "value",
    "quantity": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 48 \"Floors \"R\" Us\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-048",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "last_name is everything after the first space (ZillaCode took the second word).",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: order_id, customer_id, first_name, last_name, location, product_id, product_type, product_color, quantity\nSELECT *\nFROM customers\n",
    "supported_operations": [
     "strings",
     "joins"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: order_id, customer_id, first_name, last_name, location, product_id, product_type, product_color, quantity\nSELECT *\nFROM customers\n",
    "supported_operations": [
     "strings",
     "joins"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(customers, orders, products):\n    # Transform the input DataFrames into the result described in the brief.\n    return customers\n\n\ndisplay(etl(pd.DataFrame(customers), pd.DataFrame(orders), pd.DataFrame(products)))\n",
    "supported_operations": [
     "strings",
     "joins"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(customers, orders, products):\n    # Transform the input DataFrames into the result described in the brief.\n    return customers\n\n\ndisplay(etl(pl.DataFrame(customers), pl.DataFrame(orders), pl.DataFrame(products)))\n",
    "supported_operations": [
     "strings",
     "joins"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'customers') }}\n",
    "supported_operations": [
     "strings",
     "joins"
    ],
    "model": "split_fields"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-049-name-lengths",
   "version": "1",
   "industry": "Airlines",
   "learning_objectives": [
    "LEFT JOIN airports AS o ... LEFT JOIN airports AS d ...",
    "LENGTH(TRIM(name))."
   ],
   "optimization": "The same lookup table plays two roles, so it is joined twice with different aliases; LEFT JOINs keep every flight.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Lengths of trimmed names",
   "difficulty": "easy",
   "topics": [
    "strings",
    "left-join",
    "self-join"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "For every flight, return the length of its origin airport's name, of its destination airport's name, and of its plane's model (the plane whose plane_id equals the flight_id), each measured after trimming leading and trailing spaces. An unknown airport or plane gives a NULL length; the flight is kept.",
   "sections": [
    {
     "title": "Tables",
     "body": "flights(flight_id INTEGER, origin_airport VARCHAR, destination_airport VARCHAR)\nairports(airport_id VARCHAR, airport_name VARCHAR)\nplanes(plane_id INTEGER, plane_model VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: flight_id, origin_airport_name_length, destination_airport_name_length, plane_model_length. One row per flight. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "The airports table is used twice (origin and destination): join it twice under two aliases. An inner join would drop flights with an unknown airport."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 49, \"Busy Airline\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: Names with surrounding spaces are trimmed as the statement asks; an edge check covers them."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-049-name-lengths-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "spaces-and-unknowns"
   ],
   "hints": [
    "LEFT JOIN airports AS o ... LEFT JOIN airports AS d ...",
    "LENGTH(TRIM(name))."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "The same lookup table plays two roles, so it is joined twice with different aliases; LEFT JOINs keep every flight.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "strings"
   },
   "related_associations": [
    "zillacode/49"
   ],
   "recommendation": {
    "rank": 49,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "flight_id",
     "origin_airport_name_length",
     "destination_airport_name_length",
     "plane_model_length"
    ],
    "exact_schema": [
     "flight_id",
     "origin_airport_name_length",
     "destination_airport_name_length",
     "plane_model_length"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "flights",
     "columns": {
      "flight_id": "INTEGER",
      "origin_airport": "VARCHAR",
      "destination_airport": "VARCHAR"
     },
     "sample_rows": [
      {
       "flight_id": 1,
       "origin_airport": "A1",
       "destination_airport": "B1"
      },
      {
       "flight_id": 2,
       "origin_airport": "A2",
       "destination_airport": "B2"
      },
      {
       "flight_id": 3,
       "origin_airport": "A3",
       "destination_airport": "B3"
      }
     ]
    },
    {
     "name": "airports",
     "columns": {
      "airport_id": "VARCHAR",
      "airport_name": "VARCHAR"
     },
     "sample_rows": [
      {
       "airport_id": "A1",
       "airport_name": "San Francisco"
      },
      {
       "airport_id": "B1",
       "airport_name": "Los Angeles"
      },
      {
       "airport_id": "A2",
       "airport_name": "New York"
      },
      {
       "airport_id": "B2",
       "airport_name": "Boston"
      },
      {
       "airport_id": "A3",
       "airport_name": "Miami"
      },
      {
       "airport_id": "B3",
       "airport_name": "Orlando"
      }
     ]
    },
    {
     "name": "planes",
     "columns": {
      "plane_id": "INTEGER",
      "plane_model": "VARCHAR"
     },
     "sample_rows": [
      {
       "plane_id": 1,
       "plane_model": "Airbus A320"
      },
      {
       "plane_id": 2,
       "plane_model": "Boeing 737"
      },
      {
       "plane_id": 3,
       "plane_model": "Airbus A380"
      }
     ]
    }
   ],
   "output_schema": {
    "flight_id": "value",
    "origin_airport_name_length": "value",
    "destination_airport_name_length": "value",
    "plane_model_length": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 49 \"Busy Airline\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-049",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Names with surrounding spaces are trimmed as the statement asks; an edge check covers them.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: flight_id, origin_airport_name_length, destination_airport_name_length, plane_model_length\nSELECT *\nFROM flights\n",
    "supported_operations": [
     "strings",
     "left-join",
     "self-join"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: flight_id, origin_airport_name_length, destination_airport_name_length, plane_model_length\nSELECT *\nFROM flights\n",
    "supported_operations": [
     "strings",
     "left-join",
     "self-join"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(flights, airports, planes):\n    # Transform the input DataFrames into the result described in the brief.\n    return flights\n\n\ndisplay(etl(pd.DataFrame(flights), pd.DataFrame(airports), pd.DataFrame(planes)))\n",
    "supported_operations": [
     "strings",
     "left-join",
     "self-join"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(flights, airports, planes):\n    # Transform the input DataFrames into the result described in the brief.\n    return flights\n\n\ndisplay(etl(pl.DataFrame(flights), pl.DataFrame(airports), pl.DataFrame(planes)))\n",
    "supported_operations": [
     "strings",
     "left-join",
     "self-join"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'flights') }}\n",
    "supported_operations": [
     "strings",
     "left-join",
     "self-join"
    ],
    "model": "name_lengths"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-050-bulk-artifacts",
   "version": "1",
   "industry": "Archaeology",
   "learning_objectives": [
    "UPPER(material) and WHERE quantity > 100."
   ],
   "optimization": "A projection with a string function and a strict filter; UPPER(NULL) is NULL in every engine.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Artifacts found in bulk",
   "difficulty": "easy",
   "topics": [
    "filtering",
    "strings"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return the artifacts found in quantities greater than 100, with their material in upper case. The other columns are unchanged; an unknown material stays unknown.",
   "sections": [
    {
     "title": "Tables",
     "body": "artifacts(id VARCHAR, item VARCHAR, period VARCHAR, material VARCHAR, quantity INTEGER)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: id, item, period, material, quantity. One row per artifact with a quantity above 100. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "'Greater than 100' excludes exactly 100."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 50, \"Archaeology Record Keeping\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: id is text, as the statement's schema says; columns are snake_case."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-050-bulk-artifacts-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "boundary-and-null"
   ],
   "hints": [
    "UPPER(material) and WHERE quantity > 100."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "A projection with a string function and a strict filter; UPPER(NULL) is NULL in every engine.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "filtering"
   },
   "related_associations": [
    "zillacode/50"
   ],
   "recommendation": {
    "rank": 50,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "id",
     "item",
     "period",
     "material",
     "quantity"
    ],
    "exact_schema": [
     "id",
     "item",
     "period",
     "material",
     "quantity"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "artifacts",
     "columns": {
      "id": "VARCHAR",
      "item": "VARCHAR",
      "period": "VARCHAR",
      "material": "VARCHAR",
      "quantity": "INTEGER"
     },
     "sample_rows": [
      {
       "id": "1",
       "item": "Pottery",
       "period": "Prehistoric",
       "material": "clay",
       "quantity": 150
      },
      {
       "id": "2",
       "item": "Weapon",
       "period": "Medieval",
       "material": "metal",
       "quantity": 90
      },
      {
       "id": "3",
       "item": "Jewel",
       "period": "Roman",
       "material": "gold",
       "quantity": 200
      }
     ]
    }
   ],
   "output_schema": {
    "id": "value",
    "item": "value",
    "period": "value",
    "material": "value",
    "quantity": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 50 \"Archaeology Record Keeping\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-050",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "id is text, as the statement's schema says; columns are snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: id, item, period, material, quantity\nSELECT *\nFROM artifacts\n",
    "supported_operations": [
     "filtering",
     "strings"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: id, item, period, material, quantity\nSELECT *\nFROM artifacts\n",
    "supported_operations": [
     "filtering",
     "strings"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(artifacts):\n    # Transform the input DataFrames into the result described in the brief.\n    return artifacts\n\n\ndisplay(etl(pd.DataFrame(artifacts)))\n",
    "supported_operations": [
     "filtering",
     "strings"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(artifacts):\n    # Transform the input DataFrames into the result described in the brief.\n    return artifacts\n\n\ndisplay(etl(pl.DataFrame(artifacts)))\n",
    "supported_operations": [
     "filtering",
     "strings"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'artifacts') }}\n",
    "supported_operations": [
     "filtering",
     "strings"
    ],
    "model": "bulk_artifacts"
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-051-valid-expressions",
   "version": "1",
   "industry": "Mathematics",
   "learning_objectives": [
    "The pattern is ([0-9]+[-+*/])+[0-9]+.",
    "Full match: regexp_full_match (DuckDB), REGEXP_LIKE (Snowflake), str.fullmatch (pandas)."
   ],
   "optimization": "Regular expression engines differ on whether a pattern must cover the whole value. Anchoring (or a full-match",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Valid arithmetic expressions",
   "difficulty": "medium",
   "topics": [
    "regular-expressions",
    "ordering"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Keep the rows whose whole expression is numbers (digits 0-9) separated by the operators +, -, * or /, with at least one operator: '5+3' and '2*3+1' are valid, '7', 'x1+2' and '1+2 ' are not. Return them sorted by id (the input order).",
   "sections": [
    {
     "title": "Tables",
     "body": "df_math_expr(id INTEGER, expression VARCHAR)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`."
    },
    {
     "title": "Output",
     "body": "Columns in this order: id, expression. One row per valid expression. Row order is graded: id ascending."
    },
    {
     "title": "Common pitfall",
     "body": "The whole string must match. Snowflake's REGEXP_LIKE matches the whole string implicitly, DuckDB's regexp_matches and Polars' str.contains search for a match anywhere, so anchor them with ^ and $."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 51, \"Mathematical Regular Expressions\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass changes: The order is graded by id, which is the input order ZillaCode's statement asked to keep."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-051-valid-expressions-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "partial-matches"
   ],
   "hints": [
    "The pattern is ([0-9]+[-+*/])+[0-9]+.",
    "Full match: regexp_full_match (DuckDB), REGEXP_LIKE (Snowflake), str.fullmatch (pandas)."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Regular expression engines differ on whether a pattern must cover the whole value. Anchoring (or a full-match function) makes the rule explicit; in the character class, '-' goes first so it is not a range.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "regular-expressions"
   },
   "related_associations": [
    "zillacode/51"
   ],
   "recommendation": {
    "rank": 51,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": true,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "id",
     "expression"
    ],
    "exact_schema": [
     "id",
     "expression"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_math_expr",
     "columns": {
      "id": "INTEGER",
      "expression": "VARCHAR"
     },
     "sample_rows": [
      {
       "id": 1,
       "expression": "5+3"
      },
      {
       "id": 2,
       "expression": "hello"
      },
      {
       "id": 3,
       "expression": "6/3"
      },
      {
       "id": 4,
       "expression": "world"
      },
      {
       "id": 5,
       "expression": "2*3+1"
      }
     ]
    }
   ],
   "output_schema": {
    "id": "value",
    "expression": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 51 \"Mathematical Regular Expressions\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-051",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "The order is graded by id, which is the input order ZillaCode's statement asked to keep.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: id, expression\nSELECT *\nFROM df_math_expr\n",
    "supported_operations": [
     "regular-expressions",
     "ordering"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: id, expression\nSELECT *\nFROM df_math_expr\n",
    "supported_operations": [
     "regular-expressions",
     "ordering"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_math_expr):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_math_expr\n\n\ndisplay(etl(pd.DataFrame(df_math_expr)))\n",
    "supported_operations": [
     "regular-expressions",
     "ordering"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_math_expr):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_math_expr\n\n\ndisplay(etl(pl.DataFrame(df_math_expr)))\n",
    "supported_operations": [
     "regular-expressions",
     "ordering"
    ]
   }
  }
 },
 {
  "semantic": {
   "id": "zilla-052-stars-and-planets",
   "version": "1",
   "industry": "Astronomy",
   "learning_objectives": [
    "JOIN df_planet AS p ON p.star_id = s.id.",
    "Alias every output column."
   ],
   "optimization": "Qualified column references and aliases resolve the name clash between the two tables.",
   "reflection": "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
  },
  "fixture_version": "1",
  "common": {
   "schema_version": 1,
   "title": "Planets with their stars",
   "difficulty": "easy",
   "topics": [
    "joins",
    "renaming"
   ],
   "tags": [
    "zillacode",
    "leetcode-style",
    "shared-fixture"
   ],
   "origin": "migrated",
   "prompt": "Return every planet with the star it orbits (df_planet.star_id = df_star.id). Both tables have id, name, type and distance columns, so rename them: star_name, star_color, star_type, planet_name, planet_type, distance_star_earth (the star's distance, in light years) and distance_planet_star (the planet's, in AU). Planets of unknown stars and stars without planets are left out.",
   "sections": [
    {
     "title": "Tables",
     "body": "df_star(id INTEGER, name VARCHAR, color VARCHAR, type VARCHAR, distance DOUBLE)\ndf_planet(id INTEGER, name VARCHAR, star_id INTEGER, type VARCHAR, distance DOUBLE)"
    },
    {
     "title": "How each engine reads the tables",
     "body": "SQL (DuckDB): `FROM <table>`. Snowflake SQL: `FROM <table>`; your query is translated to DuckDB (Snowflake SQL dialect translated to DuckDB, not Snowflake), and functions outside the supported subset are refused. pandas: each table is a list of row dicts in a variable of the same name; the starter turns them into DataFrames and passes them to `etl`, whose DataFrame is displayed. Polars: the same, with `pl.DataFrame`. SparkLab: `spark.table(\"<table>\")`; the last DataFrame is the result (bounded PySpark subset, not Apache Spark). dbt: your model reads `{{ source('zilla', '<table>') }}` and is built as a table by the Datapass dbt emulation (not dbt Core)."
    },
    {
     "title": "Output",
     "body": "Columns in this order: star_name, star_color, star_type, planet_name, planet_type, distance_star_earth, distance_planet_star. One row per planet of a known star. Row order is not graded."
    },
    {
     "title": "Common pitfall",
     "body": "The join is planet.star_id = star.id, not id = id; the same column names on both sides must be qualified and renamed."
    },
    {
     "title": "Source",
     "body": "Adapted from ZillaCode problem 52, \"Space Observatory\" (https://github.com/davidzajac1/zillacode, Apache-2.0), through CodeDELeet's normalized import. Datapass kept the ZillaCode contract; expected rows were recomputed."
    }
   ],
   "fixtures": [
    {
     "id": "zilla-052-stars-and-planets-fixtures",
     "version": "1"
    }
   ],
   "visible_checks": [
    {
     "id": "example",
     "description": "Public example: ZillaCode's first test, the tables shown above."
    }
   ],
   "hidden_check_refs": [
    "more-rows"
   ],
   "edge_check_refs": [
    "unknown-star"
   ],
   "hints": [
    "JOIN df_planet AS p ON p.star_id = s.id.",
    "Alias every output column."
   ],
   "solution": {
    "available": true,
    "reveal": "explicit"
   },
   "explanation": "Qualified column references and aliases resolve the name clash between the two tables.",
   "follow_ups": [
    "Which rows would change the answer at scale: duplicates, NULL keys or ties?"
   ],
   "canonical_placement": {
    "domain": "data-engineering",
    "topic": "joins"
   },
   "related_associations": [
    "zillacode/52"
   ],
   "recommendation": {
    "rank": 52,
    "reason": "ZillaCode problem order"
   },
   "validator_version": "semantic-rows-v1",
   "validation": {
    "kind": "rows",
    "ordered": false,
    "duplicate_sensitive": true,
    "relative_tolerance": 1e-09,
    "absolute_tolerance": 1e-06,
    "required_columns": [
     "star_name",
     "star_color",
     "star_type",
     "planet_name",
     "planet_type",
     "distance_star_earth",
     "distance_planet_star"
    ],
    "exact_schema": [
     "star_name",
     "star_color",
     "star_type",
     "planet_name",
     "planet_type",
     "distance_star_earth",
     "distance_planet_star"
    ],
    "forbidden_extra_columns": true,
    "null_semantics": "equal"
   },
   "data_context": [
    {
     "name": "df_star",
     "columns": {
      "id": "INTEGER",
      "name": "VARCHAR",
      "color": "VARCHAR",
      "type": "VARCHAR",
      "distance": "DOUBLE"
     },
     "sample_rows": [
      {
       "id": 1,
       "name": "Sun",
       "color": "Yellow",
       "type": "Dwarf",
       "distance": 0.0
      },
      {
       "id": 2,
       "name": "Sirius",
       "color": "Blue",
       "type": "Main Sequence Star",
       "distance": 8.6
      },
      {
       "id": 3,
       "name": "Vega",
       "color": "Blue",
       "type": "Main Sequence Star",
       "distance": 25.0
      }
     ]
    },
    {
     "name": "df_planet",
     "columns": {
      "id": "INTEGER",
      "name": "VARCHAR",
      "star_id": "INTEGER",
      "type": "VARCHAR",
      "distance": "DOUBLE"
     },
     "sample_rows": [
      {
       "id": 1,
       "name": "Mercury",
       "star_id": 1,
       "type": "Terrestrial",
       "distance": 0.39
      },
      {
       "id": 2,
       "name": "Venus",
       "star_id": 1,
       "type": "Terrestrial",
       "distance": 0.72
      },
      {
       "id": 3,
       "name": "Mars",
       "star_id": 1,
       "type": "Terrestrial",
       "distance": 1.52
      },
      {
       "id": 4,
       "name": "Sirius B",
       "star_id": 2,
       "type": "Gas Giant",
       "distance": 20.0
      },
      {
       "id": 5,
       "name": "Vega b",
       "star_id": 3,
       "type": "Gas Giant",
       "distance": 25.0
      }
     ]
    }
   ],
   "output_schema": {
    "star_name": "value",
    "star_color": "value",
    "star_type": "value",
    "planet_name": "value",
    "planet_type": "value",
    "distance_star_earth": "value",
    "distance_planet_star": "value"
   },
   "runtime_requirements": [],
   "provenance": {
    "source": "ZillaCode problem 52 \"Space Observatory\" (https://github.com/davidzajac1/zillacode), from CodeDELeet's normalized import zilla-052",
    "license": "Apache-2.0; see LICENSE and NOTICE in content/exercise-packs/zilla-v1",
    "upstream_changes": "Imports changed from pyspark to sparklite. SQL translated from reference warehouse dialect to a bounded DuckDB variant. Ordering is not asserted unless explicitly requested; column names and row multiplicity are asserted. Explicit output projection preserves the published column names and case; it does not embed expected rows.",
    "datapass_changes": "Contract kept; names already snake_case.",
    "fixtures": "Visible: ZillaCode test 1. Hidden: ZillaCode test 2 unless noted. Edge: authored for Datapass. Expected rows recomputed from the Datapass SQL reference on DuckDB and reviewed by hand; every variant is graded in CI."
   },
   "constraints": {
    "fixtures": "At most 200 rows per table; complete results are graded, truncated results never pass."
   }
  },
  "variants": {
   "sql": {
    "starter_source": "-- Return: star_name, star_color, star_type, planet_name, planet_type, distance_star_earth, distance_planet_star\nSELECT *\nFROM df_star\n",
    "supported_operations": [
     "joins",
     "renaming"
    ]
   },
   "snowflake": {
    "starter_source": "-- Snowflake SQL. Return: star_name, star_color, star_type, planet_name, planet_type, distance_star_earth, distance_planet_star\nSELECT *\nFROM df_star\n",
    "supported_operations": [
     "joins",
     "renaming"
    ]
   },
   "python": {
    "starter_source": "import pandas as pd\n\ndef etl(df_star, df_planet):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_star\n\n\ndisplay(etl(pd.DataFrame(df_star), pd.DataFrame(df_planet)))\n",
    "supported_operations": [
     "joins",
     "renaming"
    ]
   },
   "polars": {
    "starter_source": "import polars as pl\n\ndef etl(df_star, df_planet):\n    # Transform the input DataFrames into the result described in the brief.\n    return df_star\n\n\ndisplay(etl(pl.DataFrame(df_star), pl.DataFrame(df_planet)))\n",
    "supported_operations": [
     "joins",
     "renaming"
    ]
   },
   "sparklab": {
    "starter_source": "from pyspark.sql import functions as F\nfrom pyspark.sql import Window\n\ndf_star = spark.table(\"df_star\")\ndf_planet = spark.table(\"df_planet\")\ndf_star\n",
    "supported_operations": [
     "joins",
     "renaming"
    ]
   },
   "dbt-sql": {
    "starter_source": "select *\nfrom {{ source('zilla', 'df_star') }}\n",
    "supported_operations": [
     "joins",
     "renaming"
    ],
    "model": "stars_and_planets"
   }
  }
 }
]
