[
  {
    "schema_version": 1,
    "id": "py-parse-amount",
    "version": "1",
    "title": "Parse an amount into cents",
    "difficulty": "easy",
    "topics": [
      "python-production",
      "typing",
      "parsing"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write parse_amount(text: str) -> int that turns a decimal amount such as '12.34', ' 5 ' or '-0.5' into integer cents (1234, 500, -50). At most two decimals; anything else (empty text, letters, '1.234') raises ValueError. Money never goes through float. Then write at least 3 tests.",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Parse an amount into cents.\n\nWrite parse_amount(text: str) -> int that turns a decimal amount such as '12.34', ' 5 ' or '-0.5' into integer cents (1234, 500, -50). At most two decimals; anything else (empty text, letters, '1.234') raises ValueError. Money never goes through float. Then write at least 3 tests.\n\"\"\"\n\ndef parse_amount(text: str) -> int:\n    \"\"\"Decimal amount as integer cents.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-parse-amount-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "float cannot represent 0.29 exactly.",
      "decimal.Decimal parses text exactly; its exponent tells how many decimals there are."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Decimal parses the text exactly, so 0.29 stays 0.29 and becomes 29 cents; float(0.29) * 100 is 28.999... and int() truncates it to 28. The exponent of the Decimal tells how many decimals were written, and Decimal refuses letters (nan and inf parse, so is_finite() refuses them).",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-read-totals",
    "version": "1",
    "title": "Total a CSV file per customer",
    "difficulty": "easy",
    "topics": [
      "python-production",
      "files",
      "csv"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write total_by_customer(path) -> dict[str, int] that reads a UTF-8 CSV file with a header row customer,amount (amount is an integer) and returns the total amount per customer. Blank lines are skipped. Customer names can contain commas when quoted (\"Smith, Jo\") and non-ASCII letters. Then write at least 3 tests (pytest's tmp_path fixture gives each test its own folder).",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Total a CSV file per customer.\n\nWrite total_by_customer(path) -> dict[str, int] that reads a UTF-8 CSV file with a header row customer,amount (amount is an integer) and returns the total amount per customer. Blank lines are skipped. Customer names can contain commas when quoted (\"Smith, Jo\") and non-ASCII letters. Then write at least 3 tests (pytest's tmp_path fixture gives each test its own folder).\n\"\"\"\n\nfrom pathlib import Path\n\n\ndef total_by_customer(path: str | Path) -> dict[str, int]:\n    \"\"\"Total amount per customer in a CSV file.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-read-totals-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "A quoted field can contain the separator: splitting lines on ',' breaks it.",
      "csv.DictReader reads the header and handles quotes; open the file with newline=''."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "The csv module understands quoting, so \"Smith, Jo\" stays one field; splitting on commas keeps the quotes in the name and breaks on names with commas. newline='' lets csv handle Windows line endings, and an explicit encoding keeps Zoë readable whatever the computer's default encoding is.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-log-bad-rows",
    "version": "1",
    "title": "Log the rows you drop",
    "difficulty": "medium",
    "topics": [
      "python-production",
      "logging"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write clean_rows(rows: list[dict]) -> list[dict] that keeps the rows whose 'qty' is an int >= 0 and drops the others. For each dropped row, log ONE message at WARNING level on the logger named 'pipeline.clean' that contains the row's 'id'. Use the logging module, not print. Then write at least 3 tests (pytest's caplog fixture captures log records).",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Log the rows you drop.\n\nWrite clean_rows(rows: list[dict]) -> list[dict] that keeps the rows whose 'qty' is an int >= 0 and drops the others. For each dropped row, log ONE message at WARNING level on the logger named 'pipeline.clean' that contains the row's 'id'. Use the logging module, not print. Then write at least 3 tests (pytest's caplog fixture captures log records).\n\"\"\"\n\nimport logging\n\n\ndef clean_rows(rows: list[dict]) -> list[dict]:\n    \"\"\"Keep rows with a valid qty; log a warning for each dropped row.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-log-bad-rows-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "A dropped row is a problem someone should see: which level is that?",
      "bool is a subclass of int in Python: True passes isinstance(x, int)."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "WARNING is the level for data problems the pipeline survives; INFO is filtered out by most production configurations, so those drops would be silent. A named module logger ('pipeline.clean') lets operators route and filter the messages. isinstance(True, int) is True in Python, so a quantity check must exclude bool explicitly.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-config-errors",
    "version": "1",
    "title": "Clear errors for a bad config",
    "difficulty": "medium",
    "topics": [
      "python-production",
      "errors",
      "json"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write load_config(text: str) -> dict that parses a JSON object with a required string 'source' and a required integer 'batch_size' > 0, and returns it. Define class ConfigError(ValueError). Every problem raises ConfigError with a message that names the key at fault; invalid JSON raises ConfigError chained from the JSON error (raise ... from exc). Then write at least 3 tests.",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Clear errors for a bad config.\n\nWrite load_config(text: str) -> dict that parses a JSON object with a required string 'source' and a required integer 'batch_size' > 0, and returns it. Define class ConfigError(ValueError). Every problem raises ConfigError with a message that names the key at fault; invalid JSON raises ConfigError chained from the JSON error (raise ... from exc). Then write at least 3 tests.\n\"\"\"\n\nclass ConfigError(ValueError):\n    \"\"\"A configuration problem.\"\"\"\n\n\ndef load_config(text: str) -> dict:\n    \"\"\"Parse and validate a JSON config.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-config-errors-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "json.loads raises json.JSONDecodeError: catch it and raise your own error from it.",
      "Check the types too: \"5\" and true are not integers > 0."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Callers catch one exception type (ConfigError, still a ValueError) and read which key is wrong. `raise ... from exc` keeps the JSON error as __cause__, so the traceback shows where the text broke. Validating types matters: \"5\" > 0 raises TypeError and true counts as 1.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-retry-backoff",
    "version": "1",
    "title": "Retry with exponential backoff",
    "difficulty": "medium",
    "topics": [
      "python-production",
      "retries",
      "errors"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write retry(func, attempts=3, delay=0.5, sleep=time.sleep, retry_on=(ConnectionError, TimeoutError)) that calls func() and returns its result. When func raises one of retry_on, sleep delay * 2**n (0.5, then 1.0, …) and try again, up to `attempts` calls in total; after the last failure, re-raise that exception. Any other exception propagates at once, without sleeping. Then write at least 3 tests (pass a fake sleep so the tests do not wait).",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Retry with exponential backoff.\n\nWrite retry(func, attempts=3, delay=0.5, sleep=time.sleep, retry_on=(ConnectionError, TimeoutError)) that calls func() and returns its result. When func raises one of retry_on, sleep delay * 2**n (0.5, then 1.0, …) and try again, up to `attempts` calls in total; after the last failure, re-raise that exception. Any other exception propagates at once, without sleeping. Then write at least 3 tests (pass a fake sleep so the tests do not wait).\n\"\"\"\n\nimport time\n\n\ndef retry(func, attempts=3, delay=0.5, sleep=time.sleep, retry_on=(ConnectionError, TimeoutError)):\n    \"\"\"Call func, retrying transient errors with exponential backoff.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-retry-backoff-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "Only transient errors deserve a retry: a ValueError will fail the same way next time.",
      "Inject sleep so a test can record the waits instead of waiting."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Retrying everything hides real bugs (a KeyError fails identically every time) and multiplies load on a broken service. Exponential backoff gives a struggling service room to recover. Injecting sleep keeps the tests instant and lets them check the waits.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-chunked",
    "version": "1",
    "title": "Split any iterable into batches",
    "difficulty": "easy",
    "topics": [
      "python-production",
      "generators",
      "typing"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write chunked(items: Iterable[T], size: int) -> Iterator[list[T]] that yields lists of `size` items, the last one possibly shorter. It must work lazily on generators (never call len() or list() on the whole input) and raise ValueError when size < 1. Then write at least 3 tests.",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Split any iterable into batches.\n\nWrite chunked(items: Iterable[T], size: int) -> Iterator[list[T]] that yields lists of `size` items, the last one possibly shorter. It must work lazily on generators (never call len() or list() on the whole input) and raise ValueError when size < 1. Then write at least 3 tests.\n\"\"\"\n\nfrom collections.abc import Iterable, Iterator\nfrom typing import TypeVar\n\nT = TypeVar(\"T\")\n\n\ndef chunked(items: Iterable[T], size: int) -> Iterator[list[T]]:\n    \"\"\"Yield lists of size items.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-chunked-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "itertools.islice takes the next n items of an iterator without reading further.",
      "What happens to the last few items when they do not fill a batch?"
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "islice on one shared iterator reads exactly `size` items at a time, so generators and even infinite inputs work and memory stays bounded. The last short batch still holds data: dropping it loses rows.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-latest-by-key",
    "version": "1",
    "title": "Keep the latest version of each record",
    "difficulty": "medium",
    "topics": [
      "python-production",
      "data-quality",
      "typing"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write latest_by_key(rows: list[dict], key: str, ts: str) -> list[dict] that keeps, for each value of rows[i][key], the row with the greatest rows[i][ts] (ISO-8601 strings such as '2026-09-26T10:00:00'); on a tie, the row that comes later in the input wins. Return the kept rows in the order their key first appeared. Then write at least 3 tests.",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Keep the latest version of each record.\n\nWrite latest_by_key(rows: list[dict], key: str, ts: str) -> list[dict] that keeps, for each value of rows[i][key], the row with the greatest rows[i][ts] (ISO-8601 strings such as '2026-09-26T10:00:00'); on a tie, the row that comes later in the input wins. Return the kept rows in the order their key first appeared. Then write at least 3 tests.\n\"\"\"\n\ndef latest_by_key(rows: list[dict], key: str, ts: str) -> list[dict]:\n    \"\"\"Latest row per key.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-latest-by-key-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "The first row for a key is not the latest one when input arrives out of order.",
      "ISO-8601 strings of the same format sort correctly as text; >= decides ties."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Change feeds arrive out of order, so the first row seen is not the newest. Comparing ISO-8601 strings works because they sort chronologically as text; >= lets a later row win a tie, which matches 'the last write wins'. A dict keeps insertion order, so the keys stay in order of first appearance.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  },
  {
    "schema_version": 1,
    "id": "py-write-report",
    "version": "1",
    "title": "Write a report file safely",
    "difficulty": "hard",
    "topics": [
      "python-production",
      "files",
      "errors"
    ],
    "tags": [
      "pytest",
      "production-python"
    ],
    "origin": "authored",
    "language": "pytest",
    "runtime": "datapass-pytest-v1",
    "prompt": "Write write_report(folder: Path, name: str, text: str) -> Path that writes text (UTF-8) to folder/name and returns that path. Create the folder if it is missing. Refuse (ValueError) a name that is empty, contains / or \\\\, or is '.' or '..', so nothing is written outside the folder. Write atomically: write a temporary file in the same folder, then replace the target with os.replace, so a reader never sees half a report; leave no temporary file behind. Then write at least 3 tests.",
    "sections": [
      {
        "title": "Graded by pytest (real local Python)",
        "body": "Your file runs as real local Python, only while trusted local Python is on for this workspace. Grading runs pytest twice: on your file (your tests must pass, at least 3 of them), then, on Submit, on hidden tests that import your module as `solution`."
      },
      {
        "title": "Run it yourself",
        "body": "In a VS Code terminal in the exercise folder: python -m pytest solution.py -q (with the Datapass runtime's Python or any Python with pytest)."
      }
    ],
    "starter_source": "\"\"\"Write a report file safely.\n\nWrite write_report(folder: Path, name: str, text: str) -> Path that writes text (UTF-8) to folder/name and returns that path. Create the folder if it is missing. Refuse (ValueError) a name that is empty, contains / or \\\\, or is '.' or '..', so nothing is written outside the folder. Write atomically: write a temporary file in the same folder, then replace the target with os.replace, so a reader never sees half a report; leave no temporary file behind. Then write at least 3 tests.\n\"\"\"\n\nfrom pathlib import Path\n\n\ndef write_report(folder: Path, name: str, text: str) -> Path:\n    \"\"\"Write text to folder/name atomically.\"\"\"\n    raise NotImplementedError\n\n\n# Your tests: pytest runs every function named test_... in this file (write at least 3).\ndef test_example():\n    assert True\n",
    "fixtures": [
      {
        "id": "py-write-report-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "own-tests",
        "description": "Your own tests pass (at least 3)."
      }
    ],
    "hidden_check_refs": [
      "hidden"
    ],
    "edge_check_refs": [],
    "hints": [
      "'..' and separators let a name escape the folder: refuse them before building the path.",
      "tempfile.mkstemp(dir=folder) + os.replace(temp, target) is the atomic write pattern."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "A name from outside (a report title, a customer name) must not choose where the file lands: '..' or a separator would write outside the folder. os.replace swaps the complete file in one step on the same filesystem, so readers see the old report or the new one, never a truncated file, and the except branch removes the temporary file on failure.",
    "follow_ups": [],
    "canonical_placement": {
      "domain": "python-production",
      "topic": "python-production"
    },
    "related_associations": [
      "practice/python"
    ],
    "validator_version": "pytest-v1",
    "runtime_requirements": [
      "python",
      "pytest"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench"
    },
    "constraints": {
      "truth": "Real local Python and pytest, trusted local Python only; not a security sandbox."
    },
    "truth": "real"
  }
]
