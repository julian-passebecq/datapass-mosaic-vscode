[
  {
    "schema_version": 1,
    "id": "gov-rls-sales-reps",
    "version": "1",
    "title": "Row-level security: each rep sees their own sales",
    "difficulty": "medium",
    "topics": [
      "row-level-security",
      "security-policy",
      "governance"
    ],
    "tags": [
      "cloud-lab",
      "governance",
      "sql-pool",
      "simulated"
    ],
    "origin": "authored",
    "language": "sqlpool",
    "runtime": "datapass-sqlpool-sim-v1",
    "prompt": "dbo.sales holds every sale with its sales_rep. The users alice, bob and carol are sales reps; dana is in the role sales_managers; intern can read the table but sells nothing. Write a security predicate and a security policy so that a rep sees only their own rows and sales_managers see every row. A sale without a rep must stay hidden from reps.",
    "sections": [
      {
        "title": "Simulator",
        "body": "Your T-SQL script runs on the lab's simulated Azure Synapse dedicated SQL pool: the statements are translated to DuckDB for a documented subset and really run, on an isolated catalog built for each check. Security (row-level security, column permissions, dynamic data masking) is enforced for real by rewriting each query for the user it runs as: T-SQL security translated to DuckDB, not SQL Server. Nothing connects to Azure or Fabric. End statements with ; (a function takes its own GO batch)."
      },
      {
        "title": "Graded",
        "body": "SELECT sale_id, sales_rep, amount FROM dbo.sales run as alice, dana, bob, intern and carol (EXECUTE AS USER, then REVERT): the rows each one gets."
      },
      {
        "title": "Supported T-SQL",
        "body": "CREATE FUNCTION ... RETURNS TABLE WITH SCHEMABINDING AS RETURN SELECT 1 AS ... WHERE ...; CREATE SECURITY POLICY ... ADD FILTER PREDICATE f(column) ON table WITH (STATE = ON). USER_NAME() and IS_ROLEMEMBER('role') are resolved for the principal. BLOCK predicates are refused."
      }
    ],
    "starter_source": "-- Each sales rep sees only their own sales; members of sales_managers see every sale.\nCREATE FUNCTION dbo.fn_sales_rep(@sales_rep AS VARCHAR(40))\nRETURNS TABLE\nWITH SCHEMABINDING\nAS\nRETURN SELECT 1 AS fn_result\n       WHERE @sales_rep = USER_NAME();\nGO\n-- TODO: bind the predicate to dbo.sales with a security policy.\n",
    "fixtures": [
      {
        "id": "gov-rls-sales-reps-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "reps-and-manager",
        "description": "Rows each principal gets, enforced on the lab catalog."
      }
    ],
    "hidden_check_refs": [
      "other-principals"
    ],
    "edge_check_refs": [
      "rep-without-sales-row"
    ],
    "hints": [
      "The predicate function receives the row's sales_rep as @sales_rep; USER_NAME() is the user the query runs as.",
      "A NULL sales_rep never equals USER_NAME(), so it stays hidden unless you add a clause for it."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "The inline table-valued function returns a row (so the sale is visible) when the sale's rep is the current user or when the user is in sales_managers. The security policy binds it to dbo.sales as a FILTER predicate, so every SELECT is filtered silently. Filter predicates apply to dbo too: without a clause for it, the owner sees nothing.",
    "follow_ups": [
      "Run SELECT COUNT(*) FROM dbo.sales as dbo: why is it 0, and what would you add to the predicate for an ETL account?"
    ],
    "canonical_placement": {
      "domain": "governance",
      "topic": "row-level-security"
    },
    "related_associations": [
      "cloud-lab/sql-pool"
    ],
    "recommendation": {
      "rank": 1,
      "reason": "Data governance progression"
    },
    "validator_version": "rows-v2",
    "validation": {
      "kind": "rows",
      "ordered": false,
      "duplicate_sensitive": true,
      "relative_tolerance": 1e-09,
      "absolute_tolerance": 1e-06,
      "required_columns": [
        "principal",
        "denied",
        "sale_id",
        "sales_rep",
        "amount"
      ],
      "exact_schema": [
        "principal",
        "denied",
        "sale_id",
        "sales_rep",
        "amount"
      ],
      "forbidden_extra_columns": true,
      "null_semantics": "equal"
    },
    "runtime_requirements": [
      "sqlpool-simulator"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench: row-level security, column-level security and dynamic data masking in Synapse / Fabric Warehouse; Unity Catalog row filters, column masks and tags (Microsoft Learn)",
      "fixtures": "Authored scenarios; expected rows computed by running the reference and reviewed"
    },
    "constraints": {
      "truth": "T-SQL security translated to DuckDB, not SQL Server: rows and values enforced on an isolated local catalog."
    },
    "truth": "simulated"
  },
  {
    "schema_version": 1,
    "id": "gov-column-security",
    "version": "1",
    "title": "Column-level security with GRANT and DENY",
    "difficulty": "easy",
    "topics": [
      "column-level-security",
      "grant",
      "governance"
    ],
    "tags": [
      "cloud-lab",
      "governance",
      "sql-pool",
      "simulated"
    ],
    "origin": "authored",
    "language": "sqlpool",
    "runtime": "datapass-sqlpool-sim-v1",
    "prompt": "dbo.customers holds each customer's email and card number. The user analyst builds city reports and must read customer_id, customer_name and city, and nothing else. The role finance_team (member fin1) must read every column. Nobody has permissions yet: write the GRANT (and, if you prefer, DENY) statements.",
    "sections": [
      {
        "title": "Simulator",
        "body": "Your T-SQL script runs on the lab's simulated Azure Synapse dedicated SQL pool: the statements are translated to DuckDB for a documented subset and really run, on an isolated catalog built for each check. Security (row-level security, column permissions, dynamic data masking) is enforced for real by rewriting each query for the user it runs as: T-SQL security translated to DuckDB, not SQL Server. Nothing connects to Azure or Fabric. End statements with ; (a function takes its own GO batch)."
      },
      {
        "title": "Graded",
        "body": "Queries run as analyst and fin1 (EXECUTE AS USER): the report columns, SELECT *, and a query that reads card_number. A refused query is graded by its permission error."
      },
      {
        "title": "Supported T-SQL",
        "body": "GRANT | DENY | REVOKE SELECT ON table [(columns)] TO principal. A DENY wins over any GRANT, also one given through a role."
      }
    ],
    "starter_source": "-- analyst reads customers without email and card_number; finance_team reads every column.\nGRANT SELECT ON dbo.customers TO analyst, finance_team;\n",
    "fixtures": [
      {
        "id": "gov-column-security-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "report-columns",
        "description": "Rows each principal gets, enforced on the lab catalog."
      }
    ],
    "hidden_check_refs": [
      "select-star"
    ],
    "edge_check_refs": [
      "card-query"
    ],
    "hints": [
      "A column list after the table grants only those columns: GRANT SELECT ON t (a, b) TO user.",
      "SELECT * reads every column, so it is refused as soon as one column is not granted."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Column-level security is a column list on the GRANT: analyst can read three columns, so a query on those columns works and SELECT * or any query touching email or card_number fails with a permission error. finance_team gets the whole table. Granting the table and denying columns also works, but it is easy to forget one sensitive column.",
    "follow_ups": [
      "Which is safer when a new sensitive column is added later: a column GRANT or a table GRANT with column DENYs?"
    ],
    "canonical_placement": {
      "domain": "governance",
      "topic": "column-level-security"
    },
    "related_associations": [
      "cloud-lab/sql-pool"
    ],
    "recommendation": {
      "rank": 2,
      "reason": "Data governance progression"
    },
    "validator_version": "rows-v2",
    "validation": {
      "kind": "rows",
      "ordered": false,
      "duplicate_sensitive": true,
      "relative_tolerance": 1e-09,
      "absolute_tolerance": 1e-06,
      "required_columns": [
        "principal",
        "denied",
        "customer_id",
        "customer_name",
        "city",
        "email",
        "card_number"
      ],
      "exact_schema": [
        "principal",
        "denied",
        "customer_id",
        "customer_name",
        "city",
        "email",
        "card_number"
      ],
      "forbidden_extra_columns": true,
      "null_semantics": "equal"
    },
    "runtime_requirements": [
      "sqlpool-simulator"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench: row-level security, column-level security and dynamic data masking in Synapse / Fabric Warehouse; Unity Catalog row filters, column masks and tags (Microsoft Learn)",
      "fixtures": "Authored scenarios; expected rows computed by running the reference and reviewed"
    },
    "constraints": {
      "truth": "T-SQL security translated to DuckDB, not SQL Server: rows and values enforced on an isolated local catalog."
    },
    "truth": "simulated"
  },
  {
    "schema_version": 1,
    "id": "gov-dynamic-masking",
    "version": "1",
    "title": "Dynamic data masking for support agents",
    "difficulty": "medium",
    "topics": [
      "dynamic-data-masking",
      "unmask",
      "governance"
    ],
    "tags": [
      "cloud-lab",
      "governance",
      "sql-pool",
      "simulated"
    ],
    "origin": "authored",
    "language": "sqlpool",
    "runtime": "datapass-sqlpool-sim-v1",
    "prompt": "dbo.contacts in a Fabric warehouse holds customers' email, phone and birth date. Members of support_agents may read the table but must see masked values: the email as email() shows it, only the last 4 digits of the phone (the rest written as XXX-XXX-) and the default mask for the birth date. Members of compliance must see the real values.",
    "sections": [
      {
        "title": "Simulator",
        "body": "Your T-SQL script runs on the lab's simulated Microsoft Fabric Data Warehouse: the statements are translated to DuckDB for a documented subset and really run, on an isolated catalog built for each check. Security (row-level security, column permissions, dynamic data masking) is enforced for real by rewriting each query for the user it runs as: T-SQL security translated to DuckDB, not SQL Server. Nothing connects to Azure or Fabric. End statements with ; (a function takes its own GO batch)."
      },
      {
        "title": "Graded",
        "body": "SELECT customer_id, email, phone, birth_date FROM dbo.contacts run as agent1 (support_agents) and auditor1 (compliance)."
      },
      {
        "title": "Supported T-SQL",
        "body": "ALTER TABLE ... ALTER COLUMN ... ADD MASKED WITH (FUNCTION = 'default()' | 'email()' | 'partial(prefix,\"padding\",suffix)') and DROP MASKED; GRANT | REVOKE UNMASK TO principal. random() is refused. In the lab a filter on a masked column compares the masked value; SQL Server compares the real one, which is why masking is not a security boundary."
      }
    ],
    "starter_source": "-- Support agents see masked contact details; compliance sees the real values.\nALTER TABLE dbo.contacts ALTER COLUMN email ADD MASKED WITH (FUNCTION = 'default()');\n",
    "fixtures": [
      {
        "id": "gov-dynamic-masking-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "agent-and-auditor",
        "description": "Rows each principal gets, enforced on the lab catalog."
      }
    ],
    "hidden_check_refs": [
      "agent-only"
    ],
    "edge_check_refs": [
      "auditor-only"
    ],
    "hints": [
      "partial(prefix, \"padding\", suffix) keeps prefix characters at the start and suffix at the end.",
      "UNMASK is granted to the principals that must see real values; everyone else gets the masks."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Each masked column keeps its data; the mask is applied when a principal without UNMASK reads it. email() keeps the first letter, partial(0,\"XXX-XXX-\",4) keeps the last four digits, default() shows 1900-01-01 for a date. GRANT UNMASK TO compliance lets auditors see real values. NULLs stay NULL.",
    "follow_ups": [
      "An agent runs SELECT customer_id FROM dbo.contacts WHERE phone LIKE '555-201%'. What does SQL Server return, and why does that make masking a convenience rather than a security control?"
    ],
    "canonical_placement": {
      "domain": "governance",
      "topic": "dynamic-data-masking"
    },
    "related_associations": [
      "cloud-lab/sql-pool"
    ],
    "recommendation": {
      "rank": 3,
      "reason": "Data governance progression"
    },
    "validator_version": "rows-v2",
    "validation": {
      "kind": "rows",
      "ordered": false,
      "duplicate_sensitive": true,
      "relative_tolerance": 1e-09,
      "absolute_tolerance": 1e-06,
      "required_columns": [
        "principal",
        "denied",
        "customer_id",
        "email",
        "phone",
        "birth_date"
      ],
      "exact_schema": [
        "principal",
        "denied",
        "customer_id",
        "email",
        "phone",
        "birth_date"
      ],
      "forbidden_extra_columns": true,
      "null_semantics": "equal"
    },
    "runtime_requirements": [
      "sqlpool-simulator"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench: row-level security, column-level security and dynamic data masking in Synapse / Fabric Warehouse; Unity Catalog row filters, column masks and tags (Microsoft Learn)",
      "fixtures": "Authored scenarios; expected rows computed by running the reference and reviewed"
    },
    "constraints": {
      "truth": "T-SQL security translated to DuckDB, not SQL Server: rows and values enforced on an isolated local catalog."
    },
    "truth": "simulated"
  },
  {
    "schema_version": 1,
    "id": "gov-uc-row-filter",
    "version": "1",
    "title": "Unity Catalog row filter by region",
    "difficulty": "medium",
    "topics": [
      "row-filter",
      "unity-catalog",
      "governance"
    ],
    "tags": [
      "cloud-lab",
      "governance",
      "databricks",
      "simulated"
    ],
    "origin": "authored",
    "language": "databricks-grants",
    "runtime": "datapass-databricks-sim-v1",
    "prompt": "main.silver.orders is readable by all account users. Members of emea_analysts must see only EMEA orders, members of amer_analysts only AMER orders, and members of admins every order. Anyone else sees no rows. Write the SQL UDF and bind it to the table as a row filter, in the workspace's grants.sql.",
    "sections": [
      {
        "title": "Simulator",
        "body": "Your grants.sql runs on the lab's simulated Azure Databricks Unity Catalog: grants, row filters, column masks and tags follow Databricks' rules, and each check runs real queries as each principal on an isolated local catalog (DuckDB): Unity Catalog row filters and column masks translated to DuckDB, not Databricks. Nothing connects to Azure Databricks."
      },
      {
        "title": "Graded",
        "body": "SELECT order_id, region, amount FROM main.silver.orders run as ana@corp.com (emea_analysts), ben@corp.com (amer_analysts), root@corp.com (admins) and bob@corp.com (no group)."
      },
      {
        "title": "Supported SQL",
        "body": "CREATE [OR REPLACE] FUNCTION main.<schema>.<name>(<param> <TYPE>) RETURN <expression>; ALTER TABLE ... SET ROW FILTER <function> ON (<columns>) | DROP ROW FILTER. is_account_group_member('<group>'), is_member('<group>') and current_user() are resolved for the principal."
      }
    ],
    "starter_source": "GRANT USE SCHEMA ON SCHEMA main.silver TO `account users`;\nGRANT SELECT ON TABLE main.silver.orders TO `account users`;\n-- Analysts see their region's orders; admins see every order.\nCREATE OR REPLACE FUNCTION main.silver.orders_region_filter(region STRING)\nRETURN region = 'EMEA';\n",
    "fixtures": [
      {
        "id": "gov-uc-row-filter-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "analysts",
        "description": "Rows each principal gets, enforced on the lab catalog."
      }
    ],
    "hidden_check_refs": [
      "admin-and-outsider"
    ],
    "edge_check_refs": [
      "emea-only"
    ],
    "hints": [
      "A row filter keeps the rows where the function returns true, for the user who runs the query.",
      "The admin bypass must be its own OR branch: it must not widen what analysts see."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "The UDF returns true for admins whatever the region, and for analysts only on their own region; SET ROW FILTER binds it to the table, so every query on main.silver.orders is filtered for its user. Orders without a region match no analyst branch and stay visible to admins only.",
    "follow_ups": [
      "How would you express the region of each group in a mapping table instead of the function body? (The lab keeps filters as one expression; Databricks also allows a lookup in the function.)"
    ],
    "canonical_placement": {
      "domain": "governance",
      "topic": "row-filter"
    },
    "related_associations": [
      "cloud-lab/databricks"
    ],
    "recommendation": {
      "rank": 4,
      "reason": "Data governance progression"
    },
    "validator_version": "rows-v2",
    "validation": {
      "kind": "rows",
      "ordered": false,
      "duplicate_sensitive": true,
      "relative_tolerance": 1e-09,
      "absolute_tolerance": 1e-06,
      "required_columns": [
        "principal",
        "denied",
        "order_id",
        "region",
        "amount"
      ],
      "exact_schema": [
        "principal",
        "denied",
        "order_id",
        "region",
        "amount"
      ],
      "forbidden_extra_columns": true,
      "null_semantics": "equal"
    },
    "runtime_requirements": [
      "databricks-simulator"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench: row-level security, column-level security and dynamic data masking in Synapse / Fabric Warehouse; Unity Catalog row filters, column masks and tags (Microsoft Learn)",
      "fixtures": "Authored scenarios; expected rows computed by running the reference and reviewed"
    },
    "constraints": {
      "truth": "Unity Catalog row filters and column masks translated to DuckDB, not Databricks: enforced on an isolated local catalog."
    },
    "truth": "simulated"
  },
  {
    "schema_version": 1,
    "id": "gov-uc-pii-masks",
    "version": "1",
    "title": "Mask every PII-tagged column",
    "difficulty": "medium",
    "topics": [
      "column-mask",
      "pii-tags",
      "unity-catalog",
      "governance"
    ],
    "tags": [
      "cloud-lab",
      "governance",
      "databricks",
      "simulated"
    ],
    "origin": "authored",
    "language": "databricks-grants",
    "runtime": "datapass-databricks-sim-v1",
    "prompt": "main.silver.customers is readable by all account users, and its email and phone columns are tagged pii. Mask every pii column for everyone except the group pii_readers: the email as '***@***' and the phone as '*** ' followed by its last two characters. Members of pii_readers see the real values.",
    "sections": [
      {
        "title": "Simulator",
        "body": "Your grants.sql runs on the lab's simulated Azure Databricks Unity Catalog: grants, row filters, column masks and tags follow Databricks' rules, and each check runs real queries as each principal on an isolated local catalog (DuckDB): Unity Catalog row filters and column masks translated to DuckDB, not Databricks. Nothing connects to Azure Databricks."
      },
      {
        "title": "Graded",
        "body": "For each column tagged pii: does ana@corp.com (analysts) see it masked, and does hr@corp.com (pii_readers) see it unmasked (a real query per column: masked means no non-null value comes back unchanged); and the customers query as both."
      },
      {
        "title": "Supported SQL",
        "body": "CREATE [OR REPLACE] FUNCTION ... RETURN <expression>; ALTER TABLE ... ALTER COLUMN ... SET MASK <function> [USING COLUMNS (...)] | DROP MASK; ALTER TABLE ... ALTER COLUMN ... SET TAGS ('key' = 'value') | UNSET TAGS ('key')."
      }
    ],
    "starter_source": "GRANT USE SCHEMA ON SCHEMA main.silver TO `account users`;\nGRANT SELECT ON TABLE main.silver.customers TO `account users`;\n-- Classify the personal data.\nALTER TABLE main.silver.customers ALTER COLUMN email SET TAGS ('pii' = 'email');\nALTER TABLE main.silver.customers ALTER COLUMN phone SET TAGS ('pii' = 'phone');\n-- TODO: mask the pii columns.\n",
    "fixtures": [
      {
        "id": "gov-uc-pii-masks-fixtures",
        "version": "1"
      }
    ],
    "visible_checks": [
      {
        "id": "analyst-masked",
        "description": "Rows each principal gets, enforced on the lab catalog."
      }
    ],
    "hidden_check_refs": [
      "reader-unmasked"
    ],
    "edge_check_refs": [
      "customer-rows"
    ],
    "hints": [
      "A column mask is a SQL UDF whose first parameter is the column's value; what it returns is what the user sees.",
      "The PII check follows the tags: a pii column without a mask is a leak, and so is a mask that lets the wrong group through."
    ],
    "solution": {
      "available": true,
      "reveal": "explicit"
    },
    "explanation": "Tags classify the data; masks enforce it. Each pii column gets SET MASK with a UDF that returns the real value only for pii_readers. The check walks every column tagged pii and runs a real query as each principal, so an untagged, unmasked or wrongly granted column shows up.",
    "follow_ups": [
      "Which group would you give the pii_readers role in production, and how would you audit who is in it?"
    ],
    "canonical_placement": {
      "domain": "governance",
      "topic": "column-mask"
    },
    "related_associations": [
      "cloud-lab/databricks"
    ],
    "recommendation": {
      "rank": 5,
      "reason": "Data governance progression"
    },
    "validator_version": "rows-v2",
    "validation": {
      "kind": "rows",
      "ordered": false,
      "duplicate_sensitive": true,
      "relative_tolerance": 1e-09,
      "absolute_tolerance": 1e-06,
      "required_columns": [],
      "exact_schema": null,
      "forbidden_extra_columns": true,
      "null_semantics": "equal"
    },
    "runtime_requirements": [
      "databricks-simulator"
    ],
    "provenance": {
      "source": "Authored for Datapass Workbench: row-level security, column-level security and dynamic data masking in Synapse / Fabric Warehouse; Unity Catalog row filters, column masks and tags (Microsoft Learn)",
      "fixtures": "Authored scenarios; expected rows computed by running the reference and reviewed"
    },
    "constraints": {
      "truth": "Unity Catalog row filters and column masks translated to DuckDB, not Databricks: enforced on an isolated local catalog."
    },
    "truth": "simulated"
  }
]
