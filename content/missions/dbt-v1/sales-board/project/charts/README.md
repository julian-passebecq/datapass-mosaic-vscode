# Boards

dbt Charts boards live here, one YAML file per board. `dct validate`, `dct render` and `dct serve` read them; the
dbt Lab's dbt Charts panel lists them. `dct docs` and `dct examples` show the YAML language offline.
