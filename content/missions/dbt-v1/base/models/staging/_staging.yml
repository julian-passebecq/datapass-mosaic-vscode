version: 2

models:
  - name: stg_shop__orders
    columns:
      - name: order_id
        data_tests: [unique, not_null]
  - name: stg_shop__order_lines
    columns:
      - name: order_id
        data_tests: [not_null]
  - name: stg_erp__products
    columns:
      - name: product_id
        data_tests: [unique, not_null]
